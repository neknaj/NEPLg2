#indent 4

#import "std/mem"
#use std::mem::*
#import "std/result"
#use std::result::*
#import "std/option"
#use std::option::*
#import "std/math"
#use std::math::*

// Minimal safe string helpers (placeholders)

fn len <(str)->i32> (s):
    load_i32 s

fn from_i32 <(i32)*>str> (_x):
    let out <i32> alloc 4;
    store_i32 out 0;
    out

fn to_i32 <(str)*>ResultI32> (_s):
    ResultI32::Err 1

fn find <(str,str)*>OptionI32> (_s, _pat):
    OptionI32::None
