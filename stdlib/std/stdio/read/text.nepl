//: # std/stdio/read/text
//: stdin byte input を UTF-8 text として読み込む API
//:
//: ### 目的
//: - `read/bytes` の raw byte read 結果を `std/text` の UTF-8 検証境界へ渡し、text API の失敗を `StdErrorKind` として返します。
//: - 1 行読み込みの scratch buffer と fd read 境界は `read/buffer` に閉じ、ここでは text conversion だけを担当します。
//:
//: ### 注意
//: - `read_all` / `read_line` は互換 API として失敗時に空文字列を返します。error を扱う場合は `stdio_read_all_text_result` / `stdio_read_line_result` を使ってください。

#indent 4

#import "alloc/io" as *
#import "core/result" as *
#import "std/text" as *
#import "./buffer" as *
#import "./bytes" as *

//: stdio_read_all_text_result: 標準入力を UTF-8 [検証/けんしょう]して[読/よ]み[込/こ]む
//:
//: [目的/もくてき]:
//: - stdin の raw byte [列/れつ]を text として[扱/あつか]う[前/まえ]に UTF-8 として[妥当/だとう]か[検証/けんしょう]し、invalid input を caller へ `Result` で[返/かえ]します。
//:
//: [実装/じっそう]:
//: - `stdio_read_all_bytes_result` で EOF まで[読/よ]み、成功時だけ `std/text` の checked conversion へ[委譲/いじょう]します。
//:
//: [注意/ちゅうい]:
//: - 入力 buffer は成功時も失敗時も[消費/しょうひ]されます。
//: - 互換 API の `read_all` は、この関数の失敗を空文字列へ[丸/まる]めます。error を[扱/あつか]うコードではこの関数を[使/つか]ってください。
//:
//: [計算量/けいさんりょう]:
//: - O(n)
pub fn stdio_read_all_text_result <()*>Result<str, StdErrorKind>> ():
    match stdio_read_all_bytes_result:
        Result::Ok bytes:
            text_bytebuf_to_utf8_str_result bytes
        Result::Err e:
            Result<str, StdErrorKind>::Err e

//: read_all: 標準入力を EOF まで読み込む
//:
//: 目的:
//: - stdin から EOF まで読み込み、文字列として返します。
//:
//: 実装(アルゴリズム):
//: - `stdio_read_all_text_result` で UTF-8 検証付きの text read を行います。
//:
//: 注意(重要):
//: - 失敗時は空文字列を返します。
//: - error を扱う必要がある場合は `stdio_read_all_text_result` を使ってください。
//:
//: 計算量:
//: - O(n)
//: read_all: 使い方の最小例
//:
//: [目的/もくてき]:
//: - `read_all` の典型的な使い方を最小構成で示します。
//: - 値を返す関数は結果の受け取り方も合わせて確認できます。
//:
//: [注意/ちゅうい]:
//: - 例は用途が伝わる最短の呼び出しに絞っています。
//: - 境界条件や詳細な正当性検証は専用テストで扱います。
//:
//: neplg2:test
//: stdin: "sample input"
//: stdout: "sample input"
//: ```neplg2
//:| #entry main
//:| #indent 4
//:| #target std
//:| #import "std/stdio" as *
//: fn main <()*>()> ():
//:     print read_all;
//: ```
pub fn read_all <()*>str> ():
    match stdio_read_all_text_result:
        Result::Ok text:
            text
        Result::Err _e:
            ""

//: stdio_read_line_result: 1 行を UTF-8 検証付きで読み込む
//:
//: 目的:
//: - newline か EOF まで raw bytes を読み込み、UTF-8 として妥当な場合だけ `str` として返します。
//:
//: 実装(アルゴリズム):
//: - `stdio_read_line_buffer_result` で 1 行ぶんの raw bytes を読み込み、成功時だけ `text_bytebuf_to_utf8_str_result` へ渡します。
//: - `fd_read` scratch と raw `MemPtr` view は `read/buffer` の所有権境界に閉じています。
//:
//: 注意(重要):
//: - 最大 1024 バイトまでです。
//: - 失敗時は `StdErrorKind` を返します。互換 API の `read_line` は失敗を空文字列へ丸めます。
//:
//: 計算量:
//: - O(n)
pub fn stdio_read_line_result <()*>Result<str, StdErrorKind>> ():
    match stdio_read_line_buffer_result:
        Result::Err e:
            Result<str, StdErrorKind>::Err e
        Result::Ok bytes:
            text_bytebuf_to_utf8_str_result bytes

//: read_line: 1 行読み込む
//:
//: 目的:
//: - newline か EOF まで読み込み、文字列として返します。
//:
//: 実装(アルゴリズム):
//: - `stdio_read_line_result` に委譲し、raw byte 列を UTF-8 として検証してから `str` にします。
//:
//: 注意(重要):
//: - 最大 1024 バイトまでです。
//: - 失敗時は空文字列を返します。error を扱う必要がある場合は `stdio_read_line_result` を使ってください。
//:
//: 計算量:
//: - O(n)
//: read_line: 使い方の最小例
//:
//: [目的/もくてき]:
//: - `read_line` の典型的な使い方を最小構成で示します。
//: - 値を返す関数は結果の受け取り方も合わせて確認できます。
//:
//: [注意/ちゅうい]:
//: - 例は用途が伝わる最短の呼び出しに絞っています。
//: - 境界条件や詳細な正当性検証は専用テストで扱います。
//:
//: neplg2:test
//: stdin: "line1\nline2"
//: stdout: "line1"
//: ```neplg2
//:| #entry main
//:| #indent 4
//:| #target std
//:| #import "std/stdio" as *
//: fn main <()*>()> ():
//:     print read_line;
//: ```
pub fn noshadow read_line <()*>str> ():
    match stdio_read_line_result:
        Result::Ok text:
            text
        Result::Err _e:
            ""
