//: # text
//: UTF-8 text [境界/きょうかい]で[使/つか]う checked [変換/へんかん]を[集/あつ]める facade
//:
//: ### [目的/もくてき]
//: - filesystem や stdin から[入/はい]る `ByteBuf` を、UTF-8 として[検証/けんしょう]してから `str` へ[変換/へんかん]します。
//: - root は通常利用者向けの checked conversion だけを再公開し、raw `MemPtr` helper は明示 submodule import に閉じます。
//:
//: ### [構成/こうせい]
//: - `text/convert` は validated `ByteBuf` を checked `str` へ[変換/へんかん]します。
//: - `text/validate` / `text/decode` は raw byte [列/れつ]を扱う boundary module なので、必要な実装だけが明示 import します。

#indent 4

pub #import "./text/convert" as @merge
