//: cast: core/cast.nepl に関する機能を提供するライブラリ
//:
//: [目的/もくてき]:
//: - このモジュールの公開 API を提供します。
//: - 実装の変更時に最小限の doctest 実行経路を維持します。
//:
//: [注意/ちゅうい]:
//: - 利用時は各関数の「目的」「注意」「計算量」を確認してください。
//:
//: neplg2:test[skip]
//: ```neplg2
//:| #entry main
//:| #target std
//:| fn main <()*>i32> ():
//:     ()
//:     0
//: ```

#indent 4

#import "core/mem" as *
#import "core/math" as *
#import "core/result" as *

// cast: 型変換ユーティリティ
//
// 目的:
// - 数値/ビット/ポインタの変換を提供します。
//
// 実装(アルゴリズム):
// - 多くは intrinsic に委譲します。
//
// 注意(重要):
// - bitcast は数値変換ではなくビット列の再解釈です。
// - ポインタの再解釈は型安全化方針に反するため、このモジュールでは公開しません。
//
// 計算量:
// - すべて O(1)

// cast: i32 -> f32 変換のエイリアス
//: cast: 主な用途
//:
//: [目的/もくてき]:
//: - cast の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(i32)->f32> (v):
    #intrinsic "i32_to_f32" <> (v)

// cast: f32 -> i32 変換のエイリアス
//: cast: 主な用途
//:
//: [目的/もくてき]:
//: - cast の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(f32)->i32> (v):
    #intrinsic "f32_to_i32" <> (v)

// cast: bool -> i32 変換のエイリアス
//: cast: 主な用途
//:
//: [目的/もくてき]:
//: - cast の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(bool)->i32> (v):
    if:
        v
        then:
            1
        else:
            0

// cast_bool_from_i32: i32 -> bool 変換
//
// 目的:
// - 0 以外を true として返します。
//
// 実装(アルゴリズム):
// - ne v 0 を返します。
//
// 注意(重要):
// - 0/1 以外も true 扱いになります。
//
// 計算量:
// - O(1)
//: cast_bool_from_i32: 主な用途
//:
//: [目的/もくてき]:
//: - cast_bool_from_i32 の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(i32)->bool> (v):
    ne v 0

// cast: i32 -> u8 変換のエイリアス
//: cast: 主な用途
//:
//: [目的/もくてき]:
//: - cast の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(i32)->u8> (v):
    #intrinsic "i32_to_u8" <> (v)

// cast: u8 -> i32 変換のエイリアス
//: cast: 主な用途
//:
//: [目的/もくてき]:
//: - cast の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn cast <(u8)->i32> (v):
    #intrinsic "u8_to_i32" <> (v)

// bitcast_i32_to_f32: i32 のビット列を f32 として解釈する
//
// 目的:
// - ビット列を保持したまま型だけ変えます。
//
// 実装(アルゴリズム):
// - intrinsic "reinterpret_i32_f32" を呼びます。
//
// 注意(重要):
// - 数値変換ではなく再解釈です。
//
// 計算量:
// - O(1)
//: bitcast_i32_to_f32: 主な用途
//:
//: [目的/もくてき]:
//: - bitcast_i32_to_f32 の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn bitcast_i32_to_f32 <(i32)->f32> (v):
    #intrinsic "reinterpret_i32_f32" <> (v)

// bitcast_f32_to_i32: f32 のビット列を i32 として解釈する
//
// 目的:
// - ビット列を保持したまま型だけ変えます。
//
// 実装(アルゴリズム):
// - intrinsic "reinterpret_f32_i32" を呼びます。
//
// 注意(重要):
// - 数値変換ではなく再解釈です。
//
// 計算量:
// - O(1)
//: bitcast_f32_to_i32: 主な用途
//:
//: [目的/もくてき]:
//: - bitcast_f32_to_i32 の主な用途と呼び出し方を示します。
//:
//: [実装/じっそう]:
//: - 定義済み処理をそのまま呼び出す薄いラッパで構成されています。
//:
//: [注意/ちゅうい]:
//: - 引数の値は関数呼び出しで移動するため、再利用時は束縛し直してください。
//:
//: [計算量/けいさんりょう]:
//: - 本体処理に準じます。
fn bitcast_f32_to_i32 <(f32)->i32> (v):
    #intrinsic "reinterpret_f32_i32" <> (v)

//: cast: i32 を i64 に変換する
fn cast <(i32)->i64> (v):
    extend_s v

//: cast: i32 を u32 に変換する
fn cast <(i32)->u32> (v):
    #intrinsic "i32_to_u32" <> (v)

//: cast: u32 を i32 に変換する
fn cast <(u32)->i32> (v):
    #intrinsic "u32_to_i32" <> (v)

//: cast: i64 を i32 に変換する
fn cast <(i64)->i32> (v):
    wrap v

//: cast: i64 を u64 に変換する
fn cast <(i64)->u64> (v):
    #intrinsic "i64_to_u64" <> (v)

//: cast: u64 を i64 に変換する
fn cast <(u64)->i64> (v):
    #intrinsic "u64_to_i64" <> (v)

//: cast: i32 を f64 に変換する
fn cast <(i32)->f64> (v):
    convert_s v

//: cast: f64 を i32 に変換する
fn cast <(f64)->i32> (v):
    trunc_s v

//: cast: i64 を f64 に変換する
fn cast <(i64)->f64> (v):
    convert_s v

//: cast: f64 を i64 に変換する
fn cast <(f64)->i64> (v):
    trunc_s v

//: cast: i64 を f32 に変換する
fn cast <(i64)->f32> (v):
    convert_s v

//: cast: f32 を i64 に変換する
fn cast <(f32)->i64> (v):
    trunc_s v

//: cast: f32 を f64 に昇格する
fn cast <(f32)->f64> (v):
    promote v

//: cast: f64 を f32 に縮小する
fn cast <(f64)->f32> (v):
    demote v

//: cast: i64 を i128 に変換する
fn cast <(i64)->i128> (v):
    to_i128 v

//: cast: i128 を i64 に変換する
fn cast <(i128)->i64> (v):
    get v "lo"

//: cast: i32 を i128 に変換する
fn cast <(i32)->i128> (v):
    to_i128 extend_s v

//: cast: i128 を i32 に変換する
fn cast <(i128)->i32> (v):
    wrap get v "lo"
