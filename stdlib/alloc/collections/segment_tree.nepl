//: # segment_tree
//: [区間和/くかんわ] query と[点更新/てんこうしん]を[扱/あつか]う Segment Tree の public facade
//:
//: ### [目的/もくてき]
//: - `SegmentTree` と `new` / `len` / `replace` / `add` / `sum_range` / `free` の既存 import path を保ったまま公開します。
//: - 型定義、storage 操作、layout 計算、更新伝播、区間 query、public API を submodule に分けます。
//:
//: ### [注意/ちゅうい]
//: - [現在/げんざい]は `i32` の[加算/かさん] monoid に[固定/こてい]しています。
//: - internal storage は typed `Vec i32` owner で、`base` は `n == 0` でも 1、storage length は `2 * base` です。
//: - `sum_range` は `[l, r)` の[半開/はんかい][区間/くかん]を[扱/あつか]います。
//: - `replace` / `add` の失敗は owner を失わないように `SegmentTreeUpdateError` で元の `SegmentTree` と `Diag` を返します。

#indent 4
#import "core/math" as *

pub #import "./segment_tree/types" as @merge
pub #import "./segment_tree/api" as @merge
