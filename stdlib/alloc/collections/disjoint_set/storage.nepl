//: # disjoint_set/storage
//: `DisjointSet` の typed `Vec<i32>` storage 操作
//:
//: ### [目的/もくてき]
//: - parent / size cell の読み書きを `Vec<i32>` API に集約します。
//: - public API と探索 helper が raw pointer や sentinel を扱わないようにします。

#indent 4

#import "core/option" as *
#import "alloc/collections/vec" as vec
#import "core/field" as *

pub fn dsu_load_owned <(&Vec<i32>,i32)->Option<i32>> (base, idx):
    match vec::get<i32> base idx:
        Option::Some v:
            some<i32> v
        Option::None:
            none<i32>

pub fn dsu_store_owned <(&Vec<i32>,i32,i32)*>()> (base, idx, value):
    vec::replace<i32> base idx value
