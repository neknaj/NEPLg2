//: # disjoint_set/api
//: `DisjointSet` public 操作の facade
//:
//: ### [目的/もくてき]
//: - `new` / `len` / `find` / `union` / `same` / `size` / `free` の public path を維持します。
//: - 診断生成、構築、借用 query、更新、cleanup を submodule に分け、API file に実装本体を戻さない構成にします。

#indent 4

pub #import "./api/diagnostic" as @merge
pub #import "./api/create" as @merge
pub #import "./api/observer" as @merge
pub #import "./api/mutation" as @merge
pub #import "./api/cleanup" as @merge
