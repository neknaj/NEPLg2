//: # list
//: `Vec` storage を[用/もち]いた[単純/たんじゅん]な list API の public facade
//:
//: ### [目的/もくてき]
//: - `new` / `cons` / `push` / `head` / `tail` / `get` / `reverse` など、list として[使/つか]うための[基本/きほん] API を既存 import path から公開します。
//: - 型定義、storage helper、基本操作、観測操作、変換操作を submodule に分けます。
//:
//: ### [注意/ちゅうい]
//: - 要素の観測・変換 API は `.T: Copy` を[要求/ようきゅう]します。現行 stdlib の collection 全体で non-Copy payload drop traversal が未完成なためです。
//: - `len` / `head` / `get` / `fold` などの observer は `List` を借用して読み取り、owner は caller が最後に `free` します。

#indent 4

pub #import "./list/types" as @merge
pub #import "./list/basic" as @merge
pub #import "./list/query" as @merge
pub #import "./list/transform" as @merge
