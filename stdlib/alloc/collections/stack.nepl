//: # stack
//: `Vec<Option<T>>` backed LIFO stack の public facade
//:
//: ### [目的/もくてき]
//: - `push` / `pop_top` / `peek` / `get` / `len` など、stack 操作の既存 import path を[保/たも]ちます。
//: - 型定義、slot storage 操作、public API を submodule に[分/わ]けます。
//:
//: ### [注意/ちゅうい]
//: - `.T` は `Copy` を[要求/ようきゅう]します。現行 stdlib には stack payload の drop traversal がまだ無いため、非 Copy payload は[扱/あつか]いません。
//: - `len` / `is_empty` / `peek` / `get` は `&Stack` を[受/う]け[取/と]る[観測/かんそく] API です。
//: - `pop_top` は[更新後/こうしんご]の owner と[値/あたい]を[同時/どうじ]に[返/かえ]します。

#indent 4
#import "core/field" as *

pub #import "./stack/types" as @merge
pub #import "./stack/api" as @merge
