//: raw: Vec sort implementation 用 raw helper facade
//:
//: [目的/もくてき]:
//: - sort algorithm が使う unchecked `MemPtr` based helper を explicit raw boundary としてまとめます。
//: - 通常の利用者は `alloc/collections/vec/sort` の `Vec` based API を使い、この module を import しません。

#indent 4

pub #import "./raw/access" as *
pub #import "./raw/quick" as *
pub #import "./raw/heap" as *
