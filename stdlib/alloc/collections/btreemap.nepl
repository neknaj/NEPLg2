//: # btreemap
//: [順序付き/じゅんじょつき] map の public facade
//:
//: ### [目的/もくてき]
//: - `BTreeMap<.K, .V>` と sorted-array map alias を既存 import path から公開します。
//: - 型定義、storage 操作、検索、public API、alias を submodule に分け、root file へ実装本体を戻さない構成にします。
//:
//: ### [注意/ちゅうい]
//: - [名前/なまえ]は `BTreeMap` ですが、[実体/じったい]は[整列済/せいれつず]み key/value [配列/はいれつ]です。
//: - `.K` / `.V` は `Copy` を[要求/ようきゅう]します。現行 stdlib には map payload の drop traversal がまだ無いため、非 Copy payload は[扱/あつか]いません。
//: - `len` / `contains` / `get` は map を[借用/しゃくよう]して[観測/かんそく]します。

#indent 4

pub #import "./btreemap/types" as @merge
pub #import "./btreemap/api" as @merge
pub #import "./btreemap/alias" as @merge
