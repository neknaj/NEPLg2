//: # btreemap/api
//: `BTreeMap` public 操作の facade
//:
//: ### [目的/もくてき]
//: - `new` / `insert` / `get` / `contains` / `remove` / `len` / `clear` / `free` の public path を維持します。
//: - 構築、借用 observer、挿入、削除、cleanup を submodule に分け、API file に実装本体を戻さない構成にします。

#indent 4
#import "core/field" as *

pub #import "./api/create" as @merge
pub #import "./api/observer" as @merge
pub #import "./api/insert" as @merge
pub #import "./api/remove" as @merge
pub #import "./api/cleanup" as @merge
