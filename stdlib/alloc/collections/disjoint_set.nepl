//: # disjoint_set
//: [素集合/そしゅうごう]の[併合/へいごう]と[代表元/だいひょうげん][検索/けんさく]を[扱/あつか]う Union-Find の public facade
//:
//: ### [目的/もくてき]
//: - `DisjointSet` と `new` / `len` / `find` / `union` / `same` / `size` / `free` の既存 import path を保ったまま公開します。
//: - 型定義、typed storage、探索 helper、public API を submodule に分け、root file に実装本体を戻さない構成にします。
//:
//: ### [注意/ちゅうい]
//: - `union` の失敗は owner を失わないように `DisjointSetUpdateError` で元の `DisjointSet` と `Diag` を返します。
//: - `find` / `same` / `size` は pure query を保つため path compression を行わず、`DisjointSet` を借用して読み取ります。

#indent 4

pub #import "./disjoint_set/types" as @merge
pub #import "./disjoint_set/api" as @merge
