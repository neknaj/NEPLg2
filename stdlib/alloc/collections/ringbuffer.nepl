//: # ringbuffer
//: `Vec Option .T` backed circular FIFO buffer の public facade
//:
//: ### [目的/もくてき]
//: - `push` / `pop` / `peek` を[備/そな]えた FIFO [基盤/きばん]の既存 import path を[保/たも]ちます。
//: - 型定義、index 計算、slot storage 操作、public API を submodule に[分/わ]けます。
//:
//: ### [注意/ちゅうい]
//: - `.T` は `Copy` を[要求/ようきゅう]します。現行 stdlib には ringbuffer payload の drop traversal がまだ無いため、非 Copy payload は[扱/あつか]いません。
//: - `len` / `cap` / `is_empty` / `peek` は owner を borrow して[観測/かんそく]します。
//: - `pop_front` は[更新後/こうしんご] owner と値を[返/かえ]し、`pop` は先頭値だけを返して buffer owner を[閉/と]じる terminal API です。

#indent 4

pub #import "./ringbuffer/types" as @merge
pub #import "./ringbuffer/api" as @merge
