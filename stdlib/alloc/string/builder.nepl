//: builder: StringBuilder による所有 byte buffer 構築 facade
//:
//: [目的/もくてき]:
//: - `StringBuilder` の型、capacity 管理、append、build を責務別 submodule から再公開します。
//: - `alloc/string/builder` を import する既存 module は、従来どおり同じ名前の builder API を使えます。
//:
//: [注意/ちゅうい]:
//: - この file は facade です。実装本体は `builder/types`、`builder/reserve`、`builder/append`、`builder/build` が所有します。

#indent 4

pub #import "./builder/types" as @merge
pub #import "./builder/reserve" as @merge
pub #import "./builder/append" as @merge
pub #import "./builder/build" as @merge
