//: search: string の byte 比較・検索・UTF-8 boundary predicate facade
//:
//: [目的/もくてき]:
//: - `str` の byte 列比較、prefix/suffix 判定、検索、UTF-8 boundary predicate を責務別 submodule から再公開します。
//: - `alloc/string/search` を import する既存 module は、従来どおり同じ名前の検索 helper を使えます。
//:
//: [注意/ちゅうい]:
//: - この file は facade です。実装本体は `search/compare`、`search/boundary`、`search/byte_find` が所有します。

#indent 4

pub #import "./search/compare" as *
pub #import "./search/boundary" as *
pub #import "./search/byte_find" as *
