//: slice: string の UTF-8 slice / char traversal / trim helper facade
//:
//: [目的/もくてき]:
//: - UTF-8 boundary を守った byte slice、char 単位走査、ASCII trim helper を責務別 submodule から再公開します。
//: - `alloc/string/slice` を import する既存 module は、従来どおり同じ名前の slice / char / trim helper を使えます。
//:
//: [注意/ちゅうい]:
//: - この file は facade です。実装本体は `slice/byte`、`slice/char`、`slice/trim` が所有します。

#indent 4

pub #import "./slice/byte" as @merge
pub #import "./slice/char" as @merge
pub #import "./slice/trim" as @merge
