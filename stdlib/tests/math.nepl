#entry main
#indent 4
#target wasi

#import "std/math"
#use std::math::*
#import "std/test"
#use std::test::*

fn main <()*> ()> ():
    assert_eq_i32 3 add 1 2;
    assert lt 1 2;
    assert le 2 2;
    ()
