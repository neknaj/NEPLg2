#indent 4
#target std

#import "core/math" as *
#import "core/mem" as *
#import "core/mem/allocator" as *
#import "core/mem/raw" as *

//: kpfenwick: 競技プログラミング向け Fenwick Tree（BIT）
//:
//: [目的/もくてき]:
//: - 点加算・区間和クエリを `O(log N)` で処理する Fenwick Tree を提供します。
//:
//: [実装/じっそう]:
//: - 1-index 内部配列 `bit[1..n]` を持つ実装です。
//:
//: [注意/ちゅうい]:
//: - 公開 API の添字は 0-index、内部では 1-index に変換します。
//:
//: neplg2:test[stdio, normalize_newlines]
//: stdout: "6 9\n"
//: ```neplg2
//:| #entry main
//:| #target std
//:| #import "kp/kpfenwick" as *
//:| #import "std/stdio" as *
//: fn main <()*> ()> ():
//:     let f <i32> fenwick_new 5;
//:     fenwick_add f 0 1;
//:     fenwick_add f 1 2;
//:     fenwick_add f 2 3;
//:     fenwick_add f 3 4;
//:     print_i32 fenwick_sum_range f 0 3;
//:     print " ";
//:     println_i32 fenwick_sum_range f 1 4;
//:     fenwick_free f
//: ```

//: fenwick_new: 長さ `n` の Fenwick Tree を作る
pub fn fenwick_new <(i32)*>i32> (n):
    let bit_len <i32> add n 1;
    let bit <i32> alloc_raw mul bit_len 4;
    let mut i <i32> 0;
    while lt i bit_len:
        do:
            let off <i32> mul i 4;
            let ptr <i32> add bit off;
            store_i32 ptr 0;
            set i add i 1;
    let f <i32> alloc_raw 8;
    store_i32 f n;
    store_i32 add f 4 bit;
    f

//: fenwick_free: Fenwick Tree の内部領域を解放する
pub fn fenwick_free <(i32)*>()> (f):
    let n <i32> load_i32 f;
    let bit <i32> load_i32 add f 4;
    let bit_len <i32> add n 1;
    let bytes <i32> mul bit_len 4;
    dealloc_raw bit bytes;
    dealloc_raw f 8

//: fenwick_add: `idx` に `delta` を加算する
pub fn fenwick_add <(i32,i32,i32)*>()> (f, idx, delta):
    let n <i32> load_i32 f;
    let bit <i32> load_i32 add f 4;
    let mut i <i32> add idx 1;
    while le i n:
        do:
            let off <i32> mul i 4;
            let ptr <i32> add bit off;
            store_i32 ptr add load_i32 ptr delta;
            let neg_i <i32> sub 0 i;
            let lb <i32> and i neg_i;
            set i add i lb;

//: fenwick_sum_prefix: 区間 `[0, r)` の和を返す
pub fn fenwick_sum_prefix <(i32,i32)*>i32> (f, r):
    let bit <i32> load_i32 add f 4;
    let mut i <i32> r;
    let mut acc <i32> 0;
    while lt 0 i:
        do:
            let off <i32> mul i 4;
            let ptr <i32> add bit off;
            let v <i32> load_i32 ptr;
            set acc add acc v;
            let neg_i <i32> sub 0 i;
            let lb <i32> and i neg_i;
            set i sub i lb;
    acc

//: fenwick_sum_range: 区間 `[l, r)` の和を返す
pub fn fenwick_sum_range <(i32,i32,i32)*>i32> (f, l, r):
    sub fenwick_sum_prefix f r fenwick_sum_prefix f l
