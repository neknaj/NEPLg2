#indent 4

//: # proof/api
//: self-host proof solver の public typed API をまとめて公開する facade です。
//:
//: [目的/もくてき]:
//: - domain ごとの API wrapper は `proof/api/*` に分離し、この file は公開境界だけを表します。
//: - wrapper は引き続き generic solver を経由し、checker-local proof や stdlib allowlist を作りません。

pub #import "./api/source" as *
pub #import "./api/module" as *
pub #import "./api/resource" as *
pub #import "./api/type" as *
pub #import "./api/effect" as *