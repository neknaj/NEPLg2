#indent 4

//: # proof/solver
//: generic proof solver entry を公開する facade です。
//:
//: [目的/もくてき]:
//: - source / module / resource / type / effect の rule 実装は `proof/solver/*` に分離します。
//: - この file は solver の public entry だけを re-export し、証明規則の肥大化を防ぎます。

pub #import "./solver/dispatch" as *