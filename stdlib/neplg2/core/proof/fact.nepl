#indent 4

//: # proof/fact
//: proof domain と typed fact model をまとめて公開する facade です。
//:
//: [目的/もくてき]:
//: - 既存の import boundary は保ちつつ、実装は `proof/domain` と `proof/fact/model` に分離します。
//: - この file には proof model の分岐実装を置かず、肥大化を防ぎます。

pub #import "./domain" as *
pub #import "./fact/model" as *