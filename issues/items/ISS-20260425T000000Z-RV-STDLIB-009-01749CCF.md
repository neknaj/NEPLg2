---
id: ISS-20260425T000000Z-RV-STDLIB-009-01749CCF
title: "巨大 stdlib ファイルが分割されていない"
area: stdlib
status: open
resolved: false
priority: P2
type: architecture
created: 2026-04-25
updated: 2026-05-08
target: "stdlib/core/math.nepl, stdlib/alloc/string.nepl, stdlib/alloc/io.nepl, stdlib/alloc/diag/error.nepl, stdlib/alloc/collections/vec.nepl, stdlib/alloc/encoding/json.nepl, stdlib/alloc/hash/sha256.nepl, stdlib/std/streamio.nepl, stdlib/std/stdio.nepl, stdlib/std/fs.nepl, stdlib/std/test.nepl, stdlib/nm/parser.nepl, stdlib/core/mem.nepl, stdlib/neplg2/cli/args.nepl, stdlib/platforms/wasix/tui.nepl"
legacy_id: RV-STDLIB-009
source: "doc/review20260425/stdlib.md#rv-stdlib-009"
---

# RV-STDLIB-009: 巨大 stdlib ファイルが分割されていない

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-STDLIB-009-01749CCF`。

## 要約

stdlib の主要 file が肥大化しています。API、内部 helper、target-specific raw body、doctest が同居しており、修正範囲の把握が難しくなっています。

## 影響

コンパイル性能にも影響します。loader が import file を丸ごと clone / merge する現状では、巨大 stdlib file はそのまま型検査候補数と clone 量を増やします。

## 修正方針

`math/i32`, `math/i64`, `math/f32`, `math/f64`、`string/parse`, `string/format`, `stdio/read`, `stdio/write` のように用途別 module へ分割します。public facade は既存 path 互換を保ちます。

## 検証

分割前後で public import path の doctest が同じ結果になることを確認します。

## 2026-05-07 追加対応メモ: alloc/diag/error types/diag/diags/outcome split

`stdlib/alloc/diag/error.nepl` は `DiagLevel` / `Diag` / `Diags` / `Outcome` の型定義、単一診断 constructor、診断群 storage owner 操作、Outcome helper が 1 file に同居していた。今回は public facade、型、単一診断、診断群、Outcome の 5 つへ責務を分離した。

実装内容:

- `stdlib/alloc/diag/error/types.nepl` を追加し、`DiagLevel`、`Span`、`DiagKind`、`Diag`、`Diags` と `diag_level_str` を所有させた。重大度分岐は `DiagLevel` enum の `match` 網羅性に乗る形を維持した。
- `stdlib/alloc/diag/error/diag.nepl` を追加し、`diag_new`、`diag_error`、`diag_add_note`、`diag_std_error_kind_str` など単一診断 helper を所有させた。
- `stdlib/alloc/diag/error/diags.nepl` を追加し、`Diags` の `Vec<Diag>` allocation fallback、push、by-value observer、free を所有させた。
- `stdlib/alloc/diag/error/outcome.nepl` を追加し、`Outcome<T,E>` と `outcome_result` / `outcome_with_diags` の `Diags` owner 解放契約を所有させた。
- `stdlib/alloc/diag/error.nepl` は submodule の public re-export と facade doctest だけに縮小した。
- `nepl-core/src/loader.rs` の exact raw-memory boundary に `alloc/diag/error/diags.nepl` と既存 renderer の `alloc/diag/diag.nepl` を追加し、raw load を root facade ではなく実装先だけに許可した。
- source policy を更新し、root facade に type/function body が戻らないこと、`Diag` note/help が owner-neutral text fragment のままであること、`Diags` / `Outcome` の owner 終端契約、exact raw-memory boundary を固定した。
- line count は `error.nepl` 30 行、`error/types.nepl` 151 行、`error/diag.nepl` 138 行、`error/diags.nepl` 117 行、`error/outcome.nepl` 309 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_diag_error_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/diag/error.nepl -i stdlib/alloc/diag/error/types.nepl -i stdlib/alloc/diag/error/diag.nepl -i stdlib/alloc/diag/error/diags.nepl -i stdlib/alloc/diag/error/outcome.nepl --no-tree -o tmp/diag-error-module-split-focused.json -j 1 --dist web/dist`: total=7, passed=7
- `node nodesrc/tests.js -i stdlib/tests/error.n.md -i stdlib/tests/diag.n.md -i stdlib/alloc/diag/error.nepl -i stdlib/alloc/diag/diag.nepl --no-tree -o tmp/diag-error-module-split-suite.json -j 1 --dist web/dist`: total=6, passed=6
- `node nodesrc/tests.js -i tests/stdlib/collections_diag.n.md --no-tree -o tmp/diag-error-module-split-collections-diag-fixed.json -j 1 --dist web/dist`: total=4, passed=4
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed
- `cargo test -p nepl-core --test effects`: 27 passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/string/search 関連 policy は passed。remote main `cc5d7662` 取り込み後の full run では別件として `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning が残る。
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/diag/error` は facade 化できたが、`core/mem.nepl`、`nm/parser`、`std/streamio`、`std/stdio`、`std/fs` などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/io bytebuf/bytebuilder/traits split

`stdlib/alloc/io.nepl` は `ByteBuf`、`ByteBuilder`、stream trait facade が同居し、raw memory capability も root file に残っていた。今回は public root facade、ByteBuf storage boundary、ByteBuilder storage boundary、raw-memory-free traits の 4 つへ責務を分離した。

実装内容:

- `stdlib/alloc/io/bytebuf.nepl` を追加し、`ByteBuf`、`io_bytebuf_from_str_result`、`io_bytebuf_to_str_result`、RegionToken 経由の ByteBuf 確定境界を所有させた。
- `stdlib/alloc/io/bytebuilder.nepl` を追加し、`ByteBuilder`、grow/reserve、append、LEB128、UTF-8 char encoding、`byte_builder_finish` を所有させた。
- `stdlib/alloc/io/traits.nepl` を追加し、`ByteReader` / `ByteWriter` / `TextReader` / `TextWriter` / `Flush` / `Close` と delegation facade だけを所有させた。
- `stdlib/alloc/io.nepl` は submodule の public re-export と root doctest だけに縮小した。
- `nepl-core/src/loader.rs` の exact raw-memory boundary から root `alloc/io.nepl` を外し、`alloc/io/bytebuf.nepl` と `alloc/io/bytebuilder.nepl` へ raw capability を移した。`io/traits.nepl` は raw-memory-free のまま固定した。
- source policy を更新し、root facade に type / trait / function body が戻らないこと、ByteBuf / ByteBuilder の owner boundary が submodule にあること、loader boundary が exact implementation path を指すことを固定した。
- line count は `io.nepl` 41 行、`io/bytebuf.nepl` 295 行、`io/bytebuilder.nepl` 448 行、`io/traits.nepl` 91 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_io_bytebuf_owner_boundary.js`: passed
- `node nodesrc/test_stdlib_builder_owner_boundary.js`: passed
- `node nodesrc/test_stdlib_bytebuf_utf8_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/io.nepl --no-tree -o tmp/alloc-io-facade-split-root.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/io/bytebuf.nepl --no-tree -o tmp/alloc-io-facade-split-bytebuf.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/io/bytebuilder.nepl --no-tree -o tmp/alloc-io-facade-split-bytebuilder.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/io/traits.nepl --no-tree -o tmp/alloc-io-facade-split-traits.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i tests/stdlib/io.n.md -i tests/stdlib/streamio.n.md -i tests/stdlib/text_utf8.n.md --no-tree -o tmp/alloc-io-facade-split-io-stream-text.json -j 1 --dist web/dist`: total=29, passed=29
- `cargo test -p nepl-core --test effects`: 27 passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed

この issue はまだ open のまま扱う。`alloc/io.nepl` root は facade 化できたが、`core/mem.nepl`、`nm/parser`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string float format/parse split

`stdlib/alloc/string/float.nepl` は root string facade からは分離済みだったが、f64/f32 formatting と parsing、raw byte 書き込み境界が 1 file に同居していた。今回は public facade、format、parse の 3 つへ責務を分離した。

実装内容:

- `stdlib/alloc/string/float/format.nepl` を追加し、`from_f64_result` / `from_f64` / `from_f32` と fixed-size output allocation を所有させた。
- `stdlib/alloc/string/float/parse.nepl` を追加し、`to_f64` / `to_f32` と符号・小数・指数 parsing を所有させた。
- `stdlib/alloc/string/float.nepl` は `format` / `parse` の public re-export と facade doctest だけに縮小した。
- `from_f64_result` の raw byte store は `float/format.nepl` へ移ったため、`nepl-core/src/loader.rs` の exact raw-memory boundary を `alloc/string/float/format.nepl` に追従させた。
- source policy を更新し、format/parse の関数本体が root facade に戻らないこと、loader boundary が format module を含むこと、float facade が小さいまま維持されることを固定した。
- line count は `float.nepl` 32 行、`float/format.nepl` 279 行、`float/parse.nepl` 284 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_string_float_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/float/format.nepl --no-tree -o tmp/string-float-format-parse-format.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string/float/parse.nepl --no-tree -o tmp/string-float-format-parse-parse.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string/float.nepl --no-tree -o tmp/string-float-format-parse-facade.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl --no-tree -o tmp/string-float-format-parse-root.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md --no-tree -o tmp/string-float-format-parse-suite.json -j 1 --dist web/dist`: total=9, passed=9
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib 関連は passed。既知 open issue `ISS-20260507T032436743Z-RESOURCE-RAW-CELL-RANGE-MODULE-EXCEE-92BDC72B` の `cell_state_raw_range.rs has 159 lines; responsibility split limit is 140` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string/float` は facade / format / parse に分割できたが、`core/mem.nepl`、`nm/parser` などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string integer format/parse split

`stdlib/alloc/string/integer.nepl` は common helper 分割後も、formatting API と parsing API が同居していた。今回は public facade、format、parse の 3 つへ責務を分離した。

実装内容:

- `stdlib/alloc/string/integer/format.nepl` を追加し、`from_i32` / `from_i64` / `from_u128` / `from_i128` と radix formatting 本体を所有させた。
- `stdlib/alloc/string/integer/parse.nepl` を追加し、`to_i32` / `to_i64` / `to_u128` / `to_i128`、overflow/range helper を所有させた。
- `stdlib/alloc/string/integer.nepl` は `common` / `format` / `parse` の public re-export と facade doctest だけに縮小した。
- `from_u128_radix` の raw byte store は `integer/format.nepl` へ移ったため、`nepl-core/src/loader.rs` の exact raw-memory boundary を `alloc/string/integer/format.nepl` に追従させた。
- qualified facade re-export に依存していた `std/stdio/print.nepl` は `alloc/string/integer/format` を直接 import するようにした。`builder_ext.nepl` と `float.nepl` も integer format module を直接 import する。
- source policy を更新し、format/parse の関数本体が root facade に戻らないこと、loader boundary が format module を含むこと、integer facade が小さいまま維持されることを固定した。
- line count は `integer.nepl` 34 行、`integer/common.nepl` 481 行、`integer/format.nepl` 251 行、`integer/parse.nepl` 374 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_string_integer_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_stdio_print_i32_boundary.js`: passed
- `node nodesrc/test_stdlib_string_float_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/integer/format.nepl --no-tree -o tmp/string-integer-format-parse-format-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string/integer/parse.nepl --no-tree -o tmp/string-integer-format-parse-parse-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string/integer.nepl --no-tree -o tmp/string-integer-format-parse-facade-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl --no-tree -o tmp/string-integer-format-parse-root-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md --no-tree -o tmp/string-integer-format-parse-suite-fixed.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/tests.js -i stdlib/std/stdio/print.nepl --no-tree -o tmp/string-integer-format-parse-stdio-print.json -j 1 --dist web/dist`: total=4, passed=4
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib 関連は passed。既知 open issue `ISS-20260507T032436743Z-RESOURCE-RAW-CELL-RANGE-MODULE-EXCEE-92BDC72B` の `cell_state_raw_range.rs has 159 lines; responsibility split limit is 140` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string/integer` は facade / common / format / parse に分割できたが、`alloc/string/float.nepl`、`core/mem.nepl`、`nm/parser` などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string integer common helper split

`stdlib/alloc/string/integer.nepl` は root `alloc/string` から分離済みだったが、bool 表示、digit/radix 判定、u128/i128 補助演算、public conversion API が 1 file に残っていた。今回、public conversion API と共通 arithmetic helper の責務をさらに分けた。

実装内容:

- `stdlib/alloc/string/integer/common.nepl` を追加し、`from_bool`、`to_bool`、digit/radix helper、`U128DivRem`、u128/i128 bit/helper 演算を所有させた。
- `stdlib/alloc/string/integer.nepl` は `./integer/common` を public re-export し、`from_i32_radix` / `to_u128_radix` などの conversion API 本体に集中させた。
- `nodesrc/test_stdlib_string_integer_boundary.js` を拡張し、common helper が root や `integer.nepl` 本体へ戻らないこと、`integer.nepl` が common を re-export すること、公開 conversion API が `integer.nepl` に残ること、line count boundary を固定した。
- `integer.nepl` は 581 行、`integer/common.nepl` は 481 行になった。numeric conversion の parse/format 分離は別段階で継続する。

検証:

- `node nodesrc/test_stdlib_string_integer_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/integer/common.nepl --no-tree -o tmp/string-integer-common-module-common-fixed.json -j 1 --dist web/dist`: total=2, passed=2
- `node nodesrc/tests.js -i stdlib/alloc/string/integer.nepl --no-tree -o tmp/string-integer-common-module-integer-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl --no-tree -o tmp/string-integer-common-module-root-fixed.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md --no-tree -o tmp/string-integer-common-module-suite-fixed.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string/integer.nepl` の共通 helper は分離したが、integer parse/format のさらなる分割、`core/math`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser`、`core/mem` など他の巨大 file / submodule 分割は継続する。

## 2026-05-07 追加対応メモ: alloc/string integer common bool/radix/u128 split

`stdlib/alloc/string/integer/common.nepl` は前段で integer facade から分離したものの、bool 表示・解析、digit/radix 判定、u128/i128 補助演算が 1 file に同居し続けていた。今回は `common` 自体を public facade にし、helper を責務別 submodule へ分離した。

実装内容:

- `stdlib/alloc/string/integer/common/bool.nepl` を追加し、`from_bool` / `to_bool` と `str_eq` 依存だけを所有させた。
- `stdlib/alloc/string/integer/common/radix.nepl` を追加し、`digit_to_char_lower` / `digit_from_char` / `validate_radix` を所有させた。
- `stdlib/alloc/string/integer/common/u128.nepl` を追加し、`U128DivRem`、u128/i128 bit reinterpretation、small multiply/add/divrem、overflow 判定を所有させた。
- `stdlib/alloc/string/integer/common.nepl` は submodule の public re-export だけを持つ facade に縮小した。`format.nepl` / `parse.nepl` は引き続き `./common` を import するため、公開 helper 名と呼び出し側の import 経路は維持される。
- `nodesrc/test_stdlib_string_integer_boundary.js` を更新し、common facade に helper body が戻らないこと、bool/radix/u128 の所有 module、line count boundary を固定した。
- line count は `common.nepl` 15、`common/bool.nepl` 97、`common/radix.nepl` 89、`common/u128.nepl` 291。

検証:

- `node nodesrc/test_stdlib_string_integer_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/integer/common.nepl -i stdlib/alloc/string/integer/common/bool.nepl -i stdlib/alloc/string/integer/common/radix.nepl -i stdlib/alloc/string/integer/common/u128.nepl --no-tree -o tmp/string-integer-common-helper-split-common.json -j 1 --dist web/dist`: total=2, passed=2
- `node nodesrc/tests.js -i stdlib/alloc/string/integer/format.nepl -i stdlib/alloc/string/integer/parse.nepl -i stdlib/alloc/string/integer.nepl -i stdlib/alloc/string.nepl --no-tree -o tmp/string-integer-common-helper-split-integer.json -j 1 --dist web/dist`: total=4, passed=4
- `node nodesrc/tests.js -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md --no-tree -o tmp/string-integer-common-helper-split-suite.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string/integer/common` は facade / bool / radix / u128 に分割できたが、`core/mem.nepl`、selfhost compiler の巨大 file、`nm/parser` などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec mutation/query facade split

`stdlib/alloc/collections/vec.nepl` に残っていた基本 mutation / cleanup と `get` を submodule へ分離した。

実装内容:

- `stdlib/alloc/collections/vec/mutation.nepl` を追加し、`push`、`replace`、`pop`、`clear`、`free` を所有させた。
- `get` を `stdlib/alloc/collections/vec/query.nepl` へ移し、borrowed Copy read の責務を query module に揃えた。
- `mutation.nepl` の write/read は direct offset 計算を持たず、`vec/raw::vec_write_at` / `vec/raw::vec_read_at` に委譲するようにした。
- `stdlib/alloc/collections/vec.nepl` は mutation/query API について root namespace 互換 wrapper だけを持つ facade にした。
- `nepl-core/src/loader.rs` の configured raw-memory boundary に `alloc/collections/vec/mutation.nepl` を追加した。
- `nodesrc/test_stdlib_vec_no_unsafe_unwraps.js` を拡張し、mutation 本体が submodule にあり、root wrapper が `vec_mutation::` へ委譲すること、`get` が `vec/query` にあること、loader boundary が `vec/mutation.nepl` を含むことを固定した。
- `vec.nepl` root は 360 行まで縮小した。`vec/mutation.nepl` は 266 行、`vec/query.nepl` は 356 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/mutation.nepl --no-tree -o tmp/vec-mutation-query-facade-mutation.json -j 1 --dist web/dist`: total=5, passed=5
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/query.nepl --no-tree -o tmp/vec-mutation-query-facade-query.json -j 1 --dist web/dist`: total=7, passed=7
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec.nepl --no-tree -o tmp/vec-mutation-query-facade-root.json -j 1 --dist web/dist`: total=3, passed=3
- `node nodesrc/tests.js -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-mutation-query-facade-suite.json -j 1 --dist web/dist`: total=9, passed=9

この issue はまだ open のまま扱う。`alloc/collections/vec.nepl` はほぼ facade になったが、`OwnedBuffer<T>` + initialized prefix への再設計は未完であり、他の巨大 stdlib file も残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec transform/query split

`stdlib/alloc/collections/vec.nepl` に残っていた owner-consuming transform 群と borrowed query 群を submodule へ分離した。

実装内容:

- `stdlib/alloc/collections/vec/transform.nepl` を追加し、`map`、`filter`、`partition`、`take_while`、`drop_while` を所有させた。
- `stdlib/alloc/collections/vec/query.nepl` を追加し、`count`、`fold`、`reduce`、`find`、`any`、`all` を所有させた。
- `stdlib/alloc/collections/vec.nepl` は transform/query API について root namespace 互換 wrapper だけを持つ facade にした。
- `nepl-core/src/loader.rs` の configured raw-memory boundary に `alloc/collections/vec/transform.nepl` と `alloc/collections/vec/query.nepl` を追加した。現行 `Vec` はまだ `MemPtr<T>` storage owner を持つため、raw helper 経由の storage read/write を exact path で管理する。
- `nodesrc/test_stdlib_vec_no_unsafe_unwraps.js` を拡張し、transform/query 本体が submodule にあり、root wrapper が `vec_transform::` / `vec_query::` へ委譲すること、loader boundary が分割先を含むことを固定した。
- `vec.nepl` root は 623 行まで縮小した。`vec/transform.nepl` は 369 行、`vec/query.nepl` は 304 行。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/transform.nepl --no-tree -o tmp/vec-query-transform-module-transform.json -j 1 --dist web/dist`: total=5, passed=5
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/query.nepl --no-tree -o tmp/vec-query-transform-module-query.json -j 1 --dist web/dist`: total=6, passed=6
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec.nepl --no-tree -o tmp/vec-query-transform-module-root.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/tests.js -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-query-transform-module-suite.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `cargo check -p nepl-core --tests`: passed
- `cargo test -p nepl-core --test effects`: 27 passed

この issue はまだ open のまま扱う。`Vec` root は push/get/replace/pop/clear/free と facade wrapper が残る状態まで縮小したが、`core/math`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` など他の巨大 file は未分割である。

## 2026-05-07 追加対応メモ: alloc/collections/vec raw helper split

`stdlib/alloc/collections/vec.nepl` に残っていた raw `MemPtr<T>` load/store helper と scan/fold helper を `stdlib/alloc/collections/vec/raw.nepl` へ分離した。

実装内容:

- `stdlib/alloc/collections/vec/raw.nepl` を追加し、`vec_read_at`、`vec_write_at`、`vec_fold_impl`、`vec_reduce_impl`、`vec_find_impl`、`vec_take_while_len_impl`、`vec_write_prefix_impl` を所有させた。
- `stdlib/alloc/collections/vec.nepl` は既存 doctest と root namespace 呼び出しを保つ thin facade wrapper だけを持ち、offset 計算や typed `load` / `store` 本体は `vec/raw` へ委譲する。
- `nepl-core/src/loader.rs` の configured raw-memory boundary に `alloc/collections/vec/raw.nepl` を追加し、raw memory intrinsic を許可する exact stdlib path を分割先へ追従させた。
- `nodesrc/test_stdlib_vec_no_unsafe_unwraps.js` を拡張し、raw helper 実装が root に戻らないこと、root wrapper が `vec_raw::` へ委譲すること、loader boundary が `vec/raw.nepl` を含むことを固定した。

検証:

- `trunk build`: passed
- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/raw.nepl --no-tree -o tmp/vec-raw-helper-module-raw.json -j 1 --dist web/dist`: total=7, passed=7
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec.nepl --no-tree -o tmp/vec-raw-helper-module-root.json -j 1 --dist web/dist`: total=20, passed=20
- `node nodesrc/tests.js -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-raw-helper-module-vec-suite.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `cargo check -p nepl-core --tests`: passed
- `cargo test -p nepl-core --test effects`: 27 passed

この issue はまだ open のまま扱う。`Vec` は型/storage/access/raw helper の分割が進んだが、mutation / transform / query の実装本体は root に残っている。`core/math`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` など他の巨大 file も未分割である。

## 2026-05-06 追加対応メモ: alloc/string root facade boundary

`stdlib/alloc/string.nepl` に残っていた concat、builder extension、`find` を submodule へ分離し、root を public re-export だけの facade にした。

実装内容:

- `stdlib/alloc/string/concat.nepl` を追加し、`concat_result`、`concat`、`concat3` を所有させた。
- `stdlib/alloc/string/builder_ext.nepl` を追加し、`sb_append_slice_result`、`sb_append_i32_result`、`sb_append_i32` を所有させた。
- `stdlib/alloc/string/find.nepl` を追加し、`find` を `Option<i32>` returning byte search helper として所有させた。
- `stdlib/alloc/string.nepl` は `utf8` / `storage` / `access` / `builder` / `search` / `slice` / `split` / `integer` / `float` / `concat` / `builder_ext` / `find` の再 export だけを持つ facade にした。
- remote main で先に入っていた `builder` / `integer` の raw-memory boundary に続けて、今回分離した `concat` / `builder_ext` を configured stdlib exact path の raw-memory boundary に追加した。
- `stdlib/neplg2` の `#import "alloc/string" as string` による qualified root facade 依存をやめ、`access` / `search` / `slice` / `concat` の direct import に置き換えた。
- `nodesrc/test_stdlib_string_facade_boundary.js` を追加し、root に実装本体が戻らないこと、selfhost が broad `string::` namespace に戻らないこと、loader boundary が分割先に追従することを固定した。

検証:

- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_selfhost_string_helpers_boundary.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `cargo test -p nepl-core --test effects`: 27 passed
- `trunk build`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: string facade / selfhost direct import / match decision tree は passed。既存の `nodesrc/test_stdlib_io_bytebuf_owner_boundary.js` warning は残る。

focused doctest:

- `tmp/string-root-facade-boundary-rebased.json` は 900 秒で command timeout。partial JSON は total=55, passed=43, failed=10, errored=2。root facade の `undefined identifier` と string submodule raw-memory boundary failure は解消した。
- 残件として Vec raw-memory boundary、StringBuilder/ByteBuilder owner chain、selfhost lexer/import_spec strict checker 追従不足が露出したため、以下を追加した。
  - `ISS-20260506T155747740Z-VEC-RAW-MEMORY-COLLECTION-LACKS-EXAC-3F7CA727`
  - `ISS-20260506T155757405Z-STRING-FLOAT-AND-CHAR-BUILDER-OWNER--37EDC044`
  - `ISS-20260506T155806325Z-SELFHOST-LEXER-AND-IMPORT-SPEC-FIXTU-68BADCC8`

この issue はまだ open のまま扱う。`alloc/string.nepl` root は facade 化できたが、`alloc/collections/vec.nepl`、`core/mem.nepl` など残る巨大 file / raw-memory-backed collection の分割を継続する。

## 2026-04-28 再レビュー

remote main 取り込み後の stdlib line count 上位は次の通りで、分割 issue はまだ open のまま妥当である。

- `stdlib/core/math.nepl`: 4435 lines
- `stdlib/alloc/string.nepl`: 2479 lines
- `stdlib/alloc/collections/vec.nepl`: 1701 lines
- `stdlib/std/streamio.nepl`: 1649 lines
- `stdlib/std/stdio.nepl`: 1636 lines
- `stdlib/std/fs.nepl`: 1566 lines
- `stdlib/nm/parser.nepl`: 1358 lines
- `stdlib/core/mem.nepl`: 1115 lines
- `stdlib/alloc/collections/vec/sort.nepl`: 1049 lines
- `stdlib/neplg2/cli/args.nepl`: 661 lines

`string.nepl` は再実装で安全性が上がったが、UTF-8、builder、numeric parse/format、split、float conversion が同居している。self-host では text/token/diagnostic から広く使われるため、機能単位で `alloc/string/*` へ分ける必要がある。

`stdlib/neplg2/cli/args.nepl` は 661 lines で既に parser state、enum、doctest、classifier が同居しており、S6 の driver/reporter 実装前に `args/types`, `args/parse`, `args/tests` 相当へ分ける候補として追跡する。

## 2026-04-29 対応メモ

`alloc/string.nepl` の byte scanner helper 群を `stdlib/alloc/string/scanner.nepl` へ分離した。後方互換のための wrapper facade は残さず、byte scanner を使う module が `alloc/string/scanner` を明示 import する設計にした。

この分割により、parser / scanner 向けの ASCII byte 分類、byte range find、line end / next line、inline space scan の loop 実装は `alloc/string/scanner` が所有する責務になった。`alloc/string.nepl` は UTF-8 slice / builder / numeric conversion など文字列本体の責務に寄せ、scanner helper は `scanner::str_find_byte_range` のように呼び出す。既存 facade を試した際に、同名 public API から `scanner::str_*` を呼ぶと resolver が D3001 にする問題も確認したため、compiler 側の問題は `ISS-20260429T041244376Z-FACADE-MODULE-CANNOT-DISAMBIGUATE-SA-7F898FA5` として分離した。

`stdlib/neplg2/core/module/import_spec.nepl`、`stdlib/nm/parser.nepl`、`stdlib/nm/html_gen.nepl` は scanner module を直接 import するように更新した。`nodesrc/test_stdlib_byte_scanner_helpers_boundary.js` も更新し、scanner 実装本体が `alloc/string/scanner.nepl` にあり、`alloc/string.nepl` に scanner facade が戻らないことを固定した。

この issue はまだ open のまま扱う。`alloc/string` には UTF-8 slice、StringBuilder、numeric parse/format、split が残っており、`core/math`、`std/streamio`、`std/fs`、`stdio`、`nm/parser` など他の巨大 file も未分割である。次の分割では `alloc/string/utf8` または numeric parse/format を候補にする。

### 2026-04-29 検証

- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib\alloc\string\scanner.nepl --no-tree -o tmp\string-scanner-module-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib\alloc\string.nepl --no-tree -o tmp\string-scanner-module-string.json -j 1`: total=8 passed=8
- `node nodesrc/tests.js -i tests\stdlib\neplg2_import_spec.n.md --no-tree -o tmp\string-scanner-module-import-spec.json -j 1`: total=3 passed=3
- `node nodesrc/tests.js -i tests\stdlib\nm.n.md --no-tree -o tmp\string-scanner-module-nm.json -j 1`: total=5 passed=5

## 2026-04-29 追加対応メモ: self-host CLI args classifier split

`stdlib/neplg2/cli/args.nepl` から argv token / option value の分類責務を `stdlib/neplg2/cli/args/classify.nepl` へ分離した。`SelfhostCliArgKind`、hash dispatch 用 key、`--target` / `--emit` / `--profile` value parser は classifier module が所有し、`args.nepl` は parser state machine、emit set の comma parser、compile option 変換に集中する。

この分割により、public option 型は `args/types`、parser 固有分類は `args/classify`、argv parser 本体は `args.nepl` という責務境界になった。`args.nepl` へ classifier が戻る回帰は `nodesrc/test_selfhost_cli_args_types_split.js` で検査し、hash dispatch の `match` 回帰検査は `nodesrc/test_stdlib_match_decision_trees.js` が `args/classify.nepl` を対象に見る。

この issue はまだ open のまま扱う。`core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` などの巨大 file は未分割の責務を残している。

### 2026-04-29 追加検証

- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/classify.nepl --no-tree -o tmp/cli-args-classify-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-classify-facade.json -j 1`: total=5 passed=5
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-classify-parser.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-classify-driver.json -j 1`: total=3 passed=3

## 2026-04-29 追加対応メモ: self-host CLI args emit split

`stdlib/neplg2/cli/args.nepl` に残っていた emit artifact set 操作、`--emit` の comma 区切り parser、emit 判定 helper を `stdlib/neplg2/cli/args/emit.nepl` へ分離した。`args.nepl` は `args/emit` を `pub #import` で再 export し、既存の `neplg2/cli/args` facade から parser tests / driver tests が同じ helper を参照できる構成を保つ。

この分割により、CLI option 型は `args/types`、token/value 分類は `args/classify`、emit artifact set の合成は `args/emit`、argv parser state machine は `args.nepl` という責務境界になった。`args.nepl` の行数は 471 行まで下がり、`args/emit.nepl` は 158 行の専用 module として独立した。

この issue はまだ open のまま扱う。`args.nepl` には parser loop と compile option 変換が残るが、より大きい未分割対象として `core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` が残っている。

### 2026-04-29 emit split 検証

- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/emit.nepl --no-tree -o tmp/cli-args-emit-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-emit-facade.json -j 1`: total=5 passed=5
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-emit-parser.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-emit-driver.json -j 1`: total=3 passed=3

## 2026-04-29 追加対応メモ: self-host CLI args options split

`stdlib/neplg2/cli/args.nepl` に残っていた `SelfhostCliOptions` の構築、既定値、CLI target/profile から core option への変換を `stdlib/neplg2/cli/args/options.nepl` へ分離した。`args.nepl` は `args/options` を `pub #import` で再 export し、parser loop は index 走査と usage error handling に集中する。

この分割により、CLI option 型は `args/types`、token/value 分類は `args/classify`、emit artifact set 合成は `args/emit`、core compile option 変換は `args/options`、argv parser state machine は `args.nepl` という責務境界になった。`args.nepl` の行数は 369 行まで下がり、`args/options.nepl` は 116 行の専用 module として独立した。

この issue はまだ open のまま扱う。`args.nepl` には parser loop と error/target predicate が残るが、より大きい未分割対象として `core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` が残っている。

### 2026-04-29 options split 検証

- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_selfhost_cli_driver_boundary.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/options.nepl --no-tree -o tmp/cli-args-options-direct.json -j 1`: total=2 passed=2
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-options-facade.json -j 1`: total=3 passed=3
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-options-parser.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-options-driver.json -j 1`: total=3 passed=3

## 2026-04-29 追加対応メモ: self-host CLI args parse/predicate split

`stdlib/neplg2/cli/args.nepl` に残っていた argv index 走査、usage error state machine、target/error predicate helper を分離した。parser 本体は `stdlib/neplg2/cli/args/parse.nepl`、enum predicate helper は `stdlib/neplg2/cli/args/predicates.nepl` が所有し、`args.nepl` は compatibility facade として `types`、`emit`、`options`、`parse`、`predicates` を再 export するだけにした。

この分割により、`args.nepl` から parser-private `classify` への直接依存も消えた。分類は `args/parse` だけが利用し、public import path からは parser state machine の実装詳細が露出しない。`nodesrc/test_selfhost_cli_args_types_split.js` は parse/predicate helper が `args.nepl` に戻らないことと、facade が `parse` / `predicates` を re-export することを固定する。

この issue はまだ open のまま扱う。`core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` が未分割のまま残っている。

### 2026-04-29 parse/predicate split 検証

- `trunk build`: passed
- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/test_selfhost_cli_driver_boundary.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/types.nepl --no-tree -o tmp/cli-args-types-parse-split-rebased14.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/parse.nepl --no-tree -o tmp/cli-args-parse-direct-rebased14.json -j 1`: total=2 passed=2
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/predicates.nepl --no-tree -o tmp/cli-args-predicates-direct-rebased14.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-parse-facade-rebased14.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-parse-parser-rebased14.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-parse-driver-rebased14.json -j 1`: total=3 passed=3

## 2026-04-30 追加対応メモ: core/math u8 split

`stdlib/core/math.nepl` に残っていた u8 算術・比較演算群を `stdlib/core/math/u8.nepl` へ分離した。`core/math` は `pub #import "./math/u8" as *` で u8 overload を再 export し、既存の `#import "core/math" as *` 利用は維持しつつ、u8 固有実装の責務を型別 module へ移した。

進捗:

- `add_u8` / `sub_u8` / `mul_u8` / `div_u_u8` / `rem_u_u8` と u8 比較 helper を `core/math/u8` が所有する。
- `add <(u8,u8)->u8>` などの u8 overload wrapper も `core/math/u8` 側へ移し、facade に実装が残らない構成にした。
- `core/math/u8` に direct import doctest を追加し、facade 経由に頼らず u8 module 単体で wrapping add と unsigned division を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を追加し、u8 実装が `core/math.nepl` に戻らないことと、facade が u8 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/u8.nepl --no-tree -o tmp/math-u8-module-direct-final.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-u8-split-final.json -j 1`: `25 total / 25 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md --no-tree -o tmp/numerics-after-math-u8-split-final.json -j 1`: `11 total / 11 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 新規 math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 4102 lines まで下がったが、i32/i64/f32/f64/変換/i128/bool が残っており、`math/i32`、`math/i64`、`math/float`、`math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-04-30 追加対応メモ: core/math bool split

`stdlib/core/math.nepl` に残っていた bool 論理演算 `and` / `or` / `not` を `stdlib/core/math/bool.nepl` へ分離した。`core/math` は `pub #import "./math/bool" as *` で bool overload を再 export し、i32/i64 の bitwise overload と bool 論理 overload の責務を分けた。

進捗:

- `and <(bool,bool)->bool>` / `or <(bool,bool)->bool>` / `not <(bool)->bool>` は `core/math/bool` が所有する。
- `core/math/bool` に direct import doctest を追加し、facade 経由に頼らず bool module 単体で論理演算を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、bool 実装が `core/math.nepl` に戻らないことと、facade が bool module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/bool.nepl --no-tree -o tmp/math-bool-module-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-bool-split.json -j 1`: `25 total / 25 passed`
- `node nodesrc/tests.js -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-tests-after-bool-split.json -j 1`: `6 total / 6 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 4025 lines まで下がったが、i32/i64/f32/f64/変換/i128 が残っており、次は `math/i32`、`math/i64`、`math/float`、`math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-04-30 追加対応メモ: core/math i32 split

`stdlib/core/math.nepl` に残っていた i32 算術・bitwise・比較演算群を `stdlib/core/math/i32.nepl` へ分離した。`core/math` は `pub #import "./math/i32" as *` で i32 API を再 export し、他の型の overload と同じ facade から利用できる構成を維持した。

進捗:

- `add` / `sub` / `mul` / `div_s` / `div_u` / `rem_s` / `mod_s` / `rem_u` の i32 実装を `core/math/i32` が所有する。
- i32 bitwise / rotate / count / signed and unsigned comparison も `core/math/i32` へ移した。
- `core/math/i32` に direct import doctest を追加し、facade 経由に頼らず i32 module 単体で加減算・bitwise・比較を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、i32 実装が `core/math.nepl` に戻らないことと、facade が i32 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i32.nepl --no-tree -o tmp/math-i32-module-direct.json -j 1`: `11 total / 11 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-i32-split.json -j 1`: `15 total / 15 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-i32-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 3147 lines まで下がったが、i64/f32/f64/変換/i128 が残っており、次は `math/i64`、`math/float`、`math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-04-30 追加対応メモ: core/math i64 split

`stdlib/core/math.nepl` に残っていた i64 算術・bitwise・比較演算群を `stdlib/core/math/i64.nepl` へ分離した。`core/math` は `pub #import "./math/i64" as *` で i64 API を再 export し、i32 と同じ型別 submodule 境界へ揃えた。

進捗:

- `add` / `sub` / `mul` / `div_s` / `div_u` / `rem_s` / `rem_u` の i64 実装を `core/math/i64` が所有する。
- i64 bitwise / rotate / count / signed and unsigned comparison も `core/math/i64` へ移した。
- `core/math/i64` に direct import doctest を追加し、facade 経由に頼らず i64 module 単体で加算・bitwise・比較を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、i64 実装が `core/math.nepl` に戻らないことと、facade が i64 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i64.nepl --no-tree -o tmp/math-i64-module-direct.json -j 1`: `10 total / 10 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-i64-split.json -j 1`: `6 total / 6 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-i64-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 2245 lines まで下がったが、f32/f64/変換/i128 が残っており、次は `math/float`、`math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-04-30 追加対応メモ: core/math f32 split

`stdlib/core/math.nepl` に残っていた f32 算術・単項・比較演算群を `stdlib/core/math/f32.nepl` へ分離した。`core/math` は `pub #import "./math/f32" as *` で f32 API を再 export し、整数型と同じ型別 submodule 境界へ揃えた。

進捗:

- `add` / `sub` / `mul` / `div` / `min` / `max` / `copysign` の f32 実装を `core/math/f32` が所有する。
- `sqrt` / `abs` / `neg` / `ceil` / `floor` / `trunc` / `nearest` と f32 comparison も `core/math/f32` へ移した。
- `core/math/f32` に direct import doctest を追加し、facade 経由に頼らず f32 module 単体で算術・比較を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、f32 実装が `core/math.nepl` に戻らないことと、facade が f32 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/f32.nepl --no-tree -o tmp/math-f32-module-direct.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-f32-split.json -j 1`: `5 total / 5 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-f32-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 1709 lines まで下がったが、f64/変換/i128 が残っており、次は `math/f64`、`math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-04-30 追加対応メモ: core/math f64 split

`stdlib/core/math.nepl` に残っていた f64 算術・単項・比較演算群を `stdlib/core/math/f64.nepl` へ分離した。`core/math` は `pub #import "./math/f64" as *` で f64 API を再 export し、f32 と同じ型別 submodule 境界へ揃えた。

進捗:

- `add` / `sub` / `mul` / `div` / `min` / `max` / `copysign` の f64 実装を `core/math/f64` が所有する。
- `sqrt` / `abs` / `neg` / `ceil` / `floor` / `trunc` / `nearest` と f64 comparison も `core/math/f64` へ移した。
- `core/math/f64` に direct import doctest を追加し、facade 経由に頼らず f64 module 単体で算術・比較を検証できるようにした。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、f64 実装が `core/math.nepl` に戻らないことと、facade が f64 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/f64.nepl --no-tree -o tmp/math-f64-module-direct-2.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-f64-split.json -j 1`: `4 total / 4 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-f64-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 1169 lines まで下がったが、変換/i128 が残っており、次は `math/convert`、`math/int128` などへ追加分割する必要がある。

## 2026-05-05 追加対応メモ: core/math conversion split

`stdlib/core/math.nepl` に残っていた整数幅変換・浮動小数点変換・bit pattern 再解釈 helper 群を `stdlib/core/math/convert.nepl` facade と下位 submodule へ分離した。単一の巨大 `convert` module を作らないよう、`convert/width.nepl`、`convert/float.nepl`、`convert/reinterpret.nepl` に責務を分けた。

進捗:

- `core/math` は `pub #import "./math/convert" as *` で変換 API を再 export するだけになった。
- `core/math/convert` は薄い facade として、公開 wrapper overload (`extend_s` / `wrap` / `convert_s` / `trunc_s` / `promote` / `demote`) を保持する。
- `core/math/convert/width` は `extend8_s_i32`、`extend16_s_i32`、`extend_s_i32_to_i64`、`extend_u_i32_to_i64`、`wrap_i64_to_i32` を所有する。
- `core/math/convert/float` は i32/i64 と f32/f64 の相互変換、trunc/trunc_sat、promote/demote を所有する。
- `core/math/convert/reinterpret` は i32/f32 と i64/f64 の bit pattern 再解釈を所有する。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、変換 helper が `core/math.nepl` に戻らないことと、`convert` facade が下位 module を再 export することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/convert.nepl --no-tree -o tmp/math-convert-module-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/core/math/convert/width.nepl --no-tree -o tmp/math-convert-width-direct.json -j 1`: `3 total / 3 passed`
- `node nodesrc/tests.js -i stdlib/core/math/convert/float.nepl --no-tree -o tmp/math-convert-float-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/core/math/convert/reinterpret.nepl --no-tree -o tmp/math-convert-reinterpret-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-convert-split.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-convert-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`core/math.nepl` は 244 lines まで下がったが、u128/i128 API は facade に残っている。次は `math/int128` または `math/u128_i128` として追加整数型 API を切り出す。

## 2026-05-05 追加対応メモ: core/math int128 split

`stdlib/core/math.nepl` に最後まで残っていた u128/i128 追加整数型 API を `stdlib/core/math/int128.nepl` へ分離した。`core/math` は `pub #import "./math/int128" as *` を含む公開 facade だけになり、`core/field` への直接依存も不要になった。

進捗:

- `core/math/int128` は u128/i128 の構造体、構築、加減算、比較、`mul_wide`、i128 乗算を所有する。
- `core/math/int128` は循環 import を避けるため、`core/math` facade ではなく `core/math/i64` と `core/math/convert/width` を直接 import する。
- `core/math` は実装 body を持たない 27 lines の facade になった。
- `nodesrc/test_stdlib_math_module_split.js` を拡張し、u128/i128 API が facade に戻らないこと、`core/math` が `core/field` に依存しないこと、facade に real `fn` / `struct` が残らないことを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/int128.nepl --no-tree -o tmp/math-int128-module-direct.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i stdlib/core/math.nepl --no-tree -o tmp/math-facade-after-int128-split.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-suite-after-int128-split.json -j 1`: `17 total / 17 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 拡張後の math split policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue は `core/math.nepl` については分割完了。issue 全体では `string.nepl`、`streamio.nepl`、`stdio.nepl`、`vec.nepl` など他の巨大 stdlib file が残っているため open のまま継続する。

## 2026-05-05 追加対応メモ: std/streamio writer split

`stdlib/std/streamio.nepl` に同居していた `StreamWriter` owning buffer API と write target logic を `stdlib/std/streamio/writer.nepl` へ分離した。scanner と writer の raw memory backed state を別 module に分け、writer 側は `StreamWriterTargetKind` enum + `match` による target 分岐を維持する。

進捗:

- `std/streamio` は `pub #import "./streamio/writer" as *` で writer API を再 export する。
- `std/streamio/writer` は `StreamWriter`、`StreamWriterTargetKind`、buffer 確保/flush/append、`StreamWritable`、`open WriteStream`、`write` / `writeln` / `flush` / `close` を所有する。
- `std/streamio` facade doctest の `ByteBuf` 長さ確認を `io_bytebuf_len_ref &bytes0` に変更し、確認後に `io_bytebuf_free bytes0` で owner を閉じるようにした。
- `nodesrc/test_stdlib_streamio_writer_boundary.js` を更新し、writer 実装が facade に戻らないことと、writer module 側で enum target 分岐・borrowed ByteBuf access を維持することを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_streamio_writer_boundary.js`: passed
- `node nodesrc/test_stdlib_streamio_scanner_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/std/streamio/writer.nepl --no-tree -o tmp/streamio-writer-module-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio.nepl --no-tree -o tmp/streamio-facade-after-writer-split-3.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-nmd-after-writer-split.json -j 1`: `14 total / 14 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 更新後の streamio policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`stdlib/std/streamio.nepl` は 1279 lines、`stdlib/std/streamio/writer.nepl` は 487 lines になった。次は scanner / stdio stream bridge / byte text helper の追加分割を検討する。

## 2026-05-05 追加対応メモ: std/streamio scanner and bytes split

`stdlib/std/streamio.nepl` に残っていた `StreamScanner` cursor/header state と byte/text conversion helper を `stdlib/std/streamio/scanner.nepl` / `stdlib/std/streamio/bytes.nepl` へ分離した。scanner と writer の raw memory backed state を別 module に分け、byte/text helper も scanner に混ぜない構造へ整理した。

進捗:

- `std/streamio` は `./streamio/writer`、`./streamio/bytes`、`./streamio/scanner` を re-export する facade になった。
- `std/streamio/bytes` は `stream_bytes_from_str(_result)` と `stream_bytes_to_str(_result)` を所有する。
- `std/streamio/scanner` は `StreamScanner`、scanner header enum、scanner header load/store boundary、`open ReadStream`、token/number scanner、scanner `read` overload、scanner `close` を所有する。
- `nodesrc/test_stdlib_streamio_scanner_boundary.js` と `nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js` を新 module 構造へ更新した。
- `nodesrc/test_stdlib_match_decision_trees.js` の stream scanner match policy 対象を `streamio/scanner.nepl` へ更新した。

検証:

- `node nodesrc/test_stdlib_streamio_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/tests.js -i stdlib/std/streamio/bytes.nepl --no-tree -o tmp/streamio-bytes-module-direct.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio/scanner.nepl --no-tree -o tmp/streamio-scanner-module-direct-2.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio.nepl --no-tree -o tmp/streamio-facade-after-scanner-split.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-nmd-after-scanner-split.json -j 1`: `14 total / 14 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: 更新後の streamio / match decision tree policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。

この issue はまだ open のまま扱う。`stdlib/std/streamio.nepl` は 414 lines、`stdlib/std/streamio/scanner.nepl` は 859 lines、`stdlib/std/streamio/writer.nepl` は 487 lines、`stdlib/std/streamio/bytes.nepl` は 72 lines になった。次は stdio stream bridge の分離または scanner 内部の parse helper 分割を検討する。

## 2026-05-05 追加対応メモ: std/test types/assertion/report split

`stdlib/std/test.nepl` に同居していた assertion 型、assert/check helper、report 集約、stdout 表示、exit code 変換を `stdlib/std/test/types.nepl` / `stdlib/std/test/assertion.nepl` / `stdlib/std/test/report.nepl` へ分離した。`std/test` は public facade とし、`pub #import` で既存 import path から同じ API を参照できる構成を保つ。

進捗:

- `std/test/types` は `AssertionStatus` / `AssertionKind` enum、`TestAssertion` / `TestReport`、name conversion、owner terminal を所有する。
- `std/test/assertion` は `assert_*` と quiet `check_*` helper だけを所有し、stdout や report 集約を行わない。
- `std/test/report` は `TestReport` への push、canonical render、stdout print、exit code、旧 `checks_*` 互換層を所有する。
- `std/test` root に re-export smoke doctest を追加し、facade 経路が壊れないことを確認できるようにした。
- `nodesrc/test_stdlib_std_test_no_unsafe_unwraps.js` を分割後の module 構造へ更新し、root facade の実装再混入、line limit、typed assertion/report contract、unsafe unwrap 禁止を固定した。

検証:

- `node nodesrc/test_stdlib_std_test_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_doctest_std_test_assertion_report_contract.js`: passed
- `node nodesrc/tests.js -i stdlib/std/test.nepl --no-tree -o tmp/std-test-module-split-root.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/std_test_collect.n.md --no-tree -o tmp/std-test-module-split-collect.json -j 1`: `3 total / 3 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: std/test policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/std/test.nepl` は 33 lines の facade になり、`std/test/types.nepl` は 148 lines、`std/test/assertion.nepl` は 226 lines、`std/test/report.nepl` は 207 lines になった。次は `stdlib/std/streamio/writer.nepl`、`stdlib/std/fs/raw.nepl`、`stdlib/std/fs/path.nepl` など残る中規模 file の責務境界を確認する。

## 2026-05-05 追加対応メモ: std/streamio writer internal split

`stdlib/std/streamio/writer.nepl` に残っていた writer state、flush/buffer boundary、append/format helper、public write API を `stdlib/std/streamio/writer/state.nepl` / `stdlib/std/streamio/writer/append.nepl` へ分離した。root は `WriteStream` からの `open` と `StreamWritable` trait dispatch、`write` / `writeln` / `flush` の公開 API に集中する。

進捗:

- `std/streamio/writer/state` は `StreamWriter`、`StreamWriterTargetKind`、buffer alloc/free、`drain_impl`、`reserve_impl`、`push_u8_impl`、`push_newline_impl` を所有する。
- `std/streamio/writer/append` は `str` / `ByteBuf` / i32/i64/u32/u64/f32/f64 append と format helper を所有する。
- `std/streamio/writer` root は `state` / `append` を import し、public write API の trait dispatch だけを保持する。
- `nodesrc/test_stdlib_streamio_writer_boundary.js` を分割後の module 構造へ更新し、root への state/append 実装再混入、line limit、target enum match、borrowed ByteBuf access を固定した。
- `nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js` も writer internal module を読むように更新し、`push_u8_impl` の checked store contract と append ownership contract を分割後も監視する。

検証:

- `node nodesrc/test_stdlib_streamio_writer_boundary.js`: passed
- `node nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/std/streamio/writer.nepl --no-tree -o tmp/streamio-writer-internal-split-root.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio.nepl --no-tree -o tmp/streamio-writer-internal-split-facade.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-writer-internal-split-nmd.json -j 1`: `14 total / 14 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: streamio policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/std/streamio/writer.nepl` は 132 lines、`stdlib/std/streamio/writer/state.nepl` は 145 lines、`stdlib/std/streamio/writer/append.nepl` は 239 lines になった。次は `stdlib/std/fs/raw.nepl` / `stdlib/std/fs/path.nepl` / `stdlib/std/text.nepl` など残る file の責務境界を確認する。

## 2026-05-05 追加対応メモ: std/streamio input/output split

`stdlib/std/streamio.nepl` に残っていた stdin/text/byte input adapter と stdout/stderr output adapter を `stdlib/std/streamio/input.nepl` / `stdlib/std/streamio/output.nepl` へ分離した。root は `input` / `output` / `writer` / `bytes` / `scanner` を re-export する facade にした。

進捗:

- `std/streamio/input` は `StdinStream`、`TextInputStream`、`ByteInputStream`、reader impl、`StreamReadableResult`、input `read` / `close` overload を所有する。
- `std/streamio/output` は `StdoutStream`、`StderrStream`、writer/flush/close impl、`StdoutWritable` / `StderrWritable`、output `write` / `writeln` / `flush` / `close` overload を所有する。
- `std/streamio` root は public facade とし、実装 body を持たず下位 module を re-export する構成にした。
- `nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js` を input/output 分割後の module 構造へ更新し、facade re-export と line limit を固定した。

検証:

- `node nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/std/streamio.nepl --no-tree -o tmp/streamio-root-input-output-split-facade.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio/input.nepl --no-tree -o tmp/streamio-root-input-output-split-input.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/std/streamio/output.nepl --no-tree -o tmp/streamio-root-input-output-split-output.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-root-input-output-split-nmd.json -j 1`: `14 total / 14 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: streamio policy は passed。既存の `owner_summary_variant_paths.rs has 637 lines; responsibility split limit is 380` warning は継続。
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/std/streamio.nepl` は 53 lines、`stdlib/std/streamio/input.nepl` は 172 lines、`stdlib/std/streamio/output.nepl` は 255 lines になった。`std/streamio` root の分割は完了。次は `stdlib/std/fs/raw.nepl`、`stdlib/std/fs/path.nepl`、`stdlib/std/text.nepl` などを確認する。

## 2026-05-06 追加対応メモ: nm JSON escape split

`stdlib/nm/parser.nepl` に同居していた JSON string escape helper を `stdlib/nm/json_escape.nepl` へ分離した。parser は Markdown block/inline の再走査と JSON node 組み立てに集中し、byte escape の固定分岐、builder 直接追加、builder 消費境界は専用 module が所有する。

進捗:

- `nm/json_escape` は `json_escape_byte_into` / `json_escape_mem_into` / `json_escape_into` / `json_escape_builder_into` / `json_escape` を公開 API として所有する。
- escape 対象の固定集合は char literal `match` で列挙し、`nodesrc/test_stdlib_match_decision_trees.js` が新 module を監視する。
- `nm/parser` は `./json_escape` を `json` alias で import し、inline text、ruby/gloss/math/code fence の JSON 出力で `json::json_escape_into` / `json::json_escape_builder_into` を呼ぶ。
- `nodesrc/test_stdlib_nm_json_escape_boundary.js` を追加し、JSON escape helper が parser に戻らないこと、parser が専用 module を呼ぶことを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_stdlib_nm_parser_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/test_stdlib_nm_json_escape_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/json_escape.nepl --no-tree -o tmp/nm-json-escape-split-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/parser.nepl --no-tree -o tmp/nm-json-escape-split-parser.json -j 1`: `3 total / 3 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-json-escape-split-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/nm/parser.nepl` は 761 lines、`stdlib/nm/json_escape.nepl` は 127 lines になった。次は `stdlib/alloc/string.nepl`、`stdlib/alloc/collections/vec.nepl`、`stdlib/nm/parser.nepl` 内の section stack / block serializer など、残る巨大責務を引き続き分割する。

## 2026-05-06 追加対応メモ: nm parser scanner split

`stdlib/nm/parser.nepl` に残っていた line / heading / delimiter scanner helper を `stdlib/nm/parser/scanner.nepl` へ分離した。parser と html_gen は block/inline 構造の JSON/HTML 出力に集中し、Markdown marker の byte scan、heading 判定、section close の深さ計算は専用 module が所有する。

進捗:

- `nm/parser/scanner` は `nm_read_line` / `nm_heading_level` / `nm_heading_text` / `is_fence_start` / `is_hr` / `is_section_break` / `nm_is_block_boundary` / `nm_deepest_level` / `nm_find_math_end` / `nm_find_gloss_slash` を公開 API として所有する。
- `nm/parser` は `./parser/scanner` を `scan` alias で import し、inline math/gloss delimiter、block boundary、heading 判定を `scan::` 経由で呼ぶ。
- `nm/html_gen` も同じ scanner module を直接 import し、以前の `#import "./parser" as *` による内部 helper 依存を取り除いた。
- `nodesrc/test_stdlib_nm_parser_scanner_boundary.js` を追加し、scan helper が parser/html_gen に戻らないことを source policy で固定した。
- `nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js` は scanner module も検査対象に含めるよう更新した。
- `nodesrc/test_stdlib_nm_parser_no_block_unwraps.js` は section depth helper の所有先を scanner module として検査するよう更新した。

検証:

- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/parser/scanner.nepl --no-tree -o tmp/nm-parser-scanner-split-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/parser.nepl --no-tree -o tmp/nm-parser-scanner-split-parser.json -j 1`: `3 total / 3 passed`
- `node nodesrc/tests.js -i stdlib/nm/html_gen.nepl --no-tree -o tmp/nm-parser-scanner-split-html.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-parser-scanner-split-nm-tests2.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/nm/parser.nepl` は 559 lines、`stdlib/nm/parser/scanner.nepl` は 239 lines、`stdlib/nm/html_gen.nepl` は 535 lines になった。次は `nm/parser` の section stack / block serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm HTML escape split

`stdlib/nm/html_gen.nepl` に残っていた HTML text / attribute escape helper を `stdlib/nm/html_escape.nepl` へ分離した。HTML renderer は section/inline/block の出力に集中し、HTML 構文上危険な byte の分類と entity 変換は専用 module が所有する。

進捗:

- `nm/html_escape` は `HtmlEscapeKind` / `html_escape_kind` / `escape_html` を所有する。
- escape 対象の固定集合は char literal `match` で列挙し、`nodesrc/test_stdlib_match_decision_trees.js` が新 module を監視する。
- `nm/html_gen` は `./html_escape` を `html` alias で import し、text、ruby、gloss、math、code fence info/body の HTML 出力で `html::escape_html` を呼ぶ。
- `nodesrc/test_stdlib_nm_html_escape_boundary.js` を追加し、HTML escape helper が renderer 本体へ戻らないことを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_nm_html_escape_boundary.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/html_escape.nepl --no-tree -o tmp/nm-html-escape-split-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/html_gen.nepl --no-tree -o tmp/nm-html-escape-split-html-gen.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-html-escape-split-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/nm/html_gen.nepl` は 465 lines、`stdlib/nm/html_escape.nepl` は 98 lines になった。次は `nm/html_gen` の heading/section renderer、`nm/parser` の section stack / block serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm HTML heading split

`stdlib/nm/html_gen.nepl` に残っていた HTML heading tag helper を `stdlib/nm/html_heading.nepl` へ分離した。HTML renderer は block/inline/section の出力に集中し、heading level から h1..h6 への固定分岐は専用 module が所有する。

進捗:

- `nm/html_heading` は `HtmlHeadingKind` / `html_heading_kind` / `nm_append_heading_open` / `nm_append_heading_close` を所有する。
- heading level の固定集合は i32 literal `match` で列挙し、`nodesrc/test_stdlib_match_decision_trees.js` が新 module を監視する。
- `nm/html_gen` は `./html_heading` を `heading` alias で import し、heading node の HTML 出力で `heading::nm_append_heading_open` / `heading::nm_append_heading_close` を呼ぶ。
- `nodesrc/test_stdlib_nm_html_heading_boundary.js` を追加し、heading helper が renderer 本体へ戻らないことを source policy で固定した。

検証:

- `node nodesrc/test_stdlib_nm_html_heading_boundary.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/html_heading.nepl --no-tree -o tmp/nm-html-heading-split-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/html_gen.nepl --no-tree -o tmp/nm-html-heading-split-html-gen.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-html-heading-split-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`stdlib/nm/html_gen.nepl` は 389 lines、`stdlib/nm/html_heading.nepl` は 111 lines になった。次は `nm/html_gen` の section renderer、`nm/parser` の section stack / block serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm HTML inline split

`stdlib/nm/html_gen.nepl` に残っていた inline markup HTML renderer を `stdlib/nm/html_inline.nepl` へ分離した。HTML block/section renderer は paragraph と heading の source 範囲を決める責務に集中し、ruby / gloss / inline math / text escape の inline 変換は専用 module が所有する。

進捗:

- `nm/html_inline` は `pub fn nm_inline_to_html` を所有し、marker byte dispatch は `match ch:` で固定する。
- inline math と gloss slash の delimiter search は `nm/parser/scanner`、byte range search は `alloc/string/scanner` を使う境界へ整理した。
- `nm/html_gen` は `./html_inline` を `inline` alias で import し、heading text と paragraph text の HTML 出力で `inline::nm_inline_to_html` を呼ぶ。
- `nodesrc/test_stdlib_nm_html_inline_boundary.js` を追加し、inline renderer が `html_gen` へ戻らないこと、gloss の再帰 inline 変換が維持されることを source policy で固定した。
- 既存の scanner boundary policy も分割後の責務境界へ更新し、`html_gen` は line scanner、`html_inline` は inline delimiter / byte range scanner を監視対象にした。

検証:

- `node nodesrc/test_stdlib_nm_html_inline_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/html_inline.nepl --no-tree -o tmp/nm-html-inline-split-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/html_gen.nepl --no-tree -o tmp/nm-html-inline-split-html-gen.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-html-inline-split-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/nm 関連は passed。remote main 由来の別件として `coverage_hir_projection.rs has 296 lines; responsibility split limit is 280` warning は継続。

この issue はまだ open のまま扱う。`stdlib/nm/html_gen.nepl` は 252 lines、`stdlib/nm/html_inline.nepl` は 165 lines になった。次は `nm/html_gen` の section renderer、`nm/parser` の section stack / block serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm HTML section split

`stdlib/nm/html_gen.nepl` に残っていた section stack state と section tag helper を `stdlib/nm/html_section.nepl` へ分離した。`html_gen` は block scan と出力順序に集中し、h1..h6 の open flag、current level、close 後の deepest level 再計算は `html_section` が所有する。

進捗:

- `nm/html_section` は `NmHtmlSectionState`、`nm_section_state_open_level`、`nm_section_state_close_current`、`nm_append_section_open`、`nm_append_section_close` を所有する。
- close 時の level 分岐は `match state.current_level:` に集約し、`html_gen` に重複していた `match current_level:` と `open1..open6` の local bool 群を削除した。
- `html_gen` は `html_section` を star import し、section open/close と state update を helper 経由にした。
- `nodesrc/test_stdlib_nm_html_section_boundary.js` を追加し、section state と tag helper が `html_gen` へ戻らないことを source policy で固定した。
- `nodesrc/test_stdlib_nm_parser_scanner_boundary.js` を更新し、`html_section` が parser scanner の `nm_deepest_level` を使い、scanner helper を再所有しないことを監視する。

検証:

- `node nodesrc/test_stdlib_nm_html_section_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_html_inline_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/html_section.nepl --no-tree -o tmp/nm-html-section-split-module2.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/html_gen.nepl --no-tree -o tmp/nm-html-section-split-html-gen3.json -j 1`: `2 total / 2 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-html-section-split-nm-tests2.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/nm 関連は passed。既知別件 `ISS-20260506T030752155Z-RESOURCE-HIR-PROJECTION-COVERAGE-MOD-ED65CFB3` の `coverage_hir_projection.rs has 296 lines; responsibility split limit is 280` warning は継続。

この issue はまだ open のまま扱う。`stdlib/nm/html_gen.nepl` は 180 lines、`stdlib/nm/html_section.nepl` は 190 lines になった。次は `nm/parser` の section stack / block serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm parser JSON section split

`stdlib/nm/parser.nepl` に残っていた JSON serializer の section stack / comma state を `stdlib/nm/parser/json_section.nepl` へ分離した。`document_to_json` は block scan と JSON node 出力順序に集中し、root/section children の comma 判定、h1..h6 open flag、close 後の deepest level 再計算は専用 module が所有する。

進捗:

- `nm/parser/json_section` は `NmJsonSectionState`、`nm_json_section_needs_comma`、`nm_json_section_mark_current_has`、`nm_json_section_open_level`、`nm_json_section_close_current` を所有する。
- `parser.nepl` に重複していた `current_level` / `open1..open6` / `root_has` / `has1..has6` の local state と `nm_json_needs_comma` helper を削除した。
- `document_to_json` は node 追加前の comma 判定と mark、section open/close を `json_section` helper 経由にした。
- `nodesrc/test_stdlib_nm_parser_json_section_boundary.js` を追加し、JSON section state が parser 本体へ戻らないことを source policy で固定した。
- `nodesrc/test_stdlib_nm_parser_scanner_boundary.js` を更新し、`json_section` が parser scanner の `nm_deepest_level` を使い、scanner helper を再所有しないことを監視する。

検証:

- `node nodesrc/test_stdlib_nm_parser_json_section_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/parser/json_section.nepl --no-tree -o tmp/nm-parser-json-section-module2.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/parser.nepl --no-tree -o tmp/nm-parser-json-section-parser2.json -j 1`: `3 total / 3 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-parser-json-section-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/nm 関連は passed。既知別件 `ISS-20260506T030752155Z-RESOURCE-HIR-PROJECTION-COVERAGE-MOD-ED65CFB3` の `coverage_hir_projection.rs has 296 lines; responsibility split limit is 280` warning は継続。

この issue はまだ open のまま扱う。`stdlib/nm/parser.nepl` は 391 lines、`stdlib/nm/parser/json_section.nepl` は 220 lines になった。次は `nm/parser` の inline JSON serializer、または `alloc/string.nepl` / `alloc/collections/vec.nepl` の責務分割を続ける。

## 2026-05-06 追加対応メモ: nm parser JSON inline split

`stdlib/nm/parser.nepl` に残っていた inline markup JSON serializer を `stdlib/nm/parser/json_inline.nepl` へ分離した。`parser.nepl` は document source view と block-level JSON serializer に集中し、ruby / gloss / inline math / text の marker dispatch は専用 module が所有する。

進捗:

- `nm/parser/json_inline` は `pub fn nm_inline_to_json_into` と `pub fn nm_inline_to_json` を所有する。
- marker byte dispatch は `match ch:` のまま固定し、math / gloss delimiter search は `parser/scanner`、byte range search は `alloc/string/scanner` を使う。
- `parser.nepl` は `./parser/json_inline` を `json_inline` alias で import し、heading と paragraph の inline JSON 出力を `json_inline::nm_inline_to_json_into` に委譲する。
- `nodesrc/test_stdlib_nm_parser_json_inline_boundary.js` を追加し、inline JSON serializer が parser 本体へ戻らないことを source policy で固定した。
- 既存の doc / unwrap / scanner / byte scanner / raw aggregate policy を分割後の責務境界へ更新した。

検証:

- `node nodesrc/test_stdlib_nm_parser_json_inline_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/parser/json_inline.nepl --no-tree -o tmp/nm-parser-json-inline-module.json -j 1`: `1 total / 1 passed`
- `node nodesrc/tests.js -i stdlib/nm/parser.nepl --no-tree -o tmp/nm-parser-json-inline-parser.json -j 1`: `3 total / 3 passed`
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-parser-json-inline-nm-tests.json -j 1`: `5 total / 5 passed`
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/nm 関連は passed。既知別件 `ISS-20260506T030752155Z-RESOURCE-HIR-PROJECTION-COVERAGE-MOD-ED65CFB3` の `coverage_hir_projection.rs has 296 lines; responsibility split limit is 280` warning は継続。

この issue はまだ open のまま扱う。`stdlib/nm/parser.nepl` は 221 lines、`stdlib/nm/parser/json_inline.nepl` は 204 lines になった。次は `alloc/string.nepl` / `alloc/collections/vec.nepl` など stdlib の大きな module 分割を続ける。

## 2026-05-06 追加対応メモ: core/math i32 responsibility split

`stdlib/core/math/i32.nepl` は `core/math` facade から分離済みだったが、i32 の算術・bitwise・比較演算を 1 file に持ち続け、901 lines の型別巨大 module になっていた。`core/math/i32` 自体を facade 化し、責務別 submodule へ再分割した。

進捗:

- `stdlib/core/math/i32/arith.nepl` を追加し、`add` / `sub` / `mul` / `div_s` / `div_u` / `rem_s` / `mod_s` / `rem_u` を所有させた。
- `stdlib/core/math/i32/bitwise.nepl` を追加し、`and` / `or` / `xor` / shift / rotate / `clz` / `ctz` / `popcnt` を所有させた。
- `stdlib/core/math/i32/compare.nepl` を追加し、等値・符号付き比較・符号なし比較を所有させた。
- `stdlib/core/math/i32.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、i32 facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i32/arith.nepl -i stdlib/core/math/i32/bitwise.nepl -i stdlib/core/math/i32/compare.nepl -i stdlib/core/math/i32.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-i32-responsibility-split.json -j 1`: total=32, passed=32
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl`、`math/i64.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math i64 responsibility split

`stdlib/core/math/i64.nepl` も i32 と同じく、算術・bitwise・比較演算を 927 lines の 1 file に持ち続けていた。i64 root を facade 化し、責務別 submodule へ再分割した。

進捗:

- `stdlib/core/math/i64/arith.nepl` を追加し、`add` / `sub` / `mul` / `div_s` / `div_u` / `rem_s` / `rem_u` を所有させた。
- `stdlib/core/math/i64/bitwise.nepl` を追加し、`and` / `or` / `xor` / shift / rotate / `clz` / `ctz` / `popcnt` を所有させた。
- `stdlib/core/math/i64/compare.nepl` を追加し、等値・符号付き比較・符号なし比較を所有させた。
- `stdlib/core/math/i64.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、i64 facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i64/arith.nepl -i stdlib/core/math/i64/bitwise.nepl -i stdlib/core/math/i64/compare.nepl -i stdlib/core/math/i64.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-i64-responsibility-split.json -j 1`: total=31, passed=31
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math i64 bitwise internal split

`stdlib/core/math/i64/bitwise.nepl` は i64 の AND/OR/XOR、shift/rotate、bit count intrinsic を 342 lines の 1 file に持ち続けていた。i64 bitwise root を facade 化し、演算カテゴリごとに責務を分けた。

進捗:

- `stdlib/core/math/i64/bitwise/binary.nepl` を追加し、`and` / `or` / `xor` を所有させた。
- `stdlib/core/math/i64/bitwise/shift.nepl` を追加し、`shl` / `shr_s` / `shr_u` / `rotl` / `rotr` を所有させた。
- `stdlib/core/math/i64/bitwise/count.nepl` を追加し、`clz` / `ctz` / `popcnt` を所有させた。
- `stdlib/core/math/i64/bitwise.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、i64 bitwise facade に実装が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i64/bitwise/binary.nepl -i stdlib/core/math/i64/bitwise/shift.nepl -i stdlib/core/math/i64/bitwise/count.nepl -i stdlib/core/math/i64/bitwise.nepl -i stdlib/core/math/i64.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-i64-bitwise-internal-split.json -j 1`: total=23, passed=23
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: alloc/collections/vec/sort responsibility split

`stdlib/alloc/collections/vec/sort.nepl` は比較 helper、unchecked raw access、小規模 sort、quick/heap/merge sort、owner-returning wrapper を 1105 lines の 1 file に持ち続けていた。`Vec` 本体の storage owner には触れず、sort algorithm の責務だけを submodule 化した。

進捗:

- `stdlib/alloc/collections/vec/sort.nepl` を facade 化し、`common` / `simple` / `quick` / `heap` / `merge` を再 export する形にした。
- `stdlib/alloc/collections/vec/sort/common.nepl` に比較 predicate、unchecked raw access、swap、`sort_is_sorted` を移した。
- `stdlib/alloc/collections/vec/sort/simple.nepl` に insertion / selection / bubble / cocktail / gnome / shell / comb sort を移した。
- `stdlib/alloc/collections/vec/sort/quick.nepl` に Lomuto quick sort、raw slice quick sort、`sort_quick_ret`、default `sort` を移した。
- `stdlib/alloc/collections/vec/sort/heap.nepl` に heap sift-down、`sort_heap`、`sort_heap_ret` を移した。
- `stdlib/alloc/collections/vec/sort/merge.nepl` に merge scratch buffer helper、`sort_merge`、`sort_merge_ret` を移した。
- `nodesrc/test_stdlib_vec_sort_module_split.js` を追加し、root facade へ実装が戻らないこと、関数所有先、行数上限を固定した。
- 既存 Vec source policy を新 submodule 配置に追従させ、merge sort の owner cleanup 検査を `sort/merge.nepl` に移した。

検証:

- `node nodesrc/test_stdlib_vec_sort_module_split.js`: passed
- `node nodesrc/test_stdlib_sort_merge_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/sort/common.nepl -i stdlib/alloc/collections/vec/sort/simple.nepl -i stdlib/alloc/collections/vec/sort/quick.nepl -i stdlib/alloc/collections/vec/sort/heap.nepl -i stdlib/alloc/collections/vec/sort/merge.nepl -i stdlib/alloc/collections/vec/sort.nepl -i stdlib/alloc/collections/vec.nepl -i tests/stdlib/sort.n.md -i stdlib/tests/vec.n.md --no-tree -o tmp/vec-sort-responsibility-split.json -j 1`: total=68, passed=68
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: alloc/string UTF-8 validation split

`stdlib/alloc/string.nepl` は文字列 layout、UTF-8 byte validation、builder、slice、numeric conversion を 3500 lines 超の 1 file に持ち続けていた。外部入力を `str` に変換する前提を支える UTF-8 検証責務を、文字列所有権確定処理から分離した。

進捗:

- `stdlib/alloc/string/utf8.nepl` を追加し、`StringUtf8LeadKind`、leading byte 分類、continuation / 2 / 3 / 4 byte sequence validation、`string_utf8_validate_mem` を所有させた。
- `stdlib/alloc/string.nepl` は `./string/utf8` を public re-export し、検証済み byte 列から `str` を構築する `string_from_utf8_mem_result` は string root に残した。
- `nodesrc/test_stdlib_string_utf8_boundary.js` を追加し、root へ UTF-8 validation 実装が戻らないこと、enum + `match` による検証分岐、line count 上限を固定した。
- `nodesrc/test_stdlib_string_no_unsafe_unwraps.js` を更新し、unsafe helper 禁止と UTF-8 enum 所有先を `alloc/string/utf8.nepl` へ追従させた。
- 広い UTF-8 negative-path test で owner を閉じていない既存 fixture 問題を確認し、`ISS-20260506T115339863Z-UTF-8-NEGATIVE-PATH-TESTS-LEAK-OWNED-2057C710` として分離した。

検証:

- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_utf8_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl --no-tree -o tmp/string-root-after-utf8-split.json -j 1`: total=10, passed=10
- `node nodesrc/tests.js -i tests/stdlib/string.n.md --no-tree -o tmp/tests-stdlib-string-after-utf8-split.json -j 1`: total=17, passed=17
- `node nodesrc/tests.js -i tests/stdlib/string_char.n.md --no-tree -o tmp/tests-stdlib-string-char-after-utf8-split.json -j 1`: total=3, passed=3
- `node nodesrc/tests.js -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/tests-stdlib-string-numeric-after-utf8-split.json -j 1`: total=8, passed=8
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には builder、slice/search/split、numeric parse/format が残り、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / 巨大 submodule も継続対象とする。

## 2026-05-06 追加対応メモ: core/math f32 responsibility split

`stdlib/core/math/f32.nepl` は f32 の二項算術・単項 math・比較演算を 558 lines の 1 file に持ち続けていた。f32 root を facade 化し、責務別 submodule へ再分割した。

進捗:

- `stdlib/core/math/f32/binary.nepl` を追加し、`add` / `sub` / `mul` / `div` / `min` / `max` / `copysign` を所有させた。
- `stdlib/core/math/f32/unary.nepl` を追加し、`sqrt` / `abs` / `neg` / `ceil` / `floor` / `trunc` / `nearest` を所有させた。
- `stdlib/core/math/f32/compare.nepl` を追加し、等値・大小比較を所有させた。
- `stdlib/core/math/f32.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、f32 facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/f32/binary.nepl -i stdlib/core/math/f32/unary.nepl -i stdlib/core/math/f32/compare.nepl -i stdlib/core/math/f32.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-f32-responsibility-split.json -j 1`: total=23, passed=23
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl`、`math/f64.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math f64 responsibility split

`stdlib/core/math/f64.nepl` も f32 と同じく、f64 の二項算術・単項 math・比較演算を 563 lines の 1 file に持ち続けていた。f64 root を facade 化し、責務別 submodule へ再分割した。

進捗:

- `stdlib/core/math/f64/binary.nepl` を追加し、`add` / `sub` / `mul` / `div` / `min` / `max` / `copysign` を所有させた。
- `stdlib/core/math/f64/unary.nepl` を追加し、`sqrt` / `abs` / `neg` / `ceil` / `floor` / `trunc` / `nearest` を所有させた。
- `stdlib/core/math/f64/compare.nepl` を追加し、等値・大小比較を所有させた。
- `stdlib/core/math/f64.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、f64 facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。
- facade doctest は float literal 推論に依存せず、`core/cast` で明示的に f64 値を作る形へ修正した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/f64/binary.nepl -i stdlib/core/math/f64/unary.nepl -i stdlib/core/math/f64/compare.nepl -i stdlib/core/math/f64.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-f64-responsibility-split.json -j 1`: total=23, passed=23
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math convert float responsibility split

`stdlib/core/math/convert/float.nepl` は整数から float、float から i32、float から i64、f32 / f64 幅変換を 676 lines の 1 file に持ち続けていた。変換方向ごとに責務を分け、root を facade 化した。

進捗:

- `stdlib/core/math/convert/float/int_to_float.nepl` を追加し、i32 / i64 から f32 / f64 への signed / unsigned 変換を所有させた。
- `stdlib/core/math/convert/float/float_to_i32.nepl` を追加し、f32 / f64 から i32 への truncation / saturating truncation を所有させた。
- `stdlib/core/math/convert/float/float_to_i64.nepl` を追加し、f32 / f64 から i64 への truncation / saturating truncation を所有させた。
- `stdlib/core/math/convert/float/float_width.nepl` を追加し、`promote_f32_to_f64` / `demote_f64_to_f32` を所有させた。
- `stdlib/core/math/convert/float.nepl` は 4 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、convert/float facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/convert/float/int_to_float.nepl -i stdlib/core/math/convert/float/float_to_i32.nepl -i stdlib/core/math/convert/float/float_to_i64.nepl -i stdlib/core/math/convert/float/float_width.nepl -i stdlib/core/math/convert/float.nepl -i stdlib/core/math/convert.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-convert-float-responsibility-split.json -j 1`: total=24, passed=24
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib math split policy は passed。remote main 由来の別件として `nodesrc/test_static_check_boundary_responsibility.js` が削除済み `passes/move_check.rs` を要求する stale warning を検出したため、`ISS-20260506T104320731Z-STATIC-CHECK-SOURCE-POLICY-STILL-REQ-3078A6E1` として分離した。

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math u8 responsibility split

`stdlib/core/math/u8.nepl` は core/math root からは分離済みだったが、u8 の算術・比較演算を 389 lines の 1 file に持ち続けていた。i32 / i64 と同じく、型別 root を facade 化し、責務別 submodule へ再分割した。

進捗:

- `stdlib/core/math/u8/arith.nepl` を追加し、`add_u8` / `sub_u8` / `mul_u8` / `div_u_u8` / `rem_u_u8` と u8 算術 overload を所有させた。
- `stdlib/core/math/u8/compare.nepl` を追加し、`eq_u8` / `ne_u8` / `lt_u_u8` / `le_u_u8` / `gt_u_u8` / `ge_u_u8` と u8 比較 overload を所有させた。
- `stdlib/core/math/u8.nepl` は 2 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、u8 facade に関数本体が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/u8/arith.nepl -i stdlib/core/math/u8/compare.nepl -i stdlib/core/math/u8.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-u8-responsibility-split.json -j 1`: total=21, passed=21
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math int128 responsibility split

`stdlib/core/math/int128.nepl` は u128/i128 の型定義、u128 演算、i128 演算、wide multiply を 240 lines の 1 file に持ち続けていた。128-bit 整数 API の root を facade 化し、型定義と演算対象ごとに責務を分けた。

進捗:

- `stdlib/core/math/int128/types.nepl` を追加し、`u128` / `i128` の 2 word 表現型だけを所有させた。
- `stdlib/core/math/int128/u128.nepl` を追加し、u128 の `new` / `to_u128` / `add` / `sub` / `lt` と `mul_wide` を所有させた。
- `stdlib/core/math/int128/i128.nepl` を追加し、i128 の `new` / `to_i128` / `add` / `sub` / `mul` / `lt` を所有させた。
- `stdlib/core/math/int128.nepl` は 3 submodule を再 export する facade にし、関数本体と struct 定義を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、int128 facade に実装が戻らないこと、各 submodule の行数上限、関数/struct の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/int128/types.nepl -i stdlib/core/math/int128/u128.nepl -i stdlib/core/math/int128/i128.nepl -i stdlib/core/math/int128.nepl -i stdlib/core/math.nepl -i tests/stdlib/math.n.md -i tests/stdlib/numerics.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-int128-responsibility-split.json -j 1`: total=22, passed=22
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: core/math i32 bitwise internal split

`stdlib/core/math/i32/bitwise.nepl` は i32 の AND/OR/XOR、shift/rotate、bit count intrinsic を 352 lines の 1 file に持ち続けていた。i32 bitwise root を facade 化し、演算カテゴリごとに責務を分けた。

進捗:

- `stdlib/core/math/i32/bitwise/binary.nepl` を追加し、`and` / `or` / `xor` を所有させた。
- `stdlib/core/math/i32/bitwise/shift.nepl` を追加し、`shl` / `shr_s` / `shr_u` / `rotl` / `rotr` を所有させた。
- `stdlib/core/math/i32/bitwise/count.nepl` を追加し、`clz` / `ctz` / `popcnt` を所有させた。
- `stdlib/core/math/i32/bitwise.nepl` は 3 submodule を再 export する facade にし、関数本体を持たない形にした。
- `nodesrc/test_stdlib_math_module_split.js` を更新し、i32 bitwise facade に実装が戻らないこと、各 submodule の行数上限、関数の所有先を source policy で固定した。

検証:

- `node nodesrc/test_stdlib_math_module_split.js`: passed
- `node nodesrc/tests.js -i stdlib/core/math/i32/bitwise/binary.nepl -i stdlib/core/math/i32/bitwise/shift.nepl -i stdlib/core/math/i32/bitwise/count.nepl -i stdlib/core/math/i32/bitwise.nepl -i stdlib/core/math/i32.nepl -i stdlib/core/math.nepl -i tests/stdlib/numerics.n.md -i tests/stdlib/math.n.md -i stdlib/tests/math.n.md --no-tree -o tmp/math-i32-bitwise-internal-split.json -j 1`: total=23, passed=23
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl`、`alloc/collections/vec.nepl`、`core/mem.nepl` など、残る巨大 file / 巨大 submodule の分割を継続する。

## 2026-05-06 追加対応メモ: alloc/string storage boundary split

`stdlib/alloc/string.nepl` は UTF-8 validation 分割後も、`[len][bytes...]` storage 確保、`RegionToken` projection、raw memory から `str` への ownership finalization を root に残していた。これは `str` のメモリ安全境界であり、builder / slice / split / numeric conversion と同居させると巨大 file 化だけでなく、unchecked construction 経路の監視も曖昧になる。

進捗:

- `stdlib/alloc/string/storage.nepl` を追加し、`string_alloc_region`、`string_region_len_ptr`、`string_region_data_ptr`、`string_addr`、`string_data_ptr`、`string_finish`、`string_from_addr_unchecked`、`string_finish_base`、`string_from_mem_unchecked_result`、`string_from_utf8_mem_result` を所有させた。
- `stdlib/alloc/string.nepl` は `./string/storage` を public re-export する facade として利用者向け import path を維持し、storage 関数本体は持たない形にした。
- `stdlib/alloc/string/utf8.nepl` の責務説明を更新し、検証後の `str` 構築責務が `alloc/string/storage` にあることを明記した。
- `nodesrc/test_stdlib_string_storage_boundary.js` を追加し、root に storage function body が戻らないこと、storage module が UTF-8 検証を通してから unchecked construction を呼ぶこと、`RegionToken` projection が reference 経由であることを固定した。
- `nodesrc/test_stdlib_string_no_unsafe_unwraps.js` と `nodesrc/test_stdlib_string_utf8_boundary.js` を storage module 配置へ追従させた。

検証:

- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_utf8_boundary.js`: passed
- `node nodesrc/test_stdlib_string_storage_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/text_utf8.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-storage-boundary-split-rebased.json -j 1`: total=56, passed=56
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には builder、slice/search/split、numeric parse/format が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string access and builder boundary split

`stdlib/alloc/string.nepl` は storage 分割後も、byte length / byte access helper と `StringBuilder` の owner state / grow / build logic を root に持ち続けていた。builder は `len` に依存するため、builder だけを切り出して root を import すると循環 import になる。byte accessor を `alloc/string/access` へ分け、builder が access/storage を直接 import する構成にした。

進捗:

- `stdlib/alloc/string/access.nepl` を追加し、`len`、`str_byte_len`、`byte_at`、`string_byte_at_unchecked` を所有させた。
- `stdlib/alloc/string/builder.nepl` を追加し、`StringBuilder`、capacity/grow helper、`sb_append_result`、char/ascii/byte append、`sb_build_result` を所有させた。
- `stdlib/alloc/string.nepl` は `./string/access` と `./string/builder` を public re-export し、slice / numeric と横断する `sb_append_slice_result`、`sb_append_i32_result` は root に残した。
- `alloc/string.nepl` は 3165 lines から 2583 lines まで下がった。`alloc/string/access.nepl` は 103 lines、`alloc/string/builder.nepl` は 513 lines。
- source policy を access/builder の所有先へ更新し、root に accessor / builder storage / builder grow/build が戻らないことを固定した。

検証:

- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_builder_owner_boundary.js`: passed
- `node nodesrc/test_stdlib_string_access_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/text_utf8.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-builder-boundary-split-rebased2.json -j 1`: total=56, passed=56
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には concat、slice/search/split、numeric parse/format、float formatting が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string search boundary split

`stdlib/alloc/string.nepl` は byte accessor / builder 分割後も、byte 列比較、prefix/suffix 判定、UTF-8 boundary predicate、split/search 用位置一致判定を root に持ち続けていた。これらは raw memory を直接持たず、`alloc/string/access` と `alloc/string/utf8` の上に構成できるため、slice / split / numeric conversion と同居させる必要がない。

進捗:

- `stdlib/alloc/string/search.nepl` を追加し、`str_eq`、`str_eq_loop`、`str_is_space`、`str_starts_with`、`str_eq_at`、`str_starts_with_at`、`str_ends_with`、`str_utf8_is_boundary`、`str_match_at`、`str_find`、`str_range_eq` を所有させた。
- `stdlib/alloc/string.nepl` は `./string/search` を public re-export し、slice/split/numeric 側は search API を利用する構成にした。
- `alloc/string.nepl` は 2583 lines から 2241 lines まで下がった。`alloc/string/search.nepl` は 356 lines。
- source policy を更新し、`str_is_space` の match arm 検査、self-host lexer 向け `str_starts_with_at` 境界、root に search function body が戻らないことを固定した。

検証:

- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_search_boundary.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_string_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/text_utf8.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-search-boundary-split.json -j 1`: total=56, passed=56
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には slice/char/split、numeric parse/format、float formatting が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string slice and char boundary split

`stdlib/alloc/string.nepl` は search 分割後も、byte slice、CR trim、UTF-8 char decode、char index、char slice、char predicate、trim helper を root に持ち続けていた。これらは `alloc/string/access`、`alloc/string/search`、`alloc/string/storage`、`alloc/string/utf8` の上に構成できる slice/char boundary の責務であり、split や numeric parse/format と同居させる必要がない。

進捗:

- `stdlib/alloc/string/slice.nepl` を追加し、`str_trim_suffix_cr`、`str_slice_trim_suffix_cr`、`str_slice_result`、`str_slice`、`str_next_char_result`、`str_char_count`、`str_char_byte_index_result`、`str_char_at_result`、`str_slice_chars_result`、`str_starts_with_char`、`str_contains_char`、`str_trim` などを所有させた。
- `stdlib/alloc/string.nepl` は `./string/slice` を public re-export し、既存 facade から slice/char API を参照できる構成にした。
- `alloc/string.nepl` の root file は 1909 lines、`alloc/string/slice.nepl` は 452 lines。root から slice/char 実装本体を外し、残る責務を split と numeric parse/format へ狭めた。
- source policy を更新し、slice/char 関数本体が root に戻らないこと、UTF-8 boundary を検証してから owned string を構築すること、char decode が `StringUtf8LeadKind` の enum match を使うことを固定した。

検証:

- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/test_stdlib_string_slice_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/text_utf8.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-slice-char-boundary-split.json -j 1`: total=56, passed=56
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には split、numeric parse/format、float formatting、facade としての残責務が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string split scanner boundary split

`stdlib/alloc/string.nepl` は slice/char 分割後も、allocation-free split scanner の enum state、step struct、count/next scanner を root に持ち続けていた。split scanner は `alloc/string/access` と `alloc/string/search` の上に構成でき、owned split vector を返さない range scanner として独立させられる。

進捗:

- `stdlib/alloc/string/split.nepl` を追加し、`StrSplitStepKind`、`StrSplitStep`、`str_split_count`、`str_split_done_step`、`str_split_next` を所有させた。
- `stdlib/alloc/string.nepl` は `./string/split` を public re-export し、既存 facade から split scanner API を参照できる構成にした。
- `alloc/string.nepl` の root file は 1776 lines、`alloc/string/split.nepl` は 145 lines。root から split scanner 実装本体を外し、残る責務を numeric parse/format、float formatting、concat/build helper へ狭めた。
- source policy を更新し、split scanner state が `StrSplitStepKind` enum のままであること、root に split 実装本体が戻らないこと、`Vec<str>` / `Vec<i32>` を返す allocation-bearing split が戻らないことを固定した。

検証:

- `node nodesrc/test_stdlib_string_split_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_fs_no_unsafe_unwraps.js`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/split.nepl -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md --no-tree -o tmp/string-split-boundary-split.json -j 1`: total=36, passed=36
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には numeric parse/format、float formatting、concat/build helper、facade としての残責務が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string integer conversion boundary split

`stdlib/alloc/string.nepl` は split scanner 分割後も、bool / i32 / i64 / u128 / i128 conversion、digit/radix helper、u128/i128 補助演算を root に持ち続けていた。integer conversion は `access`、`search`、`slice`、`storage`、`builder` の上に構成できる独立した境界であり、float formatting や concat と同じ file に置く必要がない。

進捗:

- `stdlib/alloc/string/integer.nepl` を追加し、`from_bool`、`to_bool`、`from_i32_radix`、`to_i64_radix`、`from_u128_radix`、`to_i128_radix`、`U128DivRem` などを所有させた。
- `from_i128_radix` の負号付与は root の `concat_result` に依存せず、`StringBuilder` 経由で構築するようにした。
- `stdlib/std/stdio/print.nepl` は `alloc/string/integer` を直接 import し、`print_i32` が broad string root の qualified facade に依存しないようにした。
- `alloc/string.nepl` の root file は 768 lines、`alloc/string/integer.nepl` は 1043 lines。root から integer conversion 実装本体を外し、残る責務を float formatting/parsing、concat/build helper、find へ狭めた。
- source policy を更新し、integer 実装本体が root に戻らないこと、u128 overflow check が残ること、stdio が integer conversion module を direct import することを固定した。
- commit 後に remote main の `8d12f487` / `e2a802ad` を取り込み、同 branch を `origin/main` へ rebase してから再検証した。

検証:

- `node nodesrc/test_stdlib_string_integer_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_stdio_print_i32_boundary.js`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: string/integer 関連 policy は passed。rebase 後の remote main 由来で `nodesrc/test_stdlib_io_bytebuf_owner_boundary.js` が `fs_finish_read_buffer` の `dealloc_raw` / `dealloc_ptr` 期待差分を warning として報告した。
- `node nodesrc/tests.js -i stdlib/alloc/string/integer.nepl -i stdlib/alloc/string/split.nepl -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/std/stdio/print.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-integer-boundary-split-rebased-no-text-utf8.json -j 1`: total=51, passed=51
- `node nodesrc/tests.js -i ... -i tests/stdlib/text_utf8.n.md --no-tree -o tmp/string-integer-boundary-split-rebased.json -j 1`: total=60, passed=57, failed=3。失敗は `fs_open_with_flags` の Resource IR owner violation で、remote main の既存 open issue `ISS-20260506T130126516Z-RESOURCE-OWNER-SUMMARIES-REJECT-FS-A-7E58243F` 系として扱う。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には float formatting/parsing、concat/build helper、find、facade としての残責務が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-06 追加対応メモ: alloc/string float conversion boundary split

`stdlib/alloc/string.nepl` は integer conversion 分割後も、f64/f32 formatting、fraction digit append、10 進 float parser を root に持ち続けていた。float conversion は `access`、`builder`、`integer` の上に構成でき、concat/build helper や find と同じ file に置く必要がない。

進捗:

- `stdlib/alloc/string/float.nepl` を追加し、`from_f64_result`、`from_f64`、`to_f64`、`from_f32`、`to_f32`、fraction formatting helper を所有させた。
- `stdlib/alloc/string.nepl` は `./string/float` を public re-export し、root から float conversion 実装本体を除去した。
- `alloc/string.nepl` の root file は 266 lines、`alloc/string/float.nepl` は 522 lines。root は concat/build helper、find、facade の残責務まで縮小した。
- source policy を更新し、float 実装本体が root に戻らないこと、StringBuilder 経由で formatting すること、raw scratch formatting が戻らないことを固定した。
- commit 後に remote main の `46d3d561` を取り込み、同 branch を `origin/main` へ rebase してから再検証した。

検証:

- `node nodesrc/test_stdlib_string_float_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_string_integer_boundary.js`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: string/float 関連 policy は passed。remote main 由来で `nodesrc/test_stdlib_io_bytebuf_owner_boundary.js` が `fs_finish_read_buffer` の `dealloc_raw` / `dealloc_ptr` 期待差分を warning として報告した。
- `node nodesrc/tests.js -i stdlib/alloc/string/float.nepl -i stdlib/alloc/string/integer.nepl -i stdlib/alloc/string/split.nepl -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/access.nepl -i stdlib/alloc/string/builder.nepl -i stdlib/alloc/string/storage.nepl -i stdlib/alloc/string/utf8.nepl -i stdlib/alloc/string.nepl -i stdlib/std/stdio/print.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string.n.md -i tests/stdlib/string_char.n.md -i tests/stdlib/string_numeric_overflow.n.md --no-tree -o tmp/string-float-boundary-split-no-text-utf8.json -j 1`: total=51, passed=51
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string.nepl` には concat/build helper、find、facade としての残責務が残っており、`alloc/collections/vec.nepl`、`core/mem.nepl` などの巨大 file / submodule 分割も継続する。

## 2026-05-07 追加対応メモ: alloc/collections/vec types/storage/access split

`stdlib/alloc/collections/vec.nepl` は sort 分割後も、型定義、storage allocation/free、基本 observer、mutation、transform、query を root に持ち続けていた。今回は `Vec` の基礎 owner state に近い型・storage・observer 境界を root から分離し、今後の `OwnedBuffer<T>` 化へ進めやすい形にした。

進捗:

- `stdlib/alloc/collections/vec/types.nepl` を追加し、`VecStorageState` / `Vec` / `VecDataLen` / `VecPop` / `VecPartition` を所有させた。
- `stdlib/alloc/collections/vec/storage.nepl` を追加し、`vec_empty` / `vec_alloc_empty` / `vec_storage_mem_ptr` / `vec_free_storage` / `new` / `with_capacity` / `filled` を所有させた。
- `stdlib/alloc/collections/vec/access.nepl` を追加し、`len` / `cap` / `data_ptr` / `data_mem_ptr` / `data_len` / `is_empty` を所有させた。
- `stdlib/alloc/collections/vec.nepl` root は `types` を public re-export し、`storage` / `access` の薄い wrapper を持つ構成にした。これにより `#import "alloc/collections/vec" as v` の qualified API は維持しつつ、実装本体は submodule が所有する。
- `nepl-core/src/loader.rs` の exact raw-memory boundary を `vec/storage.nepl` / `vec/access.nepl` / `vec/types.nepl` / `vec/sort/common.nepl` / `vec/sort/merge.nepl` に追従させた。
- `nodesrc/test_stdlib_vec_no_unsafe_unwraps.js` / `nodesrc/test_stdlib_vec_borrowed_observers.js` を更新し、root に型/storage/access 実装本体が戻らないこと、root wrapper が submodule へ委譲すること、loader boundary が split path を含むことを固定した。
- `stdlib/alloc/collections/vec.nepl` root から型定義、storage allocation/free、基本 observer の実装本体を除き、責務ごとの小さな submodule に移した。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `trunk build`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/types.nepl -i stdlib/alloc/collections/vec/storage.nepl -i stdlib/alloc/collections/vec/access.nepl --no-tree -o tmp/vec-storage-module-split-submodules-final.json -j 1 --dist web/dist`: total=10, passed=10
- `node nodesrc/tests.js -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-storage-module-split-vec-only-final.json -j 1 --dist web/dist`: total=9, passed=9

追加確認:

- `tests/stdlib/sort.n.md` full suite は doctest#18-#22 で失敗した。内容は raw pointer fixture が現在の Resource IR/effect gate とずれている問題であり、`ISS-20260507T015824839Z-SORT-RAW-POINTER-DOCTESTS-BYPASS-CUR-41C19268` として分離した。

この issue はまだ open のまま扱う。`alloc/collections/vec.nepl` には mutation / transform / query が残り、`core/mem.nepl` など他の巨大 file / submodule 分割も継続する。

## 2026-05-07 追加対応メモ: alloc/encoding/json types/builders/access/escape/serialize split

`stdlib/alloc/encoding/json.nepl` は `JsonValue` / `JsonMember` / `JsonEscapeKind` の型定義、builder、accessor、escape、serializer、Vec payload の raw load を 1 file に同居させていた。今回は JSON API を root facade と責務別 submodule へ分割し、raw memory 境界を serializer module に閉じ込めた。

進捗:

- `stdlib/alloc/encoding/json/types.nepl` を追加し、`JsonValue`、`JsonMember`、`JsonEscapeKind` を所有させた。JSON kind と escape kind は enum として維持し、数値 tag / 文字列 tag へ戻さない。
- `stdlib/alloc/encoding/json/builders.nepl` を追加し、`json_null` / `json_bool` / `json_number` / `json_string` / Array/Object builder を所有させた。
- `stdlib/alloc/encoding/json/access.nepl` を追加し、`json_is_null` と `json_as_*` accessor を所有させた。variant 判定は `JsonValue` enum の `match` に寄せた。
- `stdlib/alloc/encoding/json/escape.nepl` を追加し、`json_escape_kind` / `json_escape` / `json_quote_string` を所有させた。escape classifier は char literal `match` のまま固定した。
- `stdlib/alloc/encoding/json/serialize.nepl` を追加し、`json_serialize_array` / `json_serialize_object` / `json_serialize` と Vec payload traversal の `mem_ptr_addr` / `load` を所有させた。
- `stdlib/alloc/encoding/json.nepl` root は public re-export と facade doctest だけに縮小した。line count は root 28 行、`types` 86 行、`builders` 210 行、`access` 201 行、`escape` 119 行、`serialize` 136 行。
- `nepl-core/src/loader.rs` の exact raw-memory boundary に `alloc/encoding/json/serialize.nepl` を追加し、JSON serializer の raw Vec payload traversal を root facade ではなく実装先だけに許可した。
- `stdlib/std/test/report.nepl` は JSON quote だけを qualified call していたため、root facade alias ではなく `alloc/encoding/json/escape` へ正確に依存させた。
- `tests/stdlib/json_typed_values.n.md` の array payload 確認は `Vec.len` を borrowed observer として呼び、取り出した Vec owner を `free` する形へ修正した。
- source policy を更新し、root facade に implementation body が戻らないこと、raw Vec payload traversal が serializer に集約されること、escape/serialize が string helper overload を facade 越しに漏らさないこと、std/test/report の qualified import が exact escape module を指すことを固定した。

検証:

- `trunk build`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed
- `cargo test -p nepl-core --test effects`: 27 passed
- `node nodesrc/test_stdlib_json_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_stdlib_std_test_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/encoding/json.nepl -i stdlib/alloc/encoding/json/types.nepl -i stdlib/alloc/encoding/json/builders.nepl -i stdlib/alloc/encoding/json/access.nepl -i stdlib/alloc/encoding/json/escape.nepl -i stdlib/alloc/encoding/json/serialize.nepl --no-tree -o tmp/json-module-split-focused.json -j 1 --dist web/dist`: total=6, passed=6
- `node nodesrc/tests.js -i stdlib/tests/json.n.md -i tests/stdlib/json_typed_values.n.md --no-tree -o tmp/json-module-split-json-suite.json -j 1 --dist web/dist`: total=8, passed=8
- `node nodesrc/tests.js -i stdlib/std/test/report.nepl -i stdlib/std/test.nepl -i stdlib/tests/test.n.md --no-tree -o tmp/json-module-split-std-test-suite.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed。latest `origin/main` への rebase 後は JSON 関連 policy に加え、remote main 側で `cell_state_raw_range.rs` / `initialized_external_io_effect.rs` の既知 source-policy warning も解消済み。

この issue はまだ open のまま扱う。`alloc/encoding/json.nepl` は facade 化できたが、`core/mem.nepl`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/hash/sha256 types/round/padding/schedule/compress/digest/api split

`stdlib/alloc/hash/sha256.nepl` は `Sha256` / `Sha256State` の型定義、round constant、padding、message schedule、compression、digest 出力、public incremental API を 1 file に同居させていた。hash は selfhost に必要な基本機能であり、SHA-256 の各段階を混ぜたままにすると Resource IR の回帰調査や typed API の監査単位が大きくなりすぎる。

進捗:

- `stdlib/alloc/hash/sha256/types.nepl` を追加し、`Sha256`、`Sha256State`、`sha256_initial_state` を所有させた。
- `stdlib/alloc/hash/sha256/round.nepl` を追加し、`sha256_k`、choose / majority、big/small sigma の 32-bit word 演算を所有させた。
- `stdlib/alloc/hash/sha256/padding.nepl` を追加し、padding 後の仮想 byte 列、length field、big-endian word 読み取りを所有させた。
- `stdlib/alloc/hash/sha256/schedule.nepl` を追加し、64 word message schedule の構築を所有させた。
- `stdlib/alloc/hash/sha256/compress.nepl` を追加し、64 round と block 列 compression を所有させた。
- `stdlib/alloc/hash/sha256/digest.nepl` を追加し、final state から 32 byte digest `Vec<i32>` への変換を所有させた。
- `stdlib/alloc/hash/sha256/api.nepl` を追加し、`new_sha256` / `sha256_update` / `sha256_finalize` の public incremental API だけを所有させた。
- `stdlib/alloc/hash/sha256.nepl` root は public re-export だけの facade へ縮小した。line count は root 32 行、`types` 54 行、`round` 195 行、`padding` 124 行、`schedule` 81 行、`compress` 93 行、`digest` 80 行、`api` 63 行。
- `nodesrc/test_stdlib_sha256_no_unsafe_unwraps.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、全 submodule が unsafe unwrap / unreachable を使わないこと、`sha256_rounds_loop` の `Result::Err e` shadowing が戻らないことを固定した。

検証:

- `node nodesrc/test_stdlib_sha256_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/tests/hash.n.md -i stdlib/alloc/hash/sha256.nepl -i stdlib/alloc/hash/sha256/types.nepl -i stdlib/alloc/hash/sha256/round.nepl -i stdlib/alloc/hash/sha256/padding.nepl -i stdlib/alloc/hash/sha256/schedule.nepl -i stdlib/alloc/hash/sha256/compress.nepl -i stdlib/alloc/hash/sha256/digest.nepl -i stdlib/alloc/hash/sha256/api.nepl --no-tree -o tmp/sha256-module-split-hash-suite.json -j 1 --dist web/dist`: total=1, passed=1

この issue はまだ open のまま扱う。`alloc/hash/sha256.nepl` は facade 化できたが、`core/mem.nepl`、`std/fs/path.nepl` / `std/fs/raw.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: std/fs/path entry/normalize split

`stdlib/std/fs/path.nepl` は directory entry 名の byte helper、UTF-8 複製、sort helper、relative path normalization を 1 file に同居させていた。directory listing の entry 処理と path component stack による normalization は検証観点が異なるため、`std/fs/path` root を facade 化して責務を分けた。

進捗:

- `stdlib/std/fs/path/entry.nepl` を追加し、`fs_str_lt`、`fs_sort_strings`、`fs_string_from_bytes`、`fs_dir_name_is_dot` を所有させた。
- `stdlib/std/fs/path/normalize.nepl` を追加し、host path byte 検出、component range stack、`fs_normalize_relative_builder`、`fs_normalize_relative` を所有させた。
- `stdlib/std/fs/path.nepl` root は public re-export と既存 facade doctest だけに縮小した。line count は root 43 行、`path/entry.nepl` 119 行、`path/normalize.nepl` 280 行。
- `nodesrc/test_stdlib_fs_no_unsafe_unwraps.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、entry byte helper が `path/entry` にあり、normalization が `path/normalize` にあることを固定した。
- `nodesrc/test_stdlib_bytebuf_utf8_boundary.js` も `fs_string_from_bytes` の移動先 `path/entry.nepl` を確認するように更新し、directory entry byte range が UTF-8 検証後にだけ `str` へ確定する境界を維持した。

検証:

- `node nodesrc/test_stdlib_fs_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_bytebuf_utf8_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/std/fs/path.nepl -i stdlib/std/fs/path/entry.nepl -i stdlib/std/fs/path/normalize.nepl -i tests/stdlib/std_fs_path.n.md -i tests/stdlib/fs_path_normalize.n.md --no-tree -o tmp/fs-path-module-split-focused.json -j 1 --dist web/dist`: total=2, passed=2
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed

この issue はまだ open のまま扱う。`std/fs/path` は facade 化できたが、`std/fs/raw.nepl`、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: std/fs/raw wasi/fd_io/llvm split

`stdlib/std/fs/raw.nepl` は WASI extern / bare wasm fallback、fd_read/fd_write scratch helper、ByteBuf finish helper、LLVM syscall fallback を 1 file に同居させていた。target ABI と Resource IR が追跡すべき out-pointer 初期化境界は検証観点が異なるため、`std/fs/raw` root を facade 化して責務を分けた。

進捗:

- `stdlib/std/fs/raw/wasi.nepl` を追加し、WASI preview1 extern と bare wasm fallback を所有させた。
- `stdlib/std/fs/raw/fd_io.nepl` を追加し、`fs_fd_read_into_result` / `fs_fd_write_from_result` の iovec/out-pointer scratch 初期化と `fs_finish_read_buffer` の ByteBuf ownership normalization を所有させた。
- `stdlib/std/fs/raw/llvm.nepl` を追加し、LLVM target の Linux syscall fallback、C string conversion、WASI 互換名の fallback implementation を所有させた。
- `stdlib/std/fs/raw.nepl` root は public re-export と facade doctest だけに縮小した。line count は root 31 行、`raw/wasi.nepl` 45 行、`raw/fd_io.nepl` 155 行、`raw/llvm.nepl` 203 行。
- `nodesrc/test_stdlib_fs_no_unsafe_unwraps.js` を分割後の module 一覧へ追従し、root facade に syscall / helper body が戻らないこと、fd scratch が `raw/fd_io` にあり、LLVM fallback が `raw/llvm` にあることを固定した。
- `nodesrc/test_stdlib_io_bytebuf_owner_boundary.js` は `fs_finish_read_buffer` の移動先 `raw/fd_io.nepl` を確認するように更新した。

検証:

- `node nodesrc/test_stdlib_fs_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_io_bytebuf_owner_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/std/fs/raw.nepl -i stdlib/std/fs/raw/wasi.nepl -i stdlib/std/fs/raw/fd_io.nepl -i stdlib/std/fs/raw/llvm.nepl -i tests/stdlib/std_fs_bytes.n.md -i tests/stdlib/std_fs.n.md --no-tree -o tmp/fs-raw-module-split-focused.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`std/fs/raw` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: platforms/wasix/tui tty/ansi/text/style/box/buffer split

`stdlib/platforms/wasix/tui.nepl` は WASIX TTY ABI、ANSI 直接出力、表示幅計算、styled string、box line、raw storage 行バッファを 1 file に同居させていた。TTY と buffer は raw memory / extern boundary を持つ一方、text / box helper は pure helper なので検証観点が異なる。今回は `platforms/wasix/tui` root を facade 化し、責務を submodule に分けた。

進捗:

- `stdlib/platforms/wasix/tui/tty.nepl` を追加し、`tty_get` / `tty_set` extern、`TerminalSize`、`get_tty_state_result`、raw mode 復元契約を所有させた。
- `stdlib/platforms/wasix/tui/ansi.nepl` を追加し、cursor / screen / typed color 直接出力 helper を所有させた。
- `stdlib/platforms/wasix/tui/text.nepl` を追加し、`repeat_text`、表示幅計算、line padding/clipping、`text_wrap_lines` と checked `Vec<str>` accumulation を所有させた。
- `stdlib/platforms/wasix/tui/style.nepl` を追加し、`style_text` と OSC8 `hyperlink_text` の文字列構築を所有させた。
- `stdlib/platforms/wasix/tui/box.nepl` を追加し、box width contract と `line_box` / `line_box_styled` / `line_top` / `line_bottom` を所有させた。
- `stdlib/platforms/wasix/tui/buffer.nepl` を追加し、raw storage 行バッファと差分描画を所有させた。
- `stdlib/platforms/wasix/tui.nepl` root は `pub #import ... as @merge` の public facade に縮小した。qualified import 経由の既存 `tui::line_box` などを維持するため、submodule は `as *` ではなく `@merge` で root に合流させている。
- `nodesrc/test_stdlib_wasix_tui_no_unsafe_unwraps.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、typed color API、TTY `Result` API、checked `Vec<str>` accumulation を固定した。
- line count は root 28 行、`tty.nepl` 120 行、`ansi.nepl` 54 行、`text.nepl` 350 行、`style.nepl` 38 行、`box.nepl` 124 行、`buffer.nepl` 257 行。

検証:

- `node nodesrc/test_stdlib_wasix_tui_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i tests/stdlib/features_tui.n.md --no-tree -o tmp/wasix-tui-module-split-features-merge.json -j 1 --dist web/dist`: total=5, passed=5
- `node nodesrc/tests.js -i stdlib/features/tui.nepl -i tests/stdlib/features_tui.n.md --no-tree -o tmp/wasix-tui-module-split-features-with-facade.json -j 1 --dist web/dist`: total=6, passed=6
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`platforms/wasix/tui` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/hashmap types/storage/probe/rehash/api split

`stdlib/alloc/collections/hashmap.nepl` は型定義、typed storage の確保・解放、linear probing、rehash、公開 API、doctest を 1 file に同居させていた。HashMap は selfhost compiler の symbol table などの基盤データ構造になるため、所有権境界と probe / rehash の不変条件を module 単位で検証できる形へ分割した。

進捗:

- `stdlib/alloc/collections/hashmap/types.nepl` を追加し、`HashMapBucketState`、`HashMapInsertSlotState`、`HashMapStorage`、`HashMap` の型定義を所有させた。
- `stdlib/alloc/collections/hashmap/storage.nepl` を追加し、typed `Vec<HashMapBucketState>` / `Vec<Option<K>>` / `Vec<Option<V>>` の初期化、slot read/write、storage free を所有させた。
- `stdlib/alloc/collections/hashmap/probe.nepl` を追加し、bucket index、linear probing、present / insert slot search、rehash 用 storage insert を所有させた。
- `stdlib/alloc/collections/hashmap/rehash.nepl` を追加し、capacity 正規化、load limit、rehash、insert 前 grow 判定、constructor helper を所有させた。
- `stdlib/alloc/collections/hashmap/api.nepl` を追加し、`new` / `with_capacity` / `insert` / `get` / `contains` / `remove` / `len` / `free` の公開 API を所有させた。
- `stdlib/alloc/collections/hashmap.nepl` root は `pub #import ... as @merge` の public facade と user-defined key doctest に縮小した。qualified import と bare import の既存利用を維持するため、submodule は `@merge` で root に合流させている。
- `nodesrc/test_stdlib_hashmap_storage_contract.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、bucket state enum、typed storage、owner transfer、borrow read API、unsafe unwrap / raw memory 非使用を固定した。
- line count は root 77 行、`types.nepl` 81 行、`storage.nepl` 131 行、`probe.nepl` 103 行、`rehash.nepl` 102 行、`api.nepl` 190 行。

検証:

- `node nodesrc/test_stdlib_hashmap_storage_contract.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/hashmap.nepl -i stdlib/alloc/collections/hashmap/api.nepl --no-tree -o tmp/hashmap-module-split-doctests.json -j 1 --dist web/dist`: total=3, passed=3
- `node nodesrc/tests.js -i stdlib/alloc/collections/hashmap.nepl -i stdlib/alloc/collections/hashmap/types.nepl -i stdlib/alloc/collections/hashmap/storage.nepl -i stdlib/alloc/collections/hashmap/probe.nepl -i stdlib/alloc/collections/hashmap/rehash.nepl -i stdlib/alloc/collections/hashmap/api.nepl -i stdlib/tests/hashmap.n.md -i stdlib/tests/hashmap_str.n.md -i tests/stdlib/hash_collection_rehash.n.md -i tests/stdlib/collections_diag.n.md -i tests/stdlib/selfhost_req.n.md --no-tree -o tmp/hashmap-module-split-suite-no-traits-hash.json -j 1 --dist web/dist`: total=22, passed=22

補足:

- `tests/stdlib/traits_hash.n.md` は現在の main で compile_fail 期待 diag `resource.move.use_moved` と実出力 `resource.cell.moved` がずれているため、HashMap 分割とは別 issue として扱う。

この issue はまだ open のまま扱う。`alloc/collections/hashmap` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/hashset types/storage/probe/rehash/api split

`stdlib/alloc/collections/hashset.nepl` は HashMap と同様に型定義、typed storage の確保・解放、linear probing、rehash、公開 API、doctest を 1 file に同居させていた。HashSet は selfhost compiler の visited set / dedup set などで使う基盤データ構造になるため、HashMap と同じ module 境界へ揃えた。

進捗:

- `stdlib/alloc/collections/hashset/types.nepl` を追加し、`HashSetBucketState`、`HashSetInsertSlotState`、`HashSetStorage`、`HashSet` の型定義を所有させた。
- `stdlib/alloc/collections/hashset/storage.nepl` を追加し、typed `Vec<HashSetBucketState>` / `Vec<Option<T>>` の初期化、slot read/write、storage free を所有させた。
- `stdlib/alloc/collections/hashset/probe.nepl` を追加し、bucket index、linear probing、present / insert slot search、rehash 用 storage insert を所有させた。
- `stdlib/alloc/collections/hashset/rehash.nepl` を追加し、capacity 正規化、load limit、rehash、insert 前 grow 判定、constructor helper を所有させた。
- `stdlib/alloc/collections/hashset/api.nepl` を追加し、`new` / `with_capacity` / `insert` / `contains` / `remove` / `len` / `free` の公開 API を所有させた。
- `stdlib/alloc/collections/hashset.nepl` root は `pub #import ... as @merge` の public facade に縮小した。HashMap と同じ re-export 形式に揃え、bare import と qualified import の既存利用を維持する。
- `nodesrc/test_stdlib_hashset_storage_contract.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、bucket state enum、typed storage、owner transfer、borrow read API、unsafe unwrap / raw memory 非使用を固定した。
- line count は root 28 行、`types.nepl` 80 行、`storage.nepl` 105 行、`probe.nepl` 103 行、`rehash.nepl` 97 行、`api.nepl` 178 行。

検証:

- `node nodesrc/test_stdlib_hashset_storage_contract.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/hashset.nepl -i stdlib/alloc/collections/hashset/types.nepl -i stdlib/alloc/collections/hashset/storage.nepl -i stdlib/alloc/collections/hashset/probe.nepl -i stdlib/alloc/collections/hashset/rehash.nepl -i stdlib/alloc/collections/hashset/api.nepl -i stdlib/tests/hashset.n.md -i stdlib/tests/hashset_str.n.md -i tests/stdlib/hash_collection_rehash.n.md -i tests/stdlib/collections_diag.n.md -i tests/stdlib/traits_hash.n.md --no-tree -o tmp/hashset-module-split-suite-no-pipe.json -j 1 --dist web/dist`: total=22, passed=22

補足:

- `tests/stdlib/pipe_collections.n.md` は current main で selected import / RingBuffer overload の別件失敗が残るため、この分割 commit からは除外し、別 issue として扱う。

この issue はまだ open のまま扱う。`alloc/collections/hashset` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string/search compare/boundary/byte_find split

`stdlib/alloc/string/search.nepl` は byte 列比較、prefix/suffix、UTF-8 boundary predicate、byte find loop を 1 file に同居させていた。slice / split / parser から広く使われる基礎 API なので、検索・境界・比較の責務を module 単位で監査できるように分割した。

進捗:

- `stdlib/alloc/string/search/compare.nepl` を追加し、`str_eq`、`str_eq_loop`、`str_is_space`、`str_starts_with`、`str_eq_at`、`str_starts_with_at`、`str_ends_with`、`str_range_eq` を所有させた。
- `stdlib/alloc/string/search/boundary.nepl` を追加し、UTF-8 continuation 判定に依存する `str_utf8_is_boundary` を所有させた。
- `stdlib/alloc/string/search/byte_find.nepl` を追加し、`str_match_at` と `str_find` の byte search loop を所有させた。
- `stdlib/alloc/string/search.nepl` root は `pub #import` の public facade に縮小した。
- `nodesrc/test_stdlib_string_no_unsafe_unwraps.js` を分割後の module 一覧へ追従し、root facade に実装本体が戻らないこと、compare / boundary / byte_find の所有関数、line count boundary、unsafe unwrap 禁止を固定した。
- `nodesrc/test_stdlib_string_doc_no_boilerplate.js` の doc boilerplate policy 対象に新 submodule を追加した。
- line count は root 15 行、`compare.nepl` 240 行、`boundary.nepl` 55 行、`byte_find.nepl` 105 行。

検証:

- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `node nodesrc/test_stdlib_string_search_boundary.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_string_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/search.nepl -i stdlib/alloc/string/search/compare.nepl -i stdlib/alloc/string/search/boundary.nepl -i stdlib/alloc/string/search/byte_find.nepl --no-tree -o tmp/string-search-module-split-focused.json -j 1 --dist web/dist`: total=2, passed=2
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/split.nepl -i stdlib/alloc/string/find.nepl --no-tree -o tmp/string-search-module-split-callers.json -j 1 --dist web/dist`: total=3, passed=3
- `node nodesrc/tests.js -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md -i tests/stdlib/string_char.n.md --no-tree -o tmp/string-search-module-split-suite.json -j 1 --dist web/dist`: total=12, passed=12
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/string/search` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string/slice byte/char/trim split

`stdlib/alloc/string/slice.nepl` は byte slice、UTF-8 char traversal、ASCII trim helper を 1 file に同居させていた。slice construction は raw storage / UTF-8 boundary、char traversal は leading-byte enum match、trim は ASCII scanner という別責務なので、qualified import 互換を保った facade + submodule 構成へ分割した。

進捗:

- `stdlib/alloc/string/slice/byte.nepl` を追加し、`str_slice_result` / `str_slice` と owned string construction boundary を所有させた。
- `stdlib/alloc/string/slice/char.nepl` を追加し、`str_next_char_result`、char count、char index to byte offset、char slice、char predicate を所有させた。
- `stdlib/alloc/string/slice/trim.nepl` を追加し、`str_trim_suffix_cr`、`str_slice_trim_suffix_cr`、`str_trim` を所有させた。
- `stdlib/alloc/string/slice.nepl` root は `pub #import ... as @merge` の public facade に縮小した。`#import "alloc/string/slice" as string_slice` の qualified caller が `string_slice::str_trim` / `string_slice::str_slice_result` を引き続き解決できるようにしている。
- 前段の `alloc/string/search` facade も同じ qualified import 問題を持っていたため、`search/compare` / `search/boundary` / `search/byte_find` の re-export を `as @merge` に直した。
- source policy を更新し、slice / search facade に実装本体が戻らないこと、各 submodule の所有関数、qualified facade merge、line count boundary、UTF-8 leading-byte enum match を固定した。
- line count は `slice.nepl` 15、`slice/byte.nepl` 66、`slice/char.nepl` 281、`slice/trim.nepl` 130。

検証:

- `trunk build --release`: passed
- `node nodesrc/test_stdlib_string_search_boundary.js`: passed
- `node nodesrc/test_stdlib_string_slice_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/test_selfhost_string_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string/slice.nepl -i stdlib/alloc/string/slice/byte.nepl -i stdlib/alloc/string/slice/char.nepl -i stdlib/alloc/string/slice/trim.nepl --no-tree -o tmp/string-slice-module-split-focused-after-sync.json -j 1 --dist web/dist`: total=1, passed=1
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md -i tests/stdlib/string_char.n.md --no-tree -o tmp/string-slice-module-split-suite-after-sync.json -j 1 --dist web/dist`: total=13, passed=13
- `node nodesrc/tests.js -i tests/stdlib/selfhost_req.n.md -i examples/rpn.nepl -i examples/rpn_legacy.nepl --no-tree -o tmp/string-slice-module-split-callers-after-sync.json -j 1 --dist web/dist`: total=9, passed=9
- `node nodesrc/run_source_policy_regressions.js --warn-only`: stdlib/string/slice 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。
- `tests/stdlib/nm.n.md` は current main で別件 `effect.pure.calls_impure` (`nm/json_escape_mem_into` の raw load) が残るため、この slice split の合否からは切り分ける。

この issue はまだ open のまま扱う。`alloc/string/slice` は facade 化できたが、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/string/builder types/reserve/append/build split

`stdlib/alloc/string/builder.nepl` は `StringBuilder` 型、所有 storage、capacity grow、append、build finalization、raw-memory capability を 1 file に同居させていた。今回は root facade、型/owner state、capacity 管理、append、build の 5 つへ責務を分離した。

進捗:

- `stdlib/alloc/string/builder.nepl` を public facade 化し、`builder/types`、`builder/reserve`、`builder/append`、`builder/build` を `as @merge` で再公開した。
- `stdlib/alloc/string/builder/types.nepl` は `StringBuilder`、empty / owned pointer construction、len 更新、free を所有する。
- `stdlib/alloc/string/builder/reserve.nepl` は capacity 計算、初期 allocation、reserve/realloc、`string_builder_new` を所有する。
- `stdlib/alloc/string/builder/append.nepl` は `sb_append_result`、char/ascii/byte append、fallback wrapper を所有する。
- `stdlib/alloc/string/builder/build.nepl` は `sb_build_result` / `sb_build` と final `str` construction boundary を所有する。
- `nepl-core/src/loader.rs` の exact raw-memory boundary から root `alloc/string/builder.nepl` を外し、raw memory を実際に扱う `builder/types.nepl`、`builder/reserve.nepl`、`builder/append.nepl`、`builder/build.nepl` に capability を移した。
- source policy を更新し、root builder facade に実装本体が戻らないこと、submodule の line count、owner-preserving storage access、exact raw-memory boundary を固定した。
- line count は `builder.nepl` 15、`builder/types.nepl` 105、`builder/reserve.nepl` 179、`builder/append.nepl` 226、`builder/build.nepl` 84。

検証:

- `node nodesrc/test_stdlib_builder_owner_boundary.js`: passed
- `node nodesrc/test_stdlib_string_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_string_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- remote main `7e23143e` へ rebase 後に `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/string.nepl -i stdlib/tests/string.n.md -i tests/stdlib/string_extra.n.md -i tests/stdlib/string_char.n.md --no-tree -o tmp/string-builder-module-split-string-suite-after-rebase.json -j 1 --dist web/dist`: total=13, passed=13
- `node nodesrc/tests.js -i tests/stdlib/nm.n.md -i tests/stdlib/selfhost_req.n.md --no-tree -o tmp/string-builder-module-split-nm-selfhost-after-rebase.json -j 1 --dist web/dist`: total=11, passed=11
- `cargo fmt --check -p nepl-core`: passed
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: builder/string 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。

この issue はまだ open のまま扱う。`alloc/string/builder` は facade 化できたが、`core/mem.nepl`、collection の巨大 file、selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec root @merge facade

`stdlib/alloc/collections/vec.nepl` には各 submodule へ委譲する thin wrapper が残っており、root file 自体にも raw-memory boundary capability が付与されていた。今回は root を `as @merge` re-export だけの facade に縮小し、実装本体と raw memory capability を exact submodule へ限定した。

進捗:

- `stdlib/alloc/collections/vec.nepl` は `vec/types`、`vec/storage`、`vec/access`、`vec/raw`、`vec/mutation`、`vec/query`、`vec/transform`、`vec/sort` の public `@merge` re-export だけを持つ構成にした。
- root wrapper を削除し、`#import "alloc/collections/vec" as v` と `as *` の public API は `@merge` facade で維持する。
- `nepl-core/src/loader.rs` の exact raw-memory boundary から root `alloc/collections/vec.nepl` を外し、raw memory を実際に扱う Vec submodule だけへ capability を限定した。
- `nepl-core/tests/effects.rs` に split facade が raw-memory boundary にならない regression を追加し、Vec / StringBuilder の root facade に raw wasm body を置いた場合は `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、Vec root に function/type body が戻らないこと、private delegation alias が戻らないこと、loader boundary が exact implementation submodule だけを指すことを固定した。
- line count は `vec.nepl` 102、`vec/types.nepl` 101、`vec/storage.nepl` 191、`vec/access.nepl` 206、`vec/raw.nepl` 312、`vec/mutation.nepl` 259、`vec/query.nepl` 347、`vec/transform.nepl` 361、`vec/sort.nepl` 61。

検証:

- `git pull --ff-only origin main`: already up to date
- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `node nodesrc/test_stdlib_string_facade_boundary.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_split_facades_as_raw_memory_boundaries`: passed
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec.nepl --no-tree -o tmp/vec-root-facade-merge-root.json -j 1 --dist web/dist`: total=3, passed=3
- `node nodesrc/tests.js -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-root-facade-merge-suite.json -j 1 --dist web/dist`: total=9, passed=9
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: Vec / string facade 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec` root は pure facade 化できたが、Vec submodule の一部はまだ大きく、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象も残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec query split

`stdlib/alloc/collections/vec/query.nepl` は root からは分離済みだったが、index lookup、count/fold/reduce、find/any/all が 1 file に同居していた。今回は `query` 自体を public facade にし、borrowed query を責務別 submodule へ分離した。

進捗:

- `stdlib/alloc/collections/vec/query.nepl` は `query/get`、`query/aggregate`、`query/predicate` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/query/get.nepl` を追加し、`get` の範囲検査、empty storage 判定、`Option` result を所有させた。
- `stdlib/alloc/collections/vec/query/aggregate.nepl` を追加し、`count` / `fold` / `reduce` を所有させた。
- `stdlib/alloc/collections/vec/query/predicate.nepl` を追加し、`find` / `any` / `all` を所有させた。
- query submodule は direct raw intrinsic を持たず、raw access は `vec/raw` helper に閉じているため、`nepl-core/src/loader.rs` から `alloc/collections/vec/query.nepl` の raw-memory boundary capability を削除した。新 query submodule にも raw-memory boundary を付与しない。
- `nepl-core/tests/effects.rs` は query root / query submodule に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、query facade に implementation body が戻らないこと、各 query helper の所有 module、query 系 module に raw-memory boundary capability が付かないことを固定した。
- line count は `query.nepl` 19、`query/get.nepl` 67、`query/aggregate.nepl` 154、`query/predicate.nepl` 151。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/query.nepl -i stdlib/alloc/collections/vec/query/get.nepl -i stdlib/alloc/collections/vec/query/aggregate.nepl -i stdlib/alloc/collections/vec/query/predicate.nepl --no-tree -o tmp/vec-query-module-split-focused-pretrunk.json -j 1 --dist web/dist`: total=7, passed=7
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/query.nepl -i stdlib/alloc/collections/vec/query/get.nepl -i stdlib/alloc/collections/vec/query/aggregate.nepl -i stdlib/alloc/collections/vec/query/predicate.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-query-module-split-suite.json -j 1 --dist web/dist`: total=16, passed=16
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: Vec query 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/query` は facade 化できたが、Vec transform/raw/mutation などにはまだ分割余地があり、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象も残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec transform split and raw boundary tightening

`stdlib/alloc/collections/vec/transform.nepl` は root からは分離済みだったが、map、filter/partition、prefix transform が 1 file に同居していた。さらに `vec/access.nepl`、`vec/transform.nepl`、`vec/types.nepl` は direct raw intrinsic を持たないにもかかわらず raw-memory boundary capability を受けていた。今回は transform を責務別 submodule へ分離し、Vec の raw capability を direct raw operation を持つ module に限定した。

進捗:

- `stdlib/alloc/collections/vec/transform.nepl` は `transform/map`、`transform/filter`、`transform/prefix` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/transform/map.nepl` を追加し、element type 変換の `map` を所有させた。
- `stdlib/alloc/collections/vec/transform/filter.nepl` を追加し、predicate selection の `filter` / `partition` を所有させた。
- `stdlib/alloc/collections/vec/transform/prefix.nepl` を追加し、prefix boundary の `take_while` / `drop_while` を所有させた。
- direct raw intrinsic を持たない `vec/access.nepl`、`vec/types.nepl`、`vec/transform.nepl` の raw-memory boundary capability を `nepl-core/src/loader.rs` から削除した。transform submodule にも raw-memory boundary を付与しない。
- `nepl-core/tests/effects.rs` は access/types/transform root/transform submodule に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、transform facade に implementation body が戻らないこと、各 transform helper の所有 module、raw-free Vec module に raw-memory boundary capability が付かないことを固定した。
- line count は `transform.nepl` 19、`transform/map.nepl` 79、`transform/filter.nepl` 182、`transform/prefix.nepl` 126。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/transform.nepl -i stdlib/alloc/collections/vec/transform/map.nepl -i stdlib/alloc/collections/vec/transform/filter.nepl -i stdlib/alloc/collections/vec/transform/prefix.nepl --no-tree -o tmp/vec-transform-module-split-focused-pretrunk.json -j 1 --dist web/dist`: total=5, passed=5
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/transform.nepl -i stdlib/alloc/collections/vec/transform/map.nepl -i stdlib/alloc/collections/vec/transform/filter.nepl -i stdlib/alloc/collections/vec/transform/prefix.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-transform-module-split-suite.json -j 1 --dist web/dist`: total=14, passed=14
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: Vec transform/raw-boundary 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/transform` は facade 化できたが、Vec raw/mutation などにはまだ分割余地があり、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象も残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec raw split and exact raw boundary

`stdlib/alloc/collections/vec/raw.nepl` は root からは分離済みだったが、direct `load` / `store`、fold/reduce、find、prefix scan/copy helper が 1 file に同居していた。今回は direct raw memory operation を element module に閉じ、raw root を public facade にした。

進捗:

- `stdlib/alloc/collections/vec/raw.nepl` は `raw/element`、`raw/aggregate`、`raw/predicate`、`raw/prefix` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/raw/element.nepl` を追加し、direct `load` / `store` を使う `vec_read_at` / `vec_write_at` だけを所有させた。
- `stdlib/alloc/collections/vec/raw/aggregate.nepl` を追加し、`vec_fold_impl` / `vec_reduce_impl` を所有させた。element access は `raw/element` 経由で行う。
- `stdlib/alloc/collections/vec/raw/predicate.nepl` を追加し、`vec_find_impl` を所有させた。direct raw intrinsic は持たない。
- `stdlib/alloc/collections/vec/raw/prefix.nepl` を追加し、`vec_take_while_len_impl` / `vec_write_prefix_impl` を所有させた。raw copy は `raw/element` の read/write helper に委譲する。
- `nepl-core/src/loader.rs` の exact raw-memory boundary を `alloc/collections/vec/raw.nepl` から `alloc/collections/vec/raw/element.nepl` へ移し、direct raw operation を持たない raw root / aggregate / predicate / prefix には capability を付けない設計にした。
- `nepl-core/tests/effects.rs` は raw root / aggregate / predicate / prefix に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、raw facade に implementation body が戻らないこと、各 helper の所有 module、raw-memory boundary capability が element module にだけ付くことを固定した。
- line count は `raw.nepl` 21、`raw/element.nepl` 74、`raw/aggregate.nepl` 98、`raw/predicate.nepl` 64、`raw/prefix.nepl` 103。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/raw.nepl -i stdlib/alloc/collections/vec/raw/element.nepl -i stdlib/alloc/collections/vec/raw/aggregate.nepl -i stdlib/alloc/collections/vec/raw/predicate.nepl -i stdlib/alloc/collections/vec/raw/prefix.nepl --no-tree -o tmp/vec-raw-module-split-focused-after-trunk.json -j 1 --dist web/dist`: total=7, passed=7
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/raw.nepl -i stdlib/alloc/collections/vec/raw/element.nepl -i stdlib/alloc/collections/vec/raw/aggregate.nepl -i stdlib/alloc/collections/vec/raw/predicate.nepl -i stdlib/alloc/collections/vec/raw/prefix.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-raw-module-split-suite.json -j 1 --dist web/dist`: total=16, passed=16
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: Vec raw 関連は passed。既知 open issue `ISS-20260507T112048241Z-RESOURCE-AGGREGATE-PROJECTION-MODULE-595EC35D` の `lower_aggregate_projection.rs has 204 lines; responsibility split limit is 180` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/raw` は facade 化できたが、Vec mutation/storage/access にはまだ分割余地があり、`core/mem.nepl`、selfhost compiler の巨大 file などの分割対象も残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec mutation split and exact raw boundary

`stdlib/alloc/collections/vec/mutation.nepl` は root からは分離済みだったが、grow/push、borrowed replace、owner pop、clear/free が 1 file に同居していた。今回は `mutation` 自体を public facade にし、direct allocation/reallocation を `mutation/push` に閉じた。

進捗:

- `stdlib/alloc/collections/vec/mutation.nepl` は `mutation/push`、`mutation/replace`、`mutation/pop`、`mutation/cleanup` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/mutation/push.nepl` を追加し、`push`、capacity grow、`alloc_ptr` / `realloc_ptr` / grow 失敗時の `dealloc_raw` を所有させた。
- `stdlib/alloc/collections/vec/mutation/replace.nepl` を追加し、borrowed `replace` の範囲検査と in-place element write を所有させた。direct raw intrinsic は持たず `vec/raw` helper に委譲する。
- `stdlib/alloc/collections/vec/mutation/pop.nepl` を追加し、owner-consuming `pop` と `VecPop` 返却契約を所有させた。direct raw intrinsic は持たない。
- `stdlib/alloc/collections/vec/mutation/cleanup.nepl` を追加し、`clear` / `free` を所有させた。free は `vec/storage` の storage cleanup helper に委譲する。
- `nepl-core/src/loader.rs` の exact raw-memory boundary を `alloc/collections/vec/mutation.nepl` から `alloc/collections/vec/mutation/push.nepl` へ移し、direct raw operation を持たない mutation root / replace / pop / cleanup には capability を付けない設計にした。
- `nepl-core/tests/effects.rs` は mutation root / replace / pop / cleanup に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、mutation facade に implementation body が戻らないこと、各 mutation helper の所有 module、raw-memory boundary capability が push module にだけ付くことを固定した。
- line count は `mutation.nepl` 20、`mutation/push.nepl` 91、`mutation/replace.nepl` 58、`mutation/pop.nepl` 63、`mutation/cleanup.nepl` 78。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/mutation.nepl -i stdlib/alloc/collections/vec/mutation/push.nepl -i stdlib/alloc/collections/vec/mutation/replace.nepl -i stdlib/alloc/collections/vec/mutation/pop.nepl -i stdlib/alloc/collections/vec/mutation/cleanup.nepl --no-tree -o tmp/vec-mutation-module-split-focused.json -j 1 --dist web/dist`: total=5, passed=5
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/mutation.nepl -i stdlib/alloc/collections/vec/mutation/push.nepl -i stdlib/alloc/collections/vec/mutation/replace.nepl -i stdlib/alloc/collections/vec/mutation/pop.nepl -i stdlib/alloc/collections/vec/mutation/cleanup.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-mutation-module-split-suite.json -j 1 --dist web/dist`: total=14, passed=14
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: Vec mutation 関連は passed。remote main の resource 分割後に残る別件として `initialized_summary_apply.rs has 151 lines; responsibility split limit is 130` warning は継続。
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/mutation` は facade 化できたが、Vec storage/access や selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec storage split and exact raw boundary

`stdlib/alloc/collections/vec/storage.nepl` は root からは分離済みだったが、empty storage view、allocation constructor、storage cleanup、filled constructor が 1 file に同居していた。今回は `storage` 自体を public facade にし、direct raw operation を持つ module と raw-free view module を分離した。

進捗:

- `stdlib/alloc/collections/vec/storage.nepl` は `storage/view`、`storage/alloc`、`storage/cleanup`、`storage/fill` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/storage/view.nepl` を追加し、raw-free の `vec_empty` / `vec_storage_mem_ptr` を所有させた。
- `stdlib/alloc/collections/vec/storage/alloc.nepl` を追加し、`vec_alloc_empty` / `new` / `with_capacity` と direct `alloc_ptr` を所有させた。
- `stdlib/alloc/collections/vec/storage/cleanup.nepl` を追加し、`vec_free_storage` と direct `dealloc_raw` を所有させた。
- `stdlib/alloc/collections/vec/storage/fill.nepl` を追加し、`filled` と direct `alloc_ptr` / `store` による cell initialization を所有させた。
- `nepl-core/src/loader.rs` の exact raw-memory boundary を `alloc/collections/vec/storage.nepl` から `storage/alloc.nepl` / `storage/cleanup.nepl` / `storage/fill.nepl` へ移し、storage root / view には capability を付けない設計にした。
- `nepl-core/tests/effects.rs` は storage root / view に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、storage facade に implementation body が戻らないこと、各 storage helper の所有 module、raw-memory boundary capability が direct raw operation module にだけ付くことを固定した。
- line count は `storage.nepl` 20、`storage/view.nepl` 26、`storage/alloc.nepl` 102、`storage/cleanup.nepl` 26、`storage/fill.nepl` 74。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/storage.nepl -i stdlib/alloc/collections/vec/storage/view.nepl -i stdlib/alloc/collections/vec/storage/alloc.nepl -i stdlib/alloc/collections/vec/storage/cleanup.nepl -i stdlib/alloc/collections/vec/storage/fill.nepl --no-tree -o tmp/vec-storage-module-split-focused.json -j 1 --dist web/dist`: total=3, passed=3
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/storage.nepl -i stdlib/alloc/collections/vec/storage/view.nepl -i stdlib/alloc/collections/vec/storage/alloc.nepl -i stdlib/alloc/collections/vec/storage/cleanup.nepl -i stdlib/alloc/collections/vec/storage/fill.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-storage-module-split-suite.json -j 1 --dist web/dist`: total=12, passed=12
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/storage` は facade 化できたが、Vec access や selfhost compiler の巨大 file などの分割対象は残る。

## 2026-05-07 追加対応メモ: alloc/collections/vec access split

`stdlib/alloc/collections/vec/access.nepl` は root からは分離済みだったが、header observer と data pointer view が 1 file に同居していた。今回は `access` 自体を public facade にし、read-only observer の責務を header/data view へ分離した。

進捗:

- `stdlib/alloc/collections/vec/access.nepl` は `access/header`、`access/data` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/alloc/collections/vec/access/header.nepl` を追加し、`len` / `cap` / `is_empty` を所有させた。
- `stdlib/alloc/collections/vec/access/data.nepl` を追加し、`data_ptr` / `data_mem_ptr` / `data_len` を所有させた。
- access 系 module は direct raw intrinsic を持たないため、loader raw-memory boundary は追加しない。`nepl-core/tests/effects.rs` は access root / header / data に raw wasm body を置いた場合に `effect.pure.calls_impure` になることを固定した。
- source policy を更新し、access facade に implementation body が戻らないこと、各 access helper の所有 module、access 系 module に raw-memory boundary capability が付かないことを固定した。
- line count は `access.nepl` 16、`access/header.nepl` 100、`access/data.nepl` 114。

検証:

- `node nodesrc/test_stdlib_vec_no_unsafe_unwraps.js`: passed
- `node nodesrc/test_stdlib_vec_borrowed_observers.js`: passed
- `cargo fmt --check -p nepl-core`: passed
- `cargo test -p nepl-core --test effects loader_marks_configured_stdlib_implementation_boundaries_as_raw_memory_boundary -- loader_does_not_mark_raw_memory_free_split_modules_as_raw_memory_boundaries`: passed
- `trunk build --release`: passed
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/access.nepl -i stdlib/alloc/collections/vec/access/header.nepl -i stdlib/alloc/collections/vec/access/data.nepl --no-tree -o tmp/vec-access-module-split-focused.json -j 1 --dist web/dist`: total=6, passed=6
- `node nodesrc/tests.js -i stdlib/alloc/collections/vec/access.nepl -i stdlib/alloc/collections/vec/access/header.nepl -i stdlib/alloc/collections/vec/access/data.nepl -i stdlib/tests/vec.n.md -i tests/stdlib/vec_collections.n.md --no-tree -o tmp/vec-access-module-split-suite.json -j 1 --dist web/dist`: total=15, passed=15
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/issues.js check`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`alloc/collections/vec/access` は facade 化できたが、selfhost compiler の巨大 file など他の分割対象は残る。

## 2026-05-07 追加対応メモ: nm/parser document split

`stdlib/nm/parser.nepl` は scanner / JSON inline / JSON section を分離した後も、`Document` 型、`parse_markdown`、`document_to_json`、block-level JSON serializer を root facade に保持していた。今回は `nm/parser` root を public facade にし、document source view と document-level JSON serialization を `stdlib/nm/parser/document.nepl` へ分離した。

進捗:

- `stdlib/nm/parser.nepl` は `parser/document` の public `@merge` re-export と facade doc/doctest だけを持つ構成にした。
- `stdlib/nm/parser/document.nepl` を追加し、`Document`、`parse_markdown`、`document_to_json`、block scanner loop、`NmJsonSectionState` 利用箇所を所有させた。
- `parser/document` は `parser/scanner`、`parser/json_inline`、`parser/json_section`、`nm/json_escape` を直接 import し、root facade が scanner / serializer の実装依存を抱えない形にした。
- `nodesrc/test_stdlib_nm_parser_document_boundary.js` を追加し、root facade に `Document` / `document_to_json` / `StringBuilder` / serializer loop が戻らないこと、document module が必要な責務境界を持つことを固定した。
- 既存の nm parser / JSON escape / byte scanner / raw aggregate source policy を、`Document` 実装が `parser/document` にある設計へ更新した。
- line count は `parser.nepl` 28、`parser/document.nepl` 221。

検証:

- `node nodesrc/test_stdlib_nm_parser_document_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_doc_no_boilerplate.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_block_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_no_inline_unwraps.js`: passed
- `node nodesrc/test_stdlib_nm_parser_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_json_inline_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_parser_json_section_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_json_escape_boundary.js`: passed
- `node nodesrc/test_stdlib_nm_no_raw_aggregate_detours.js`: passed
- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/nm/parser.nepl -i stdlib/nm/parser/document.nepl -i stdlib/nm/html_gen.nepl -i tests/stdlib/nm.n.md --no-tree -o tmp/nm-parser-document-split-suite.json -j 1 --dist web/dist`: total=11, passed=11
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check HEAD`: passed

この issue はまだ open のまま扱う。`nm/parser` root は facade 化できたが、`parser/scanner.nepl`、`parser/json_section.nepl`、`std/streamio/scanner/number.nepl` など残る中規模 file の責務境界は継続確認する。

## 2026-05-07 追加対応メモ: std/streamio scanner number int/float split

`stdlib/std/streamio/scanner/number.nepl` は root scanner から分離済みだったが、整数 parser と浮動小数点 parser を 1 file に同居させていた。今回は `number` 自体を public facade にし、整数 parser を `number/int.nepl`、浮動小数点 parser を `number/float.nepl` へ分けた。

進捗:

- `stdlib/std/streamio/scanner/number.nepl` は `number/int` と `number/float` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/std/streamio/scanner/number/int.nepl` は `scan_i32_impl` / `scan_u32_impl` / `scan_u64_impl` / `scan_i64_impl` を所有する。
- `stdlib/std/streamio/scanner/number/float.nepl` は `scan_f64_impl` / `scan_f32_impl` を所有する。
- どちらの実装も raw buffer access を直接行わず、`stream_scanner_byte_at` と scanner state boundary 経由の設計を維持した。
- `nodesrc/test_stdlib_streamio_scanner_boundary.js` と `nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js` を更新し、number facade に parser implementation body が戻らないこと、int/float module の責務と line count を固定した。
- line count は `number.nepl` 13、`number/int.nepl` 205、`number/float.nepl` 173。

検証:

- `node nodesrc/test_stdlib_streamio_scanner_boundary.js`: passed
- `node nodesrc/test_stdlib_streamio_no_unsafe_unwraps.js`: passed
- `node nodesrc/tests.js -i stdlib/std/streamio/scanner/number.nepl -i stdlib/std/streamio/scanner/number/int.nepl -i stdlib/std/streamio/scanner/number/float.nepl -i stdlib/std/streamio/scanner.nepl -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-scanner-number-split-suite.json -j 1 --dist web/dist`: total=15, passed=15
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check HEAD`: passed

この issue はまだ open のまま扱う。`std/streamio/scanner/number` は facade 化できたが、`std/streamio/scanner/state.nepl`、`std/stdio/debug.nepl`、`std/stdio/ansi.nepl` など残る中規模 file の責務境界は継続確認する。

## 2026-05-07 追加対応メモ: std/stdio debug profile split

`stdlib/std/stdio/debug.nepl` は `profile=debug` の出力実装と `profile=release` の no-op 実装を 1 file に同居させていた。今回は `std/stdio/debug` 自体を public facade にし、profile 別の実装責務を `debug/enabled.nepl` と `debug/disabled.nepl` へ分けた。

進捗:

- `stdlib/std/stdio/debug.nepl` は `debug/enabled` と `debug/disabled` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/std/stdio/debug/enabled.nepl` は `profile=debug` の `debug` / `debug_color` / `debugln` / `debugln_color` を所有し、出力は従来どおり `std/stdio/print` と typed `AnsiColor` API へ委譲する。
- `stdlib/std/stdio/debug/disabled.nepl` は `profile=release` の同 API を no-op として所有する。
- `nodesrc/test_stdlib_stdio_debug_boundary.js` を更新し、root facade と debug facade に function body が戻らないこと、enabled/disabled module の profile 別責務と line count を固定した。
- line count は `debug.nepl` 13、`debug/enabled.nepl` 165、`debug/disabled.nepl` 178。

検証:

- `node nodesrc/test_stdlib_stdio_debug_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib/std/stdio/debug.nepl -i stdlib/std/stdio/debug/enabled.nepl -i stdlib/std/stdio/debug/disabled.nepl -i stdlib/std/stdio.nepl -i tests/stdlib/stdout.n.md --no-tree -o tmp/stdio-debug-profile-split-suite.json -j 1 --dist web/dist`: total=16, passed=16
- `cargo check -p nepl-core --tests`: passed
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check HEAD`: passed

この issue はまだ open のまま扱う。`std/stdio/debug` は facade 化できたが、`std/stdio/read.nepl`、`std/stdio/ansi.nepl`、`std/text.nepl` など残る中規模 file の責務境界は継続確認する。

## 2026-05-08 Agent 2 追加対応メモ: std/stdio ansi types/code/print split

`stdlib/std/stdio/ansi.nepl` は typed enum 設計自体は妥当だったが、型定義、style constructor、escape code 生成、stdout 出力 helper が 1 file に同居していた。今回は public import path を維持したまま、`ansi` root を facade 化し、ANSI の責務を `types` / `code` / `print` へ分離した。

進捗:

- `stdlib/std/stdio/ansi.nepl` は `ansi/types`、`ansi/code`、`ansi/print` の public `@merge` re-export だけを持つ facade にした。
- `stdlib/std/stdio/ansi/types.nepl` は `AnsiColor` / `AnsiTextWeight` / `AnsiTextDecoration` / `AnsiTextStyle` と、`ansi_color_style` など style constructor を所有する。
- `stdlib/std/stdio/ansi/code.nepl` は `ansi_color_code` / `ansi_background_color_code` / `ansi_text_style_code` など、typed enum/struct から escape code 文字列への変換を所有する。`concat` 依存は `alloc/string/concat` から明示 import するようにした。
- `stdlib/std/stdio/ansi/print.nepl` は `print_style_start` / `print_style_reset` / `print_style` / `println_style` / `print_color` / `println_color` を所有し、`std/stdio/print` への委譲だけを担当する。
- `nodesrc/test_stdlib_stdio_ansi_boundary.js` を更新し、`std/stdio/ansi` root facade に enum / struct / code 変換 / print helper の実装が戻らないこと、各 helper が責務別 module に存在すること、enum 変換が wildcard なしの match であることを固定した。
- line count は `ansi.nepl` 17、`ansi/types.nepl` 158、`ansi/code.nepl` 123、`ansi/print.nepl` 87。

検証:

- `node nodesrc/test_stdlib_stdio_ansi_boundary.js`: passed
- `trunk build`: passed
- `node nodesrc/tests.js -i stdlib/std/stdio/ansi.nepl -i stdlib/std/stdio/ansi/types.nepl -i stdlib/std/stdio/ansi/code.nepl -i stdlib/std/stdio/ansi/print.nepl -i tests/stdlib/stdout.n.md -i tests/stdlib/features_tui.n.md --no-tree -o tmp/stdio-ansi-split-focused-rebased.json -j 1 --dist web/dist`: total=13, passed=13
- `node nodesrc/tests.js -i examples -o tmp/stdio-ansi-split-examples-rebased.json -j 4 --dist web/dist`: total=32, passed=32
- `node nodesrc/run_source_policy_regressions.js --warn-only`: passed
- `node nodesrc/issues.js check`: passed
- `git diff --check`: passed

この issue はまだ open のまま扱う。`std/stdio/ansi` は facade 化できたが、`std/stdio/read.nepl`、`std/text.nepl`、selfhost compiler の巨大 file など残る分割対象は継続する。

## 旧レビュー本文

- 解決済: false
- 状態: open
- 優先度: P2
- 種別: architecture
- 対象: `stdlib/core/math.nepl`, `stdlib/alloc/string.nepl`, `stdlib/std/stdio.nepl`, `stdlib/alloc/collections/vec.nepl`

### 根拠

- `stdlib/core/math.nepl`: 4435 行。
- `stdlib/alloc/string.nepl`: 2134 行。
- `stdlib/std/streamio.nepl`: 1525 行。
- `stdlib/alloc/collections/vec.nepl`: 1488 行。
- `stdlib/std/stdio.nepl`: 1403 行。

### 問題

stdlib の主要 file が肥大化しています。API、内部 helper、target-specific raw body、doctest が同居しており、修正範囲の把握が難しくなっています。

### 影響

コンパイル性能にも影響します。loader が import file を丸ごと clone / merge する現状では、巨大 stdlib file はそのまま型検査候補数と clone 量を増やします。

### 修正方針

`math/i32`, `math/i64`, `math/f32`, `math/f64`、`string/parse`, `string/format`, `stdio/read`, `stdio/write` のように用途別 module へ分割します。public facade は既存 path 互換を保ちます。

### 検証

分割前後で public import path の doctest が同じ結果になることを確認します。
