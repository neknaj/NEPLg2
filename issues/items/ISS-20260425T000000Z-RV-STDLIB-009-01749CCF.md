---
id: ISS-20260425T000000Z-RV-STDLIB-009-01749CCF
title: "巨大 stdlib ファイルが分割されていない"
area: stdlib
status: open
resolved: false
priority: P2
type: architecture
created: 2026-04-25
updated: 2026-04-29
target: "stdlib/core/math.nepl, stdlib/alloc/string.nepl, stdlib/alloc/collections/vec.nepl, stdlib/std/streamio.nepl, stdlib/std/stdio.nepl, stdlib/std/fs.nepl, stdlib/nm/parser.nepl, stdlib/core/mem.nepl, stdlib/neplg2/cli/args.nepl"
legacy_id: RV-STDLIB-009
source: "doc/review20260425/stdlib.md#rv-stdlib-009"
---

# RV-STDLIB-009: 巨大 stdlib ファイルが分割されていない

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-STDLIB-009-01749CCF`。

## 要約

stdlib の主要 file が肥大化しています。API、内部 helper、target-specific raw body、doctest が同居しており、修正範囲の把握が難しくなっています。

## 影響

コンパイル性能にも影響します。loader が import file を丸ごと clone / merge する現状では、巨大 stdlib file はそのまま型検査候補数と clone 量を増やします。

## 修正方針

`math/i32`, `math/i64`, `math/f32`, `math/f64`、`string/parse`, `string/format`, `stdio/read`, `stdio/write` のように用途別 module へ分割します。public facade は既存 path 互換を保ちます。

## 検証

分割前後で public import path の doctest が同じ結果になることを確認します。

## 2026-04-28 再レビュー

remote main 取り込み後の stdlib line count 上位は次の通りで、分割 issue はまだ open のまま妥当である。

- `stdlib/core/math.nepl`: 4435 lines
- `stdlib/alloc/string.nepl`: 2479 lines
- `stdlib/alloc/collections/vec.nepl`: 1701 lines
- `stdlib/std/streamio.nepl`: 1649 lines
- `stdlib/std/stdio.nepl`: 1636 lines
- `stdlib/std/fs.nepl`: 1566 lines
- `stdlib/nm/parser.nepl`: 1358 lines
- `stdlib/core/mem.nepl`: 1115 lines
- `stdlib/alloc/collections/vec/sort.nepl`: 1049 lines
- `stdlib/neplg2/cli/args.nepl`: 661 lines

`string.nepl` は再実装で安全性が上がったが、UTF-8、builder、numeric parse/format、split、float conversion が同居している。self-host では text/token/diagnostic から広く使われるため、機能単位で `alloc/string/*` へ分ける必要がある。

`stdlib/neplg2/cli/args.nepl` は 661 lines で既に parser state、enum、doctest、classifier が同居しており、S6 の driver/reporter 実装前に `args/types`, `args/parse`, `args/tests` 相当へ分ける候補として追跡する。

## 2026-04-29 対応メモ

`alloc/string.nepl` の byte scanner helper 群を `stdlib/alloc/string/scanner.nepl` へ分離した。後方互換のための wrapper facade は残さず、byte scanner を使う module が `alloc/string/scanner` を明示 import する設計にした。

この分割により、parser / scanner 向けの ASCII byte 分類、byte range find、line end / next line、inline space scan の loop 実装は `alloc/string/scanner` が所有する責務になった。`alloc/string.nepl` は UTF-8 slice / builder / numeric conversion など文字列本体の責務に寄せ、scanner helper は `scanner::str_find_byte_range` のように呼び出す。既存 facade を試した際に、同名 public API から `scanner::str_*` を呼ぶと resolver が D3001 にする問題も確認したため、compiler 側の問題は `ISS-20260429T041244376Z-FACADE-MODULE-CANNOT-DISAMBIGUATE-SA-7F898FA5` として分離した。

`stdlib/neplg2/core/module/import_spec.nepl`、`stdlib/nm/parser.nepl`、`stdlib/nm/html_gen.nepl` は scanner module を直接 import するように更新した。`nodesrc/test_stdlib_byte_scanner_helpers_boundary.js` も更新し、scanner 実装本体が `alloc/string/scanner.nepl` にあり、`alloc/string.nepl` に scanner facade が戻らないことを固定した。

この issue はまだ open のまま扱う。`alloc/string` には UTF-8 slice、StringBuilder、numeric parse/format、split が残っており、`core/math`、`std/streamio`、`std/fs`、`stdio`、`nm/parser` など他の巨大 file も未分割である。次の分割では `alloc/string/utf8` または numeric parse/format を候補にする。

### 2026-04-29 検証

- `node nodesrc/test_stdlib_byte_scanner_helpers_boundary.js`: passed
- `node nodesrc/tests.js -i stdlib\alloc\string\scanner.nepl --no-tree -o tmp\string-scanner-module-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib\alloc\string.nepl --no-tree -o tmp\string-scanner-module-string.json -j 1`: total=8 passed=8
- `node nodesrc/tests.js -i tests\stdlib\neplg2_import_spec.n.md --no-tree -o tmp\string-scanner-module-import-spec.json -j 1`: total=3 passed=3
- `node nodesrc/tests.js -i tests\stdlib\nm.n.md --no-tree -o tmp\string-scanner-module-nm.json -j 1`: total=5 passed=5

## 2026-04-29 追加対応メモ: self-host CLI args classifier split

`stdlib/neplg2/cli/args.nepl` から argv token / option value の分類責務を `stdlib/neplg2/cli/args/classify.nepl` へ分離した。`SelfhostCliArgKind`、hash dispatch 用 key、`--target` / `--emit` / `--profile` value parser は classifier module が所有し、`args.nepl` は parser state machine、emit set の comma parser、compile option 変換に集中する。

この分割により、public option 型は `args/types`、parser 固有分類は `args/classify`、argv parser 本体は `args.nepl` という責務境界になった。`args.nepl` へ classifier が戻る回帰は `nodesrc/test_selfhost_cli_args_types_split.js` で検査し、hash dispatch の `match` 回帰検査は `nodesrc/test_stdlib_match_decision_trees.js` が `args/classify.nepl` を対象に見る。

この issue はまだ open のまま扱う。`core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` などの巨大 file は未分割の責務を残している。

### 2026-04-29 追加検証

- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/classify.nepl --no-tree -o tmp/cli-args-classify-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-classify-facade.json -j 1`: total=5 passed=5
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-classify-parser.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-classify-driver.json -j 1`: total=3 passed=3

## 2026-04-29 追加対応メモ: self-host CLI args emit split

`stdlib/neplg2/cli/args.nepl` に残っていた emit artifact set 操作、`--emit` の comma 区切り parser、emit 判定 helper を `stdlib/neplg2/cli/args/emit.nepl` へ分離した。`args.nepl` は `args/emit` を `pub #import` で再 export し、既存の `neplg2/cli/args` facade から parser tests / driver tests が同じ helper を参照できる構成を保つ。

この分割により、CLI option 型は `args/types`、token/value 分類は `args/classify`、emit artifact set の合成は `args/emit`、argv parser state machine は `args.nepl` という責務境界になった。`args.nepl` の行数は 471 行まで下がり、`args/emit.nepl` は 158 行の専用 module として独立した。

この issue はまだ open のまま扱う。`args.nepl` には parser loop と compile option 変換が残るが、より大きい未分割対象として `core/math`、`alloc/string`、`alloc/collections/vec`、`std/streamio`、`std/stdio`、`std/fs`、`nm/parser` が残っている。

### 2026-04-29 emit split 検証

- `node nodesrc/test_selfhost_cli_args_types_split.js`: passed
- `node nodesrc/test_stdlib_match_decision_trees.js`: passed
- `node nodesrc/test_selfhost_cli_args_no_owner_field_reads.js`: passed
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args/emit.nepl --no-tree -o tmp/cli-args-emit-direct.json -j 1`: total=1 passed=1
- `node nodesrc/tests.js -i stdlib/neplg2/cli/args.nepl --no-tree -o tmp/cli-args-emit-facade.json -j 1`: total=5 passed=5
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cliarg_parser.n.md --no-tree -o tmp/cli-args-emit-parser.json -j 1`: total=10 passed=10
- `node nodesrc/tests.js -i tests/stdlib/selfhost_cli_driver.n.md --no-tree -o tmp/cli-args-emit-driver.json -j 1`: total=3 passed=3

## 旧レビュー本文

- 解決済: false
- 状態: open
- 優先度: P2
- 種別: architecture
- 対象: `stdlib/core/math.nepl`, `stdlib/alloc/string.nepl`, `stdlib/std/stdio.nepl`, `stdlib/alloc/collections/vec.nepl`

### 根拠

- `stdlib/core/math.nepl`: 4435 行。
- `stdlib/alloc/string.nepl`: 2134 行。
- `stdlib/std/streamio.nepl`: 1525 行。
- `stdlib/alloc/collections/vec.nepl`: 1488 行。
- `stdlib/std/stdio.nepl`: 1403 行。

### 問題

stdlib の主要 file が肥大化しています。API、内部 helper、target-specific raw body、doctest が同居しており、修正範囲の把握が難しくなっています。

### 影響

コンパイル性能にも影響します。loader が import file を丸ごと clone / merge する現状では、巨大 stdlib file はそのまま型検査候補数と clone 量を増やします。

### 修正方針

`math/i32`, `math/i64`, `math/f32`, `math/f64`、`string/parse`, `string/format`, `stdio/read`, `stdio/write` のように用途別 module へ分割します。public facade は既存 path 互換を保ちます。

### 検証

分割前後で public import path の doctest が同じ結果になることを確認します。
