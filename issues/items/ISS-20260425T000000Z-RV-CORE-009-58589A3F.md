---
id: ISS-20260425T000000Z-RV-CORE-009-58589A3F
title: "move/borrow/drop が Resource IR なしで後付け実装されている"
area: core
status: open
resolved: false
priority: P1
type: architecture
created: 2026-04-25
updated: 2026-05-06
target: "nepl-core/src/resource, nepl-core/src/passes/drop_insertion.rs, nepl-core/src/compiler.rs"
legacy_id: RV-CORE-009
source: "doc/review20260425/core.md#rv-core-009"
---

# RV-CORE-009: move/borrow/drop が Resource IR なしで後付け実装されている

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-CORE-009-58589A3F`。

## 要約

Resource IR がなく、HIR 木を直接走査して所有権状態を推測しています。borrow の lifetime release、field move、auto drop と user drop の整合、branch merge が局所状態で処理されており、仕様化しづらいです。

## 影響

false positive / false negative の両方が起きやすく、stdlib の owning collection と組み合わせたときに double free や use-after-move を防ぎきれません。

## 修正方針

`doc/neplg3/impl/compiler_structure.md` の方針どおり HIR 後に Resource IR を導入し、move、borrow、region、drop elaboration を別 pass に分けます。drop 挿入前に ownership を確定し、auto drop は Resource IR 上で挿入します。

## 検証

branch/loop/borrow/field move/drop trait の compile_fail と should_panic を整理し、Resource IR dump の snapshot test を追加します。

## 関連ドキュメント

- [NEPLg2 静的検査の複雑化解消計画](../../doc/neplg2/static_check_complexity_reduction_plan.md)
- [NEPLg2 compiler diagnostic redesign plan](../../doc/neplg2/compiler_diagnostics_redesign_plan.md)

## 2026-05-06 Resource IR pre-drop authority gate 追記

Stage 4 の残件であった「生成 drop が source resource violation を隠す」順序問題に対し、`prepare_module_for_codegen_with_source_map` の Resource IR gate を HIR `passes::insert_drops` より前へ移した。

ただし、Resource IR を typecheck 直後の未単相化 HIR 全体へ直接掛けると `#target std` の未使用 stdlib source まで検査対象になり、`tests/compiler/move_effect.n.md::doctest#108` が 60 秒 timeout した。そのため、Resource IR gate の入力は「drop 未挿入の source semantics を保持したまま monomorphize された reachable HIR」とする。これにより、検査対象は実際に到達する単相化済み関数に限定され、生成 drop 呼び出しは Resource IR の source violation 判定へ混入しない。

deep prefix chain では HIR の再帰 `clone()` 自体が native stack overflow を起こすため、Resource IR 用 HIR と codegen 用 HIR は clone ではなく typecheck を二度実行して分離する。これは HIR drop insertion が残っている間の安全な分離であり、最終設計では HIR `passes::insert_drops` を Resource IR drop elaboration へ置き換え、この二重経路を統合する。

回帰防止として `nodesrc/test_resource_gate_order.js` は次を監視する。

- Resource IR gate が drop 未挿入 source HIR を monomorphize してから実行されること。
- Resource IR gate が HIR drop insertion より前に実行されること。
- Resource IR 用に深い HIR を再帰 clone しないこと。

確認済みの focused regression:

- `cargo test -p nepl-core --test check_pipeline prepare_codegen_accepts_deep_prefix_chain_without_stack_overflow -- --nocapture`
- `cargo test -p nepl-core --test check_pipeline resource_static_check_accepts_deep_prefix_chain_without_stack_overflow -- --nocapture`
- `cargo test -p nepl-core --test drop -- --nocapture`
- `cargo test -p nepl-core --test resource_ir resource_ir_compiler_rejects -- --nocapture`
- `node nodesrc/tests.js -i tests/compiler/move_effect.n.md --runner wasm --no-tree -j 1`
- `node nodesrc/tests.js -i tests/compiler/drop_overwrite.n.md --runner wasm --no-tree -j 1`

## 2026-05-06 Resource IR EndScope auto-drop 追記

Stage 4 の drop elaboration 移行として、Resource IR initialized/cell checker が `ResourceOp::EndScope` で live non-Copy local を auto-drop state transition として扱うようにした。これにより、source Resource IR check は HIR `passes::insert_drops` の生成済み `drop` 式に依存せず、scope exit の drop obligation を Resource IR 上で表現できる。

この実装では、live initialized non-Copy local だけを `CellState::Dropped` に遷移させる。move 済み、drop 済み、未初期化、Copy local は auto-drop 対象にしない。auto-drop 時には raw alias、function alias、pending realloc、variant raw-cell initialization の補助状態も同時に scope local から削除する。

同時に、同名・同型 shadowing で inner scope の auto-drop が outer local を壊す問題を避けるため、Resource IR lowering は有効範囲内で同名 local が再宣言された場合に Resource IR 上の local place を固有化する。通常の非 shadowing local は従来通りの名前を維持し、shadowed local だけ `x#N` 形式の内部 place へ分ける。

残る未完了点は、codegen 側の HIR `passes::insert_drops` を Resource IR drop elaboration の結果から生成する構造へ置き換えることである。

## 2026-05-06 Resource IR drop plan 追記

EndScope auto-drop を checker 内部の暗黙処理に閉じないため、`ResourceDropPlan` / `ResourceDropFunctionPlan` / `ResourceAutoDrop` / `ResourceAutoDropKind` を追加した。`compute_resource_drop_plan` は Resource IR の nested `Branch` / `Loop` / `Match` を含む `EndScope` を走査し、non-Copy scope local の auto-drop 候補を明示データとして列挙する。

initialized/cell checker は `auto_drop_candidates_for_end_scope` を通じて同じ候補列挙を使う。これにより、今後 codegen 側が HIR `passes::insert_drops` を置き換えるときにも、checker と codegen で別々の drop 対象推定を持たずに済む。

## 2026-04-29 diagnostic 再設計追記

Resource IR の authoritative gate が増えた結果、`D3100` / `D3101` / `D3025` だけでは cell state、owner obligation、borrow conflict、raw identity escape、lowering completeness の意味差を十分に保持できないことを確認した。

この issue の Stage 4/5 実装では、検査を弱めずに compiler error 化を進める一方で、診断体系は `ISS-20260429T040748194Z-RUST-COMPILER-DIAGNOSTICS-ARE-NOT-AL-1617747D` と同期する。短期的には legacy `diag_id` を維持し、長期的には Resource IR diagnostic kind から stable string code を経由して legacy ID を補う形へ移行する。

## 2026-04-28 issue 整理

この issue は静的検査大規模修正の Stage 2-4、つまり Resource IR 型定義、HIR lowering、Resource IR 上の move/borrow/lifetime/drop obligation 検査を追跡する親 issue とする。

現行の raw alias summary、enum payload alias、aggregate field alias、function value raw effect summary は、Resource IR 導入までの暫定防壁として扱う。今後は同種の補助 summary を増やし続けるのではなく、`ResourceId`、`Place`、`StorageId`、`CellState`、`BorrowState`、`PointerProvenance` へ統合する。compiler 側の module 分割は `ISS-20260425T000000Z-RV-CORE-002-D17C4B3C`、stdlib raw API 移行は `ISS-20260427T204839136Z-STDLIB-RAW-MEMORY-BACKED-APIS-REQUIR-E503CD84` へ分ける。

## 2026-04-28 Stage 2 Resource IR skeleton 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 2 として、`nepl-core/src/resource/` に Resource IR の最小型、HIR lowering skeleton、deterministic dump を追加した。

今回の目的は enforcement ではなく可視化である。`ResourceModule`、`ResourceFunction`、`ResourceBlock`、`ResourceOp`、`Place`、`ResourceState` に加えて、今後の Stage 3-4 で必要になる `ResourceId`、`StorageId`、`CellState`、`OwnerState`、`BorrowState`、`PointerProvenance`、`EffectOp` を compiler-owned model として分けた。現行 `move_check` / `drop_insertion` にはまだ接続していないため、既存検査の意味論は変更していない。

HIR lowering skeleton は local read、let/set、borrow、drop、call effect、raw memory intrinsic を粗く Resource op として記録する。ただし Copy / move 判定、field offset、branch state merge、drop obligation の enforcement は Stage 3-4 へ残す。Stage 2 の dump snapshot test では、local declaration と read が安定した text dump に現れることを固定した。

次の作業は Stage 3 の local / aggregate / branch lowering である。ここでは HIR 直走査の raw alias summary を増やすのではなく、`Place` projection と `EffectOp` へ移す方向で進める。

## 2026-04-28 Stage 3 local / aggregate / branch lowering 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 3 commit 単位 1 として、Resource IR lowering が HIR の local / aggregate / branch 構造を失わないようにした。

今回の変更では、`ResourceOp::Expr` に出力 `Place` を持たせ、local read の `Read`、`DeclareLocal` の initializer、`Assign` の value、`Borrow` / `Move` の output を明示した。さらに tuple / struct / enum construction を `Construct`、`if` を nested `Branch`、`while` を `Loop`、`match` を `Match` として下げ、dump でも nested op を構造付きで確認できるようにした。

local lowering では HIR の `let` / `set` が式としては unit を返す仕様を守り、local place と式出力 temp を分離した。あわせて lowering context に local 型スコープを追加し、`drop x` の HIR 式型 unit を drop 対象型として誤用しないようにした。branch / loop / match arm の lowering では local scope を復元し、片側の branch や arm だけで宣言された local が外側の Resource IR lowering に漏れないようにしている。

この段階でも Resource IR はまだ enforcement へ接続していない。次の Stage 3 作業は raw memory operation lowering、function call / callback effect lowering、旧 checker との comparison diagnostics である。

## 2026-04-28 Stage 3 function call / callback effect lowering 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 3 commit 単位 3 として、function call と callback 境界を Resource IR で第一級に残すようにした。

今回の変更では `ResourceOp::Call`、`ResourceOp::IndirectCall`、`ResourceOp::FunctionValue`、`ResourceCallTarget` を追加した。direct call は target、argument places、output、effect を保持し、indirect call は callee place、parameter types、result type、argument places、output、unknown effect を保持する。`@fn` の function value も name と effect を持つ op として dump に出るため、Stage 4 で callback 候補と resource effect を dataflow で追える入力ができた。

あわせて Resource IR lowering に module-level effect map を追加し、`FuncRef::User` の effect を常に `Pure` として潰していた skeleton の問題を修正した。同名 overload / extern が混在する場合は安全側に `Impure` を優先する。これにより、direct call と function value の dump は現行 HIR 関数定義の surface effect を保持する。

この段階でも indirect call の concrete callee 解決や raw memory effect summary の適用はまだ旧 `move_check` 側に残っている。次の Stage 3 作業は old checker との comparison diagnostics であり、Resource IR lowering と既存 summary の差分を検出できるようにする。

## 2026-04-28 Stage 3 lowering coverage comparison 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 3 commit 単位 4 として、HIR と Resource IR lowering の coverage comparison diagnostic を追加した。

今回追加した `compare_hir_resource_lowering` は、HIR 側と Resource IR 側で direct call、indirect call、function value、raw memory operation の件数を関数単位で比較する。これは compiler 本体の診断挙動をまだ変えない debug 境界であり、Resource IR lowering が HIR の静的検査入力を落とした場合に test で検出できるようにする目的である。

既存 `move_check` の raw alias / function summary はまだ旧 HIR 走査側に残っているが、Stage 4 ではこの coverage 境界を足場として、`RawMemoryOp`、`Call`、`IndirectCall`、`FunctionValue` を `CellState` / `BorrowState` / `PointerProvenance` の検査へ接続する。coverage comparison は enforcement ではなく、移行中に lowering 欠落を隠さないための safety net として扱う。

## 2026-04-28 Stage 4 initialized / moved state 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 4 commit 単位 1 として、Resource IR 上で `CellState` による initialized / moved / dropped の検査を開始した。

今回追加した `check_resource_initialized_moves` は、関数 parameter を initialized cell として開始し、`DeclareLocal`、`Read`、`Assign`、`Move`、`Drop`、`Call`、`IndirectCall`、`RawMemory`、`Construct`、`Branch`、`Loop`、`Match` の Resource op を `CellState` の状態遷移として評価する。`TypeCtx::is_copy` を使い、Copy 値の read は元 place を維持し、non-Copy 値の by-value use は元 place を `Moved` にする。未初期化 cell、move 済み cell、drop 済み cell を使った場合は `ResourceCheckDiagnostic::CellUnavailable` として関数名、operation、place、state、span を保持する。

現時点では compiler pipeline の診断挙動をまだ切り替えていない。旧 `move_check` を弱めず、Resource IR 検査の結果を unit test で固定しながら移行するためである。また branch / loop / match の merge はまだ enforcement せず、`ResourceCheckDeferred` に deferred count として表面化した。これは Stage 4 commit 単位 4 の branch / loop merge で、現在の deferred 境界を実際の state merge に置き換える。

回帰テストでは、Copy 値の複数 read が通ること、non-Copy 値の二重 read が `Moved` として検出されること、drop 後 read が `Dropped` として検出されること、initializer なし local の read が `Uninit` として検出されることを `nepl-core/tests/resource_ir.rs` に固定した。

## 2026-04-28 Stage 4 borrow / lifetime 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 4 commit 単位 3 として、Resource IR 上で `BorrowState` による shared / unique borrow の競合検査を開始した。

今回追加した `check_resource_borrow_lifetimes` は、`ResourceOp::Borrow` の output を borrow token として source place に紐づける。shared borrow は shared count を増やし、unique borrow は active shared / unique borrow がない場合だけ許可する。active unique borrow 中の source read、active borrow 中の source assign / move / drop、shared borrow 中の unique borrow は `ResourceBorrowDiagnostic::BorrowConflict` として報告する。

borrow token の `Drop` は release として扱い、shared borrow count を減らす。`DeclareLocal` / `Read` / `Assign` / `Move` では borrow token の transfer を行う。ただし branch / loop / match の merge、関数境界での borrow summary、戻り値として escape する borrow の lifetime 検査はまだ後続 Stage に残す。現時点では compiler pipeline に接続せず、旧 `move_check` の防壁を維持しながら Resource IR 側の検査を regression で固定する。

回帰テストでは、shared borrow 中の read が通ること、unique borrow 中の source read が拒否されること、shared borrow 中の unique borrow が拒否されること、shared borrow release 後に unique borrow が通ることを `nepl-core/tests/resource_ir.rs` に固定した。

## 2026-04-28 Stage 4 borrow return escape 追記

`ISS-20260428T110657405Z-RESOURCE-BORROW-CHECKER-ALLOWS-BORRO-EC3BEA97` として、Resource IR borrow checker が `ResourceTerminator::Return` を見ず、active borrow token を戻り値として外へ逃がせる問題を分離した。

`ResourceBorrowCheckEngine` は return value が active borrow token binding を持つ場合に `ResourceBorrowOperation::ReturnValue` の `BorrowConflict` を出すようになった。これにより、borrow token の release 前に function boundary を越える lifetime escape を Resource IR shadow check で検出できる。

## 2026-04-28 Stage 4 borrow token return summary 追記

`ISS-20260428T111052658Z-RESOURCE-BORROW-CHECKER-LOSES-BORROW-F9979F31` として、borrow token が helper の戻り値として返ると `BorrowTable` binding が call output へ伝播せず、return escape 検査を迂回できる問題を分離した。

Resource IR borrow checker は direct user function の parameter-to-return token summary を計算し、`ResourceOp::Call` で active token binding を call output へ伝播するようになった。これにより、`borrow_id(token)` のような helper 経由でも active borrow token の return escape を検出できる。

## 2026-04-28 Stage 4 borrow token function value summary 追記

`ISS-20260428T111459572Z-RESOURCE-BORROW-CHECKER-LOSES-BORROW-E16E4C63` として、known function value 経由の `IndirectCall` で borrow token return summary が適用されず、active token binding が落ちる問題を分離した。

Resource IR borrow checker に function value alias table を追加し、`FunctionValue` / local copy / branch merge を通じて callee alias を保持するようにした。`IndirectCall` の callee が known alias を持つ場合は direct call と同じ borrow token return summary を適用するため、first-class function 経由でも active borrow token の return escape を検出できる。

## 2026-04-28 Stage 4 unknown callback borrow token summary 追記

`ISS-20260428T112207828Z-RESOURCE-BORROW-CHECKER-LOSES-BORROW-A173C06B` として、callee が known function value alias を持たない `IndirectCall` で active borrow token binding が落ちる問題を分離した。

Resource IR borrow checker は unknown callback を保守的に扱い、戻り値型と一致する active borrow token 引数を output へ伝播するようにした。これにより callback parameter や higher-order helper 経由でも borrow token の return escape を `ReturnValue` conflict として検出できる。

## 2026-04-28 Stage 4 owner return summary 追記

`ISS-20260428T112744824Z-RESOURCE-OWNER-CHECKER-LOSES-OWNERS--BB7D4D5F` として、direct helper が返す owner/free obligation を caller 側へ伝播していない問題を分離した。

Resource IR owner checker に direct user function の owner return summary を追加し、fresh owner return と parameter-to-return owner transfer を `ResourceOp::Call` に適用するようにした。これにより helper が allocation を返して caller が解放しない leak と、helper が owner 引数を戻り値へ移す transfer を Resource IR 上で扱える。

## 2026-04-28 Stage 4 owner function value summary 追記

`ISS-20260428T113234640Z-RESOURCE-OWNER-CHECKER-LOSES-OWNER-R-AE831E7E` として、known function value 経由の `IndirectCall` で owner return summary が適用されない問題を分離した。

Resource IR owner checker は `FunctionValue` / local copy / branch merge を通じて known callee alias を保持し、known `IndirectCall` へ direct call と同じ owner return summary を適用するようにした。これにより first-class function 経由でも fresh owner return の leak と owner argument transfer を検査できる。

## 2026-04-28 Stage 4 unknown callback owner argument return 追記

`ISS-20260428T113927369Z-RESOURCE-OWNER-CHECKER-LOSES-OWNER-A-4E0D013D` として、callee が known function alias を持たない `IndirectCall` で owner 引数の return transfer が落ちる問題を分離した。

Resource IR owner checker は unknown callback が output と同型の live owner 引数を返し得ると保守的に扱い、その owner を output へ transfer するようにした。fresh allocation は unknown callback へ無条件に仮定せず、既存 owner obligation の transfer に限定している。

## 2026-04-28 Stage 4 aggregate construct owner transfer 追記

`ISS-20260428T114547680Z-RESOURCE-OWNER-CHECKER-DOES-NOT-MOVE-0F2678E0` として、`ResourceOp::Construct` が owner/free obligation を aggregate output へ移していない問題を分離した。

Resource IR owner checker は construct input の live owner を output の deterministic projection へ移すようにした。これにより aggregate 構築後に元 owner place を再度 dealloc / move する経路を `OwnerUnavailable` として検出できる。

## 2026-04-28 Stage 4 aggregate owner projection move 追記

`ISS-20260428T114942018Z-RESOURCE-OWNER-CHECKER-DOES-NOT-MOVE-B551A456` として、aggregate output projection に入った owner が aggregate value の move / assign / branch transfer に追従しない問題を分離した。

Resource IR owner checker は exact place だけでなく source prefix 配下の owner projection を target prefix 配下へ移すようにした。これにより aggregate value movement と field owner obligation が一致し、旧 aggregate field path の再利用を `Moved` として検出できる。

## 2026-04-28 Stage 4 aggregate owner return summary 追記

`ISS-20260428T115405922Z-RESOURCE-OWNER-CHECKER-LOSES-AGGREGA-DE066CEF` として、return value 配下の owner projection が caller output projection へ伝播しない問題を分離した。

Resource IR owner checker の function return summary を projection owner に拡張し、callee return では descendant owner を move-out、caller call output では同じ projection suffix へ owner obligation を付けるようにした。これにより helper が owner を包んだ aggregate を返す API でも leak は caller 側 output field として報告される。

## 2026-04-28 Stage 4 aggregate parameter return descendant owner transfer 追記

`ISS-20260428T120003205Z-RESOURCE-OWNER-CHECKER-DOES-NOT-TRAN-C68B3366` として、owner return summary の適用時に exact argument owner だけを見ており、aggregate argument 配下の owner projection を transfer しない問題を分離した。

Resource IR owner checker は exact place または descendant projection に live owner がある場合を transferable とみなし、parameter-to-return summary と unknown callback fallback で `transfer_owner` を呼ぶようにした。これにより `fn id_wrapper(w): w` のような aggregate identity helper でも field owner obligation が caller output へ移る。

## 2026-04-28 Stage 4 borrow projection overlap 追記

`ISS-20260428T120507528Z-RESOURCE-BORROW-CHECKER-IGNORES-OVER-E9E862D3` として、borrow checker が exact place のみを見ており、aggregate root と field projection の重なりを検出しない問題を分離した。

Resource IR borrow checker は同一 root で projection prefix が重なる place を overlap とみなし、borrow 作成、read、assign / move / drop の conflict 判定に使うようにした。これにより field borrow 中の aggregate mutation や aggregate borrow 中の field mutation を検出できる。

## 2026-04-28 Stage 4 owner assignment overwrite 追記

`ISS-20260428T122245127Z-RESOURCE-OWNER-CHECKER-OVERWRITES-LI-209EA97B` として、owner checker が `ResourceOp::Assign` で target 側の既存 live owner/free obligation を見ずに新しい owner を上書きし、古い storage obligation を失う問題を分離した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 4 commit 単位 2 の続きとして、assign 前に target exact place と descendant projection の live owner を診断する。これにより raw owner local だけでなく、owner 入り aggregate field を別 aggregate で上書きする経路でも、古い free obligation を `OwnerLeaked` / `OwnerMaybeLeaked` として保持できる。

## 2026-04-28 Stage 4 function alias clear 追記

`ISS-20260428T122920913Z-RESOURCE-FUNCTION-ALIAS-TABLE-KEEPS--6208EF78` として、`FunctionAliasTable::copy_alias` が source に known function alias がない場合に target の古い alias を消さず、unknown callback を stale known callee として扱う問題を分離した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 4 の function / callback boundary の続きとして、alias copy を target state の上書き操作に変更する。これにより unknown function value を代入した後の indirect call では unknown callback fallback が働き、borrow token return escape や owner return transfer の conservative propagation が抜けにくくなる。

## 2026-04-28 Stage 5 effect function alias clear 追記

`ISS-20260428T123750146Z-RESOURCE-EFFECT-CHECKER-KEEPS-STALE--0C4747E0` として、effect checker 側の `FunctionAliasTable::copy_alias` も source に known function alias がない場合に target の古い alias を消さず、unknown callback raw identity propagation を stale known callee で迂回する問題を分離した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 5 の public escape diagnostics に向け、effect checker でも function alias copy を target state の上書きとして扱う。これにより unknown callback を代入した後の indirect call は raw identity 引数を保守的に output へ伝播し、pure function の raw address escape を検出できる。

## 2026-04-28 Stage 5 raw pointer alias overwrite 追記

`ISS-20260428T124306511Z-RESOURCE-EFFECT-CHECKER-KEEPS-STALE--E36BD546` として、effect checker の `RawPointerAliasTable::copy_alias` / `RawIdentityTable::copy_identity` が target place を旧 group から外さず、pointer variable 再代入後も旧 storage identity group に残る問題を分離した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 5 の raw identity / pointer provenance 境界として、raw value identity と pointer alias copy を target state の上書きに変更する。あわせて `RawMemoryIdentityTable` から target place を外してから pointer alias を付け替えることで、再代入後の `store` が旧 storage の raw identity state を誤って clear しないようにした。

## 2026-04-28 Stage 4 cell projection state 追記

`ISS-20260428T121156309Z-RESOURCE-CELLSTATE-CHECKER-IGNORES-A-C6DF3CB2` として、initialized / moved cell checker が exact place のみを見ており、aggregate root と descendant field projection の状態を相互に反映しない問題を分離した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 4 commit 単位 1 の続きとして、`CellState` の availability 判定を projection-aware にする。constructed aggregate の ancestor initialized state は field projection の読み出しを許可する一方で、field move / drop / maybe move は aggregate root の by-value use を拒否し、aggregate root の move / drop は旧 field projection の使用を拒否する必要がある。

## 2026-04-28 Stage 4 branch / loop merge 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 4 commit 単位 4 として、Resource IR 上の `CellState` / `OwnerState` / `BorrowState` を branch / loop / match 後に合流するようにした。

`CellState` は片側だけ move / drop / uninit になる経路を `MaybeMoved` に合流し、後続 read / move / drop で `CellUnavailable` になる。`OwnerState` は片側で dealloc され、片側で live のまま残る経路を `MaybeFreed` に合流し、後続 dealloc や関数終了時の maybe leak を診断する。`BorrowState` は片側だけ active な shared / unique borrow も active として合流し、後続 mutation / move / drop を安全側に拒否する。

loop は「condition が少なくとも一度評価される」ことを反映し、condition 後 state と body 後 state を合流する。match は各 arm の state を合流する。これにより Stage 4 commit 単位 1-3 で `ResourceCheckDeferred` / `ResourceOwnerCheckDeferred` / `ResourceBorrowCheckDeferred` に残していた branch / loop / match の deferred count は、この範囲では 0 になる。

回帰テストでは、片 branch の non-Copy move 後 read、loop body の non-Copy move 後 read、条件付き dealloc 後の再 dealloc、片 branch の shared borrow 後 mutation を `nepl-core/tests/resource_ir.rs` に固定した。

## 2026-04-28 Stage 4 old checker shadow gate 追記

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 4 commit 単位 5 として、旧 `move_check` の入口から Resource IR 検査を shadow 実行できる接続を追加した。

今回追加した `check_hir_resource_safety_shadow` は、HIR から Resource IR skeleton を生成し、lowering coverage、initialized / moved cell check、owner free obligation check、borrow lifetime check を 1 つの `ResourceSafetyShadowReport` にまとめる。`compiler.rs` の `run_move_check` は verbose mode のときだけこの shadow report を生成し、lowering / cell / owner / borrow の診断件数をログへ出す。通常の compiler 成否と diagnostic は引き続き旧 `move_check` が authoritative である。

ここで新 Resource IR 検査を即時エラー化しない理由は、function resource summary、aggregate owner decomposition、borrow escape lifetime、raw provenance の完全な関数境界伝播がまだ未完成だからである。現段階で enforcement へ切り替えると、正しい self-host 実装や stdlib 移行を false positive で止める危険がある。今回の接続により、旧 checker を弱めずに Resource IR 側の差分を継続観測できる。

回帰テストでは、`check_hir_resource_safety_shadow` が lowering diagnostic と3種類の resource check を同一 report へまとめ、non-Copy use-after-move を `CellState::Moved` として検出できることを `nepl-core/tests/resource_ir.rs` に固定した。

## 旧レビュー本文

- 解決済: false
- 状態: open
- 優先度: P1
- 種別: architecture
- 対象: `nepl-core/src/passes/move_check.rs`, `nepl-core/src/passes/drop_insertion.rs`, `nepl-core/src/compiler.rs`

### 根拠

- `nepl-core/src/compiler.rs:374`: drop 挿入後に monomorphize し、その後 move check。
- `nepl-core/src/passes/move_check.rs:35`: 変数状態は `BTreeMap<String, Vec<VarState>>`。
- `nepl-core/src/passes/move_check.rs:616`: `AddrOf` は borrow として状態を変える。
- `nepl-core/src/passes/drop_insertion.rs:91`: scope exit で trait drop call を後付け生成。

### 問題

Resource IR がなく、HIR 木を直接走査して所有権状態を推測しています。borrow の lifetime release、field move、auto drop と user drop の整合、branch merge が局所状態で処理されており、仕様化しづらいです。

### 影響

false positive / false negative の両方が起きやすく、stdlib の owning collection と組み合わせたときに double free や use-after-move を防ぎきれません。

### 修正方針

`doc/neplg3/impl/compiler_structure.md` の方針どおり HIR 後に Resource IR を導入し、move、borrow、region、drop elaboration を別 pass に分けます。drop 挿入前に ownership を確定し、auto drop は Resource IR 上で挿入します。

### 検証

branch/loop/borrow/field move/drop trait の compile_fail と should_panic を整理し、Resource IR dump の snapshot test を追加します。

## 2026-04-27 全体レビュー追記

`nepl-core/src/passes/move_check.rs` と `nepl-core/src/passes/drop_insertion.rs` は、直近の修正で field move、borrow last-use、overwrite drop などの個別 regression が増えています。一方で、どちらも HIR tree を直接たどる別々の状態機械であり、`load` / `store`、field offset、branch merge、scope drop の resource state は共有 IR で正規化されていません。

今回のレビューでは、`core/mem` の raw `load<T>` / `store<T>` / `alloc_raw` / `dealloc_raw` が ownership と effect 境界をまたいでいることも確認しました。これは Resource IR がないため memory place / token / internal effect を表現できない問題なので、具体 issue は `ISS-20260427T132406497Z-CORE-MEM-RAW-MEMORY-OPERATIONS-BYPAS-DC67DF04` に分離し、この issue 側では Resource IR 導入時の必須 input として扱います。

## 2026-04-28 compiler / mem 責務分割レビュー追記

`drop_insertion` の structural field drop と `move_check` の raw memory place state は有効な部分修正だが、どちらも Resource IR の代替にはなっていない。現状は `nepl-core/src/passes/move_check.rs:61` 以降で raw load/store を call name と address expression から推測し、`drop_insertion` 側でも別の走査で field address / raw storage cleanup を組み立てている。move、borrow/lifetime、initialized state、drop elaboration、raw pointer provenance が一つの resource state として共有されていない。

Resource IR 導入時の必須 input として、今回次の問題を追加した。

- raw body memory instruction の effect 分類: `ISS-20260427T152947135Z-RAW-BODY-MEMORY-INSTRUCTIONS-BYPASS--162A8C00`
- runtime allocator ABI と public stdlib API の分離: `ISS-20260427T152951013Z-RUNTIME-ALLOCATOR-HELPER-LOOKUP-DEPE-D070168E`
- safe `core/mem` からの raw address escape 除去: `ISS-20260427T152954558Z-CORE-MEM-EXPOSES-RAW-ADDRESS-ESCAPE--4185EA5D`
- `MemPtr` / `RegionToken` の provenance / owner token 設計: `ISS-20260427T152958303Z-MEMPTR-AND-REGIONTOKEN-LACK-COMPILER-0BC8ECDF`

修正順は、まず effect / runtime ABI を compiler 側で表現できるようにし、その後 `core/mem.nepl` の public raw API を縮小し、最後に Resource IR で owner token、borrow state、initialized state、drop obligation を共有する形にする。

## 2026-04-28 追加責務レビュー追記

`core/mem.nepl` と compiler の境界を再確認した結果、Resource IR 導入前に設計 input として固定すべき issue を追加した。

- `ISS-20260427T164419173Z-MEMORY-LAYOUT-RULES-ARE-DUPLICATED-A-FDB20787`: storage size / field offset / aggregate layout が typecheck、move_check、drop_insertion、WASM/LLVM codegen に重複している。Resource IR は共有 layout plan を参照しなければならない。
- `ISS-20260427T164412420Z-CORE-MEM-TYPED-MEM-COPY-AND-MEM-MOVE-621A41C7`: typed `MemPtr` bulk copy が non-Copy owner の複製を許す。Resource IR では copy と move-init を別 operation にする必要がある。
- `ISS-20260427T164432612Z-CORE-MEM-DEALLOC-APIS-DO-NOT-ENCODE--204F1F47`: raw dealloc が initialized cell の drop obligation を持たない。Resource IR は storage-only state と initialized owner state を区別する必要がある。
- `ISS-20260427T164425727Z-CORE-MEM-RAW-BODY-PRIVILEGE-IS-GRANT-043DAD95`: raw memory privilege が SourceMap path suffix で与えられている。Resource IR と module loader は compiler-owned capability を共有する必要がある。

## 2026-04-28 raw memory effect summary 追記

`move_check` の raw place state は direct call の検査を強化していたが、helper 関数内の raw memory operation を caller context へ伝播できず、関数境界で resource state が途切れる問題を `ISS-20260427T214055047Z-MOVE-CHECK-IGNORES-RAW-MEMORY-WRITES-417A7103` として修正した。

今回の関数サマリ拡張は Resource IR 導入前の補強であり、raw memory effect を function summary に載せて caller 側で再検査する。根本的には、関数境界をまたぐ move/borrow/drop/raw initialized state を Resource IR 上の effect として共有し、個別 summary の重ね合わせに依存しない設計へ移す必要がある。

## 2026-04-28 indirect call effect summary 追記

関数境界の direct call summary に続き、function value / higher-order helper 経由で raw memory effect が落ちる問題を `ISS-20260427T215657067Z-MOVE-CHECK-LOSES-RAW-MEMORY-EFFECTS--BDFF8DD5` として修正した。

現行 HIR 上では `@fn` と function-typed parameter の alias を追跡し、`CallIndirect` を known callee summary へ展開する補強を入れた。これは Resource IR 導入前の安全側強化であり、最終的には function effect、raw initialized state、borrow/drop obligation を型・Resource IR の共通表現へ統合する必要がある。

## 2026-04-28 enum payload function effect summary 追記

function value alias を通常の変数と parameter に保持するだけでは、`Option::Some @fn` などの enum payload を通過した時点で callback の resource effect が失われる問題を `ISS-20260427T221533970Z-MOVE-CHECK-LOSES-RAW-EFFECTS-THROUGH-308A8AC3` として修正した。

今回の補強では enum payload function alias を context stack と関数サマリへ追加し、`match` payload bind で function value alias を復元する。これにより現行 HIR 上の false negative は塞いだが、Resource IR では enum payload、function effect、raw initialized state を別々の補助 summary ではなく同じ resource state として表現する必要がある。

## 2026-04-28 memory model 方針レビュー追記

現在の raw memory 回帰修正は、既存 public API を壊さずに安全側へ寄せる移行措置としては意味がある。一方で、同じ方向を最終実装として継続するのは NEPLg2 の memory safety / effect 方針に対して適切ではない。

現状の複雑化は、`MemPtr` / raw `i32` / `RegionToken` / enum payload / callback / helper call / branch merge などを HIR 上の個別 summary で後追いしていることが原因である。この形では「何が owning resource で、何が non-owning pointer で、どの cell が initialized で、どこに drop obligation が残っているか」を compiler が単一の表現で保持できない。

したがって今後は個別 summary を積み増すのではなく、次の境界へ移す。

- safe surface から raw address escape を閉じ、raw operation は unsafe/internal boundary へ隔離する。
- `InternalAlloc` / `UnsafeMemory` 相当の internal effect を導入し、外へ raw identity が漏れない場合だけ surface `Pure` へ fold する。
- HIR 直走査ではなく Resource IR で owner token、borrow state、initialized state、drop obligation、pointer provenance を共有する。
- stdlib collection / string / selfhost buffer は `OwnedRegion` / `Storage` / builder token などの safe wrapper へ寄せ、`MemPtr<T>` を owner と pointer の兼用型として増やさない。

self-host は S1/S2 の lexer/parser/module loader など pure data model から開始できるが、S3 以降の move/resource/checker や raw memory backed buffer はこの設計移行を前提にする。

## 2026-04-28 Stage 4 aggregate field function alias 追記

`ISS-20260428T125012535Z-RESOURCE-FUNCTION-ALIAS-TABLE-LOSES--28281D3F` として、Resource IR の function alias state が aggregate construction で field projection へ伝播しない問題を分離し、修正した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 4 の function / callback boundary と owner token / free obligation の接続として、function value を struct / tuple / enum payload に格納してから field projection 経由で `IndirectCall` する場合も、known callee summary を使えるようにした。これにより、field-stored callback が fresh owner を返すケースで、caller 側の free obligation leak を見逃さない。

## 2026-04-28 静的検査大規模修正レビュー追記

Stage 2-5 の実装状況を確認した結果、Resource IR の data model、dump、call/raw memory lowering、cell/owner/borrow/effect shadow checks、branch/loop/match state merge、aggregate projection 伝播の多くは実装済みである。一方で、compiler pipeline 上は旧 `move_check` が authoritative であり、Resource IR は `RawAddressEscapeFromInternalAlloc` の D3025 gate と verbose shadow report を除いて強制にはなっていない。

新たに `ISS-20260428T132105486Z-RESOURCE-IR-LOWERING-COVERAGE-DOES-N-01BE2923` を追加した。`lower.rs` は non-variable addressable expression を `Place::unknown` に落とし、`Deref` も generic `Expr` のままで resource place transition を持たない。さらに `coverage.rs` は call/raw op count だけを比較するため、projection / borrow place lowering の欠落が lowering diagnostic に出ない。この issue は Stage 3 の lowering completeness と Stage 4 enforcement 切替前の blocker として扱う。

## 2026-04-28 Stage 3/4 lowering completeness 追記

`ISS-20260428T132105486Z-RESOURCE-IR-LOWERING-COVERAGE-DOES-N-01BE2923` を修正し、`AddrOf` / `Deref` の Resource IR lowering が addressable expression を `PlaceProjection::Deref` として保持するようにした。あわせて `add(base, offset)` を storage offset projection として表現し、raw storage / pointer provenance の lowering 入力を `Place::unknown` に落としにくくした。

coverage comparison は direct call / indirect call / function value / raw memory op だけでなく、construct / declare / read / move / assign / borrow / drop / deref projection / unknown place も比較する。Resource IR 側に unknown place が残った場合は operation 名付きで diagnostic 化されるため、Stage 4 の authoritative 切り替え前に lowering 欠落を test で捕捉できる。

## 2026-04-28 Stage 4 raw memory cell state 追記

`ISS-20260428T135023009Z-RESOURCE-CELLSTATE-CHECKER-TREATS-RA-DA292E10` として、Resource IR CellState checker が `RawMemoryOp::Load` / `Store` を普通の argument check として扱い、pointer が指す cell の initialized / moved state を持たない問題を分離し、修正した。

今回の変更では raw memory slot を `address.*` の `PlaceProjection::Deref` cell として扱い、store は value を consume して pointee cell を initialized にし、non-Copy load は pointee cell を `Moved` にする。あわせて CellState の prefix 判定を見直し、aggregate field の initialized state は子 field へ伝播し、`StorageOffset` は address value としては base availability を継承するが、`Deref` を越えて pointee cell へは initialized state を伝播しないようにした。

## 2026-04-28 Stage 4 raw storage destructive op 追記

`ISS-20260428T141745924Z-RESOURCE-CELLSTATE-CHECKER-IGNORES-D-40CECA56` として、Resource IR CellState checker が `dealloc` / `realloc` / `fill` / `bulk copy` / `bulk move` を ordinary argument check として扱い、`address.*` に残る initialized non-Copy cell を storage-only operation で消せる問題を分離し、修正した。

`doc/neplg2/static_check_complexity_reduction_plan.md` Stage 4 commit 単位 1 の続きとして、raw storage operation を address value availability と pointee cell obligation に分けた。`Store` は live non-Copy cell overwrite を拒否し、`Dealloc` / `Realloc` / `Fill` は live cell を持つ storage operation を拒否する。`BulkCopy` / `BulkMove` は source shallow copy と destination overwrite の両方を検査する。non-Copy `Load` で payload を consume 済みの cell は storage-only dealloc を許可するため、旧 D3100 防壁を弱めずに Resource IR 側へ移した。

## 2026-04-28 静的検査大規模修正レビュー追記 2

Stage 2-5 の全体レビューとして、`nepl-core/src/resource/check.rs` が 2674 行になり、CellState、OwnerState、BorrowState、function summary、raw cell helper、merge helper、shadow report が同居していることを確認した。これは旧 `move_check.rs` の責務集中を Resource IR 側に再作成する危険があるため、`ISS-20260428T142719860Z-RESOURCE-CHECKER-IS-BECOMING-A-NEW-M-5F78F7E4` として分離した。

この issue の修正は Stage 4/5 の authoritative gate 拡大前に行う。Resource IR の public API は `resource/mod.rs` で維持しつつ、`cell_state`、`owner_obligation`、`borrow_lifetime`、`raw_cell`、`function_summary`、`state_merge`、`shadow_report` に分け、検査意味論を変えずに module 境界を作る。

## 2026-04-28 静的検査大規模修正 post-BorrowState review 追記

`889a1a8` / `2fe878d` 後に `doc/neplg2/static_check_complexity_reduction_plan.md` と `nepl-core/src/resource` を再確認した。

現状では Stage 2 の Resource IR data model / dump、Stage 3 の local / aggregate / branch / raw memory / function call / callback lowering、Stage 4 の `CellState` / `OwnerState` / `BorrowState` 検査と branch / loop / match merge は実装済みである。raw memory load/store/destructive storage op も `address.*` cell state に接続済みで、Resource IR 側の focused regression は `cargo test -p nepl-core --test resource_ir -- --nocapture` で 67 件通過している。

一方で、2026-04-28 時点の compiler pipeline はまだ旧 `passes::move_check::run` を authoritative にしており、Resource IR は verbose shadow report と `RawAddressEscapeFromInternalAlloc` の D3025 gate を除いて強制されていなかった。したがって当時の Stage 4 は「実装されたが移行完了ではない」状態であり、authoritative 化前の残件は次の通りだった。

- `ISS-20260428T142719860Z-RESOURCE-CHECKER-IS-BECOMING-A-NEW-M-5F78F7E4`: `check.rs` は 2674 行から 2200 行まで減ったが、engine traversal / function summary / shadow report がまだ同居している。
- `nepl-core/src/resource/effect.rs`: 1273 行で raw identity summary、raw pointer summary、function alias、raw memory identity table、pointer alias table、branch merge が同居しており、Stage 5 の gate 拡大前に同じ責務分割が必要である。
- Stage 6 の stdlib memory API / `MemPtr` non-owning pointer 化 / owner token API は未完了であり、public safe surface から raw address escape を閉じる設計 issue は open のまま残る。

今回のレビューでは新規の独立 issue は追加しない。上記の残リスクは既存の P1 issue で追跡されており、次の実装単位は `ISS-20260428T142719860Z-RESOURCE-CHECKER-IS-BECOMING-A-NEW-M-5F78F7E4` の function summary / effect checker table 分割、または Resource IR authoritative gate の切替前条件の明文化にする。

## 2026-04-29 Stage 4/5 lowering coverage gate 追記

Resource IR の coverage comparison を verbose shadow report だけでなく compiler pipeline の gate に接続した。旧 `move_check` を通過した後、HIR と Resource IR の lowering coverage を比較し、function 欠落、operation count mismatch、`Place::unknown` が残る場合は `D3101` として compile error にする。

この変更は CellState / OwnerState / BorrowState の全 diagnostic を即時 authoritative 化するものではない。Stage 4/5 の検査を error 化する前提として、Resource IR lowering が静的検査入力を失っていないことを先に強制するための gate である。coverage diagnostic には span を保持するようにし、Resource IR がどの関数・どの operation で検査入力を落としたかを compiler diagnostic から追えるようにした。

`RawAddressEscapeFromInternalAlloc` の既存 D3025 gate は、同じ lowering 結果から得た effect boundary report を使う形に整理した。これにより Stage 5 の public raw identity escape gate と Stage 4 の lowering completeness gate が、同じ Resource IR input を前提に動く。

## 2026-04-29 Stage 4 destructive raw cell gate 追記

Resource IR の `check_resource_initialized_moves` が持つ CellState 診断のうち、destructive raw storage operation を compiler pipeline の D3100 gate に接続した。対象は `RawMemoryStoreCell`、`RawMemoryDeallocCell`、`RawMemoryReallocCell`、`RawMemoryFillCell`、bulk destination/source cell である。これにより live non-Copy payload を raw storage operation で上書き・解放・byte move する経路は、旧 `move_check` だけでなく Resource IR 側の状態遷移からも compiler error になる。

この gate の前提として、`initialized.rs` に raw address alias table を追加した。HIR lowering では同じ local raw address を読むたびに別 temporary place が生じるため、alias table がないと `store p` 後の `load p` で store cell と load cell が別 key になり、false `Uninit` になる。local read、let initializer、assign、move、raw alloc、raw realloc を通じて address alias を正規化し、raw cell state は canonical address 上で保持する。

`RawMemoryLoadCell` は今回の authoritative gate から除外した。試験的に full gate を有効化したところ、pointer-returning helper、`MemPtr` address wrapper、projection-based raw field access で raw cell initialization summary がまだ不足し、既存の D3025 / D3100 回帰より前に false D3100 が出た。この残件は `ISS-20260428T170745661Z-RESOURCE-IR-RAWMEMORYLOADCELL-GATE-N-1CBE1D0E` として分離した。

compiler-owned `core/mem` raw boundary 内部は、既存の raw identity gate と同じ SourceMap capability で除外した。allocator metadata の内部 raw load/store は Stage 6 の stdlib memory API 移行まで public user code と同じ基準では扱わない。

検証では、compiler gate の変換 unit test、raw address local read の Resource IR regression、`cargo check -p nepl-core --tests`、`trunk build`、`tests/compiler/move_effect.n.md` 110 件、`tests/compiler/move_check.n.md` 52 件を通した。

## 2026-04-29 Stage 4 borrow return gate 追記

Resource IR の `check_resource_borrow_lifetimes` が検出する `ReturnValue` borrow conflict を compiler pipeline の D3099 gate に接続した。これは active borrow token が関数境界を越えて戻り値として escape する経路を、旧 `move_check` の後に Resource IR 側でも authoritative にする変更である。非 return の borrow conflict は旧 checker との parity がまだ未確認なため、今回の gate では shadow-only に残した。

この作業中に、直前の selector-preserving `get_field` 変更と旧 `move_check` の alias recovery の不整合も見つけた。`field::get` が intrinsic `get_field` として HIR に残るようになった一方、aggregate field function alias は source-level `get` call と raw `load(add(...))` だけを projection として扱っていたため、struct field に保存した callback の known callee summary が `CallIndirect` へ伝播しなかった。`ISS-20260429T005109303Z-GET-FIELD-INTRINSIC-LOSES-AGGREGATE--37C1AFA1` として分離し、`get_field` intrinsic も同じ projection として扱うように修正した。

一方、Resource owner obligation gate も同時に authoritative 化できるか試したが、`ISS-20260429T004144320Z-RESOURCE-OWNER-GATE-TREATS-RAW-POINT-216A5E25` の blocker を確認した。現行 owner checker は raw pointer の通常 read/copy を owner transfer として扱うため、`load_i32 p` 後の `dealloc_raw p` のような正しい raw storage use を `Moved` / `NoFreeObligation` と誤診断する。Stage 4 の次の owner gate 拡大は、`MemPtr` を non-owning pointer、`OwnedRegion/Storage` を free obligation owner として分離し、dealloc / realloc / return / construct だけが owner obligation を消費する形へ整理してから行う。

検証では、Resource borrow gate unit test、Resource IR borrow check regression、`cargo check -p nepl-core --tests`、`trunk build`、`tests/compiler/move_effect.n.md` 110 件、`tests/compiler/move_check.n.md` 52 件を通した。

## 2026-04-29 Stage 4 borrow conflict gate 追記

`ISS-20260429T034515729Z-RESOURCE-BORROW-NON-RETURN-CONFLICTS-12A5A5E4` として、`ReturnValue` 以外の Resource IR borrow conflict も compiler pipeline に接続した。前回は return escape の `D3099` のみを authoritative にし、`Read` / `Move` / `Assign` / `Drop` / `SharedBorrow` / `UniqueBorrow` は旧 checker との parity 未確認を理由に shadow-only へ残していた。

今回の変更では、通常の borrow conflict を raw memory violation ではなく既存の borrow/move 診断 ID に写像した。shared borrow 中の move は `D3051`、unique borrow 中の read/move は `D3052`、borrow 中の assign/drop は `D3055`-`D3058`、borrow 作成 conflict は `D3061` / `D3062` になる。これにより Resource IR borrow checker が既存仕様と別のエラー体系を作らず、Stage 4 の borrow/lifetime check を compiler error として扱える。

検証では、compiler borrow gate unit test、Resource IR borrow check regression、`cargo check -p nepl-core --tests`、`trunk build`、`tests/compiler/move_check.n.md` 52 件、`tests/compiler/move_effect.n.md` 110 件を通した。

## 2026-04-29 Stage 4 owner alias gate 追記

`ISS-20260429T004144320Z-RESOURCE-OWNER-GATE-TREATS-RAW-POINT-216A5E25` を修正し、Resource owner checker の `Read` を free obligation transfer ではなく non-owning raw pointer alias として扱うようにした。これにより `load_i32 p` のような raw pointer read が storage owner を `Moved` にせず、その後の `dealloc_raw p` は alias から元の owner place を解決して free obligation を 1 回だけ解放する。

owner-consuming operation は `DeclareLocal` / `Assign` / `Move` / `Construct` / `Return` / `Dealloc` / `Realloc` に限定した。function owner return summary も alias 解決を使うため、helper return や callback return 経由で owner obligation が消えない。compiler pipeline には Resource owner gate を追加し、owner leak / double-free / moved-owner use は D3100 に昇格する。ただし raw identity escape では D3025 を優先するため、effect boundary gate を owner gate より先に実行する。

この作業中に、固定 raw address など unmanaged storage の origin を Resource IR が持たない問題を `ISS-20260429T012328323Z-RESOURCE-IR-LACKS-STORAGE-ORIGIN-FOR-549F82A4` として分離した。`NoFreeObligation` を恒久的に shadow-only にするのは Stage 4/6 の完了条件に反するため、次の owner gate 拡張では owned storage と unmanaged/internal storage の origin を明示し、owned storage の NoFreeObligation だけを D3100 にする。

検証では、Resource owner regression、compiler owner gate unit test、`cargo check -p nepl-core --tests`、`trunk build`、`tests/compiler/move_effect.n.md` 110 件、`tests/compiler/move_check.n.md` 52 件を通した。

## 2026-04-30 静的検査設計確認追記

`doc/neplg2/static_check_design_verification_20260430.md` で、現行 Rust 実装と self-host / stdlib 設計の整合を再確認した。

Resource IR の model、lowering coverage gate、CellState / OwnerState / BorrowState gate は、型安全・メモリ安全を compiler-owned state で検査する方向として妥当である。一方で、2026-04-30 時点の pipeline はまだ次の移行途中状態だった。

- 当時は `passes::insert_drops` が Resource IR check より前に HIR 上で drop を挿入していた。
- 当時は `passes::move_check::run` が先に authoritative として走り、その後に Resource IR gate が追加で走っていた。
- 当時は Resource IR gate のうち cell / owner diagnostic が compiler diagnostic では `resource.raw.ownership_violation` に潰れていた。
- 当時は `UnsafeMemoryInPureFunction` が stdlib raw-memory-backed API 移行のため shadow-only であり、最終的な static safety gate ではなかった。

したがって、この issue の完了条件は維持する。旧 HIR checker と drop insertion の special-case を増やすのではなく、drop elaboration を checked Resource IR 上へ移し、Resource IR を move / borrow / initialized / owner / raw provenance / drop obligation の単一 authority にする。

## 2026-04-30 raw fill helper initialized-cell summary 追記

`ISS-20260429T233515324Z-RESOURCE-IR-DOES-NOT-SUMMARIZE-RAW-F-48450939` として、`stdlib/core/mem.nepl` の `memset_u8` / `fill_i32` doctest が `RawMemoryLoadCell ... found Uninit` で失敗する問題を分離した。

これは assertion report 形式の問題ではなく、compiler-owned raw fill helper の caller-visible effect が Resource IR の initialized cell state へ summary されていない問題である。Stage 4 の RawMemoryLoadCell gate を弱めず、`alloc_raw -> raw fill/write helper -> load` の Copy cell 初期化だけを正しく表現する必要がある。

同 issue で、`memset_u8` / `fill_u8` / `fill_i32` を Resource IR の `RawMemoryOp::Fill` に分類し、fill 後の Copy raw cell 初期化を unknown-offset cell として記録するようにした。non-Copy cell を fill で構築した扱いにはせず、既存の live non-Copy overwrite compile-fail は維持している。

## 2026-04-30 Resource IR non-Copy regression harness 追記

`ISS-20260429T223654297Z-RESOURCE-IR-NON-COPY-REGRESSION-TEST-8F153A57` として、Resource IR の non-Copy initialized/moved regression が custom `TypeCtx` ではなく `lower_hir_module_skeleton` の fresh `TypeCtx` で lowering され、`TypeId` 不整合により panic していた問題を分離して修正した。

今回の修正は checker semantics を弱めるものではない。custom non-Copy 型を使う test は同じ `TypeCtx` で `lower_hir_module` に通し、`CellUnavailable` の診断を確認できる状態へ戻した。これにより non-Copy use-after-move / read-after-drop の回帰監視が、compiler panic ではなく Resource IR diagnostic に対する検証として機能する。

## 2026-05-06 Stage 4 normal cell gate 追記

`ISS-20260506T003017725Z-RESOURCE-IR-CELL-GATE-FILTERS-NON-RA-233DF19C` として、compiler boundary が `ResourceCheckDiagnostic::CellUnavailable` のうち raw-memory cell operation だけを hard error へ写像し、通常 read / move / drop / call argument / construct input / branch / match / return の cell-state violation を shadow-only に残していた問題を分離して修正した。

今回の変更で `run_resource_cell_gate` は raw-memory operation に限定せず、すべての `CellUnavailable` を `resource.cell.*` compiler diagnostic へ変換する。raw-memory-boundary capability による stdlib/core/mem 移行中許可は既存通り file capability に限定して維持する。

これにより、old move checker が見逃した通常 value cell の use-after-move / uninit / dropped / maybe-moved も Resource IR boundary で止まる。Stage 4 の残件は、通常 cell diagnostic の error 化ではなく、old `passes::move_check::run` と HIR `passes::insert_drops` を checked Resource IR / drop elaboration へ統合して削除することである。

## 2026-05-06 Stage 4 Resource IR gate ordering 追記

`ISS-20260506T004527322Z-RESOURCE-IR-GATES-ARE-MASKED-BY-LEGA-C34CEE5E` として、`compiler::run_move_check` が旧 `passes::move_check::run` を Resource IR lowering / cell / borrow / effect / owner gate より先に実行し、legacy HIR diagnostic が fail-fast で Resource IR diagnostic を隠し得る問題を分離して修正した。

今回の変更で Resource IR hard gate はすべて旧 checker より先に実行される。旧 checker は Resource IR gate 通過後の fallback 防壁として残す。これにより Stage 4 の authority は Resource IR 優先になり、self-host 実装で旧 HIR special-case を正規設計としてコピーする圧力を下げた。

回帰防止として `nodesrc/test_resource_gate_order.js` を追加し、source policy runner で gate 順序を監視する。Stage 4 の残件は、fallback として残る old move checker の削除と、HIR `insert_drops` の checked Resource IR drop elaboration への統合である。

## 2026-05-06 Stage 4 move_check Resource IR authority 追記

`ISS-20260506T022705566Z-MOVE-CHECK-DOCTESTS-ARE-STALE-AFTER--EDD8402F` として、`tests/compiler/move_check.n.md` が Resource IR gate authority 後も legacy diagnostic bucket を期待していた問題と、field reference projection lowering が initialized cell state を落とし得る問題を分離して修正した。

`field::get_ref` は offset 0 の場合も owner 全体の `AddrOf` を別型で再利用せず、typed `get_field_ref` intrinsic として保持する。Resource IR lowering は `get_ref` / `get_field_ref` / compiler-lowered `add &owner offset` を field `Borrow` として表し、raw address view と initialized field cell state を混同しない。HIR/Resource IR coverage はこの field reference projection を borrow + deref projection として数えるため、coverage gate を弱めずに新しい lowering を監視できる。

あわせて、`Never` value を返す branch / match arm を initialized-state merge path から除外した。これにより `Result::Err -> unreachable` のような到達不能 path が reachable path の initialized state を `MaybeMoved` / `Uninit` に汚染しない。

検証では Resource IR regression 2 件、`cargo check -p nepl-core --tests`、`cargo fmt --check -p nepl-core`、`trunk build`、`tests/compiler/move_check.n.md` 52 件、`tests/compiler/move_effect.n.md` 110 件を通した。Stage 4 の残件は、旧 `passes::move_check::run` fallback の削除と HIR `insert_drops` の Resource IR drop elaboration への統合である。

## 2026-05-06 Stage 4 legacy move_check fallback 削除追記

`ISS-20260506T025727360Z-REMOVE-LEGACY-MOVE-CHECK-FALLBACK-AF-C143E79B` として、Resource IR gate 通過後に残っていた旧 `passes::move_check::run` fallback を削除した。

compiler pipeline の関数名は `run_resource_static_check` とし、Resource IR lowering coverage / cell / borrow / effect / owner gate を authoritative path として実行する。旧 HIR move checker module は compiled pass から外し、source policy は compiler が legacy fallback を呼ばないことを監視する。

fallback 削除後の deep prefix chain 検証で、plain user function return の raw-address alias を lowering が各 call に展開し、owner gate の alias group を不要に膨張させる問題も確認した。これは old checker fallback なしで Resource IR を authority にする際の実用上の blocker であり、function return identity / owner transfer は Resource IR summary gate に一本化し、lowering では core mem wrapper と named raw helper の explicit raw semantics だけを materialize するよう整理した。

この変更により Stage 4 の二重 checker authority は解消した。残る Stage 4 の主な未完了点は、HIR `passes::insert_drops` を Resource IR drop elaboration へ統合することである。

旧 fallback 削除に合わせて、Resource IR borrow checker は aggregate / enum payload 内の borrow token を prefix/suffix 付きの token tree として Read / Move / Assign / Construct / Match bind / call return summary に伝播するようにした。branch / match arm の内部検査では外側の continuation を順序付きに参照し、使用より前に token scope が終わる場合は外側 EndScope で token を過剰保持しない。match arm bind は arm 終端に `EndScope` を持つため、pattern payload の参照寿命が arm 外へ漏れない。

回帰では `tests/compiler/move_check.n.md` 52 件が旧 checker なしで通過した。一方で `tests/compiler/move_effect.n.md` は 105/110 で、raw address helper literal offset と higher-order / aggregate / enum payload function value raw write の残件があるため `ISS-20260506T005443213Z-MOVE-EFFECT-DOCTESTS-ARE-STALE-AFTER-6955AAD2` を再オープンした。
