---
id: ISS-20260425T000000Z-RV-STDLIB-018-663A986A
title: "streamio の WASI doctest が trait bound 不一致と出力破損で失敗する"
area: stdlib
status: verified
resolved: true
priority: P1
type: bug
created: 2026-04-25
updated: 2026-04-26
target: "stdlib/std/streamio.nepl, tests/stdlib/streamio.n.md"
legacy_id: RV-STDLIB-018
source: "doc/review20260425/stdlib.md#rv-stdlib-018"
---

# RV-STDLIB-018: streamio の WASI doctest が trait bound 不一致と出力破損で失敗する

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-STDLIB-018-663A986A`。

## 要約

`ByteBuf` には `StreamWritable` 実装がなく、binary reader から受け取った buffer を `write` へ渡す正当な streamio の使い方が trait bound で拒否されていました。

runtime stdout mismatch は `RV-STDLIB-025` の enum storage / zero-sized struct field lowering 修正で解消しており、streamio 固有の残件は `ByteBuf` 書き込み API と numeric write fixture でした。

## 影響

binary stdin を stdout へ流す streamio doctest が compile できず、tutorial や競技プログラミング向け I/O の検証が止まっていました。`ByteBuf` を `str` へ変換して回避すると UTF-8 前提を混入させるため、binary payload と text payload の境界を保った修正が必要でした。

## 修正方針

`ByteBuf` を `StreamWritable` に追加し、`str` の layout を借りずに `ByteBuf` の `ptr` / `len` から writer buffer へ byte 単位で追記します。`ByteBuf` は所有値として受け取り、copy 後に `io_bytebuf_free` で解放します。

numeric write fixture は core の未解決 pipe 問題を避けるため、`let two <i64> cast 2;` で値を束縛してから `|> writeln two` へ渡す形にし、streamio の `i64` 書き込み自体を検証します。

## 対応

`append_bytebuf_impl` と `impl StreamWritable for ByteBuf` を追加しました。writer の capacity は既存の `push_u8_impl` に任せるため、入力長が内部 buffer を超える場合も flush を挟んで安全に追記できます。

`tests/stdlib/streamio.n.md` の numeric fixture は、inline cast を pipe 右辺へ置かず、明示的な `i64` local を `writeln` へ渡す形に変更しました。

pipe 右辺で `|> writeln <i64> cast 2` が失敗する問題は streamio API ではなく core の call boundary 問題として別 issue へ分離しました。

## 検証

- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/rv-stdlib-018-final-streamio-crlf.json -j 1` (`total=13`, `passed=13`, `failed=0`)
- `node nodesrc/tests.js -i tests/stdlib/io.n.md --no-tree -o tmp/rv-stdlib-018-final-io-crlf.json -j 1` (`total=6`, `passed=6`, `failed=0`)
- `node nodesrc/tests.js -i tests/stdlib/kp.n.md -i tests/stdlib/kp_i64.n.md --no-tree -o tmp/rv-stdlib-018-final-kp-crlf.json -j 1` (`total=11`, `passed=11`, `failed=0`)
- `node nodesrc/tests.js -i tests/stdlib --no-tree -o tmp/rv-stdlib-018-final-tests-stdlib-crlf.json -j 4` (`total=202`, `passed=199`, `failed=3`)

残りの3件は `tests/stdlib/features_tui.n.md` の undefined identifier 2件と `tests/stdlib/traits_text.n.md` の runtime return mismatch 1件で、streamio とは別原因として新規 issue 化しました。

## 旧レビュー本文

- 解決済: false
- 状態: open
- 優先度: P1
- 種別: bug
- 対象: `stdlib/std/streamio.nepl`, `tests/stdlib/streamio.n.md`, `nodesrc/run_test.js`

### 根拠

- GitHub Actions run `24940960078` の `wasi-test` / `nmd-doctest` artifact で、`tests/stdlib/streamio.n.md` が5件失敗している。
- `doctest#2` と `doctest#12` は `error[D3069]: type does not satisfy trait bound 'StreamWritable'` で `write bytes0` が解決できない。
- `doctest#5` は `writeln` が `D3006`、pipe が `D3013`、`close` が `D3016` で失敗している。
- `doctest#6` は expected `line1\nline2` に対して actual が `" \u0000\u0000\u0000\u000b\u0000\u0000\u0000\u0010\u0000\u0000"`、`doctest#7` は expected `text via read` に対して actual が `"\r\u0000\u0000\u0000\u0010\u0000\u0000\u0000 rea\u0000"` になり、文字列ではなく raw layout 由来の bytes が stdout に混入している。

### 問題

streamio は trait bound 解決と runtime I/O の両方で壊れています。compile failure は `StreamWritable` の impl / marker / import のいずれかが現在の型推論と噛み合っていないことを示し、runtime stdout mismatch は `str` / byte buffer / stream write の境界で内部 layout をそのまま出力している可能性を示します。

### 影響

標準 I/O wrapper が信頼できず、tutorial や競技プログラミング向け I/O の検証が止まります。`stdio.read_all` や UTF-8 保証の issue と関連する可能性がありますが、streamio 固有の trait 解決と出力破損を先に最小再現へ分ける必要があります。

### 修正方針

まず `tests/stdlib/streamio.n.md` の5失敗を compile と runtime に分けます。compile 側は `StreamWritable` impl の対象型、import clause、generic bound の正規化を確認します。runtime 側は `write` / `writeln` が `str` の data pointer と length を正しく取り出しているか、`ByteBuf` と `str` を混同していないかを確認します。修正は `unwrap` や raw memory で覆わず、stream abstraction の境界で型と layout を明確にします。

### 検証

- `node nodesrc/tests.js -i tests/stdlib/streamio.n.md --no-tree -o tmp/streamio-rv-stdlib-018.json -j 1`
- `node nodesrc/tests.js -i tests/stdlib -o tmp/tests-stdlib-streamio-after.json -j 4`
- `node nodesrc/tests.js -i tests -o tmp/tests-current-streamio-after.json -j 4`
