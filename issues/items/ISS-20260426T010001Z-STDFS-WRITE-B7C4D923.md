---
id: ISS-20260426T010001Z-STDFS-WRITE-B7C4D923
title: "std/fs lacks write and create interfaces required for self-host artifacts"
area: selfhost
status: verified
resolved: true
priority: P1
type: architecture
created: 2026-04-26
updated: 2026-04-26
target: stdlib/std/fs.nepl, nepl-cli/src/main.rs
source: doc/neplg2/self_host_plan.md
---

# ISS-20260426T010001Z-STDFS-WRITE-B7C4D923: std/fs lacks write and create interfaces required for self-host artifacts

## 概要

NEPLg2.0 self-host CLI は `.wasm`、LLVM IR、diagnostic JSON、dependency cache などをファイルへ出力する必要があるが、現行 `std/fs` は read 専用である。

## 対象

- `stdlib/std/fs.nepl`
- `nepl-cli/src/main.rs`
- `nepl-cli/tests/cli_output.rs`
- `tests/stdlib/fs.n.md`

## 根拠

- `fs_open_read`、`fs_read_fd_bytes`、`fs_read_to_bytes`、`fs_read_to_string` は存在する。
- `path_open` の write/create/truncate flags、`fd_write`、`fs_write_bytes`、`fs_write_string`、`FileWriter` に相当する public API がない。

## 問題

self-host compiler が artifact を外部へ保存できない。
stdout へ binary を流すだけでは、複数 artifact、cache、reproducible build 比較、CLI の `-o` 指定を実装できない。

## 影響

Stage 6 以降の Pass A / Pass B 比較が成立せず、セルフホスト完了条件を自動検証できない。
また、ユーザー向け CLI としてもコンパイル結果を明示的な output path へ書けない。

## 修正方針

`std/fs` に write 系を追加した。
追加 API は `fs_open_write`、`fs_write_fd_bytes`、`fs_write_to_bytes`、`fs_write_to_string`、`fs_close` とする。
WASI は preopen root 内の relative path を前提にし、create/truncate/write rights を明示する。
LLVM は Linux syscall 互換実装を既存 `__fs_syscall` 経路に追加した。
`nepl-cli` の WASI shim も read-only `path_open` / stdout-stderr 専用 `fd_write` のままでは self-host CLI で動かないため、write file descriptor を保持する `FileState::Write` を追加し、create/truncate/write と parent escape 拒否を runtime 側で検証する。

## 検証

- 存在しない一時ファイルへ `fs_write_to_string` し、`fs_read_to_string` で一致を確認する doctest。
- binary NUL を含む `ByteBuf` を書き戻して長さと内容を確認する doctest。
- preopen 外 path の拒否を CLI / WASI 側で確認する integration test。
- `git fetch origin main` 後、`origin/main` の `39fd5b7` まで取り込んで再検証した。
- `node nodesrc/tests.js -i stdlib/std/fs.nepl -i tests/stdlib/fs.n.md --no-tree -o tmp/fs-write-create-after-main-sync.json -j 1`: 11/11 passed
- `node nodesrc/tests.js -i stdlib/std/fs.nepl -i tests/stdlib/fs.n.md --no-tree -o tmp/fs-write-create-post-rebase.json -j 1`: 11/11 passed
- `cargo test -p nepl-cli run_wasi -- --nocapture`: 6/6 passed
- `trunk build`: passed
- `node nodesrc/tests.js -i stdlib --no-tree -o tmp/stdlib-fs-write-create-post-rebase.json -j 4`: 406/406 passed
- `node nodesrc/cli.js -i tests/playground_editor --playground-editor-tests -o json=tmp/playground-editor-fs-write-create-after-main-sync.json`: 13/13 passed
- `node nodesrc/issues.js check`: pass
- `git diff --check`: pass
- LLVM compile-only はこの環境で `clang --version` を実行できず `failed to execute clang --version` で未検証。
