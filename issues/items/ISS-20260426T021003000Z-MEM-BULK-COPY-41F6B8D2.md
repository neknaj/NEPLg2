---
id: ISS-20260426T021003000Z-MEM-BULK-COPY-41F6B8D2
title: "stdlib byte copy paths use byte-by-byte loops instead of bulk memory copy"
area: stdlib
status: verified
resolved: true
priority: P2
type: performance
created: 2026-04-26
updated: 2026-04-26
target: "stdlib/core/mem.nepl, stdlib/alloc/io.nepl, stdlib/alloc/string.nepl"
source: doc/neplg2/pre_selfhost_performance_audit_20260426.md
---

# ISS-20260426T021003000Z-MEM-BULK-COPY-41F6B8D2: stdlib byte copy paths use byte-by-byte loops instead of bulk memory copy

## 概要

stdlib の raw memory copy は `load_u8` / `store_u8` の byte loop に偏っている。
`stdlib/core/mem.nepl` には `memory.copy` などで効率を上げる TODO があり、`realloc_raw`、`io_bytebuf_from_str`、`io_bytebuf_to_str`、string copy 系 API が同じ制約を受ける。

## 根拠

- `stdlib/core/mem.nepl:20` に `memory.copy` 活用 TODO がある。
- `mem.nepl:409` の `realloc_raw` は `copy_bytes` 分を 1 byte ずつ copy する。
- `stdlib/alloc/io.nepl:86` と `:107` は `ByteBuf` / `str` 変換で 1 byte ずつ copy する。
- `stdlib/alloc/string.nepl` の `concat` / `sb_build` / `str_slice` も byte loop で copy する。

## 問題

self-host compiler は source text、diagnostic message、WASM binary、module path、serialized output で大量の連続 byte copy を行う。
byte loop だけだと copy 自体の計算量は O(n) でも定数倍が大きく、Vec grow や StringBuilder build のたびに実行時間が増える。

## 影響

WASM emitter の binary section build、source file read、diagnostic rendering が大きくなるほど CPU 時間が増える。
bulk copy がない状態で ByteBuilder を実装すると、builder 自体は growable でも append の実コストが不要に高いまま残る。

## 修正方針

`mem_copy` / `mem_move` / typed slice copy API を stdlib public API として追加し、WASM backend では `memory.copy`、LLVM backend では `llvm.memcpy` 相当へ下げる。
overlap を許す API と許さない API を分け、`realloc_raw`、`ByteBuf`、string copy、ByteBuilder から順に利用する。

## 対応

- raw wasm instruction parser に `memory.copy` を追加し、`#wasm` raw body から bulk memory copy を生成できるようにした。
- `stdlib/core/mem.nepl` に raw `mem_copy` / `mem_move` と `MemPtr` typed overload を追加し、wasm は `memory.copy`、llvm は `llvm.memcpy` / `llvm.memmove` に下げるようにした。
- `realloc_raw`、`io_bytebuf_from_str_result`、`io_bytebuf_to_str_result`、`concat`、`sb_build`、`str_slice` の byte-by-byte copy を bulk copy 呼び出しへ置き換えた。
- `origin/main` の `ISS-20260426T060223863Z-BYTEBUF-CONVERSIONS-HIDE-ALLOCATION--3BF03711` 修正を取り込み、Result-returning ByteBuf API と bulk copy を統合した。

## 検証

- overlap なし / overlap あり / zero length の memory copy doctest。
- `realloc_raw` と `ByteBuf` conversion が byte列一致を保つ regression。
- 大きな byte buffer copy の focused benchmark fixture。
- `node nodesrc/tests.js -i tests/stdlib/mem_bulk_copy.n.md --no-tree -o tmp/mem-bulk-copy-targeted.json -j 1`: `total=6`, `passed=6`, `failed=0`
- `node nodesrc/tests.js -i tests/stdlib/string.n.md --no-tree -o tmp/mem-bulk-copy-string.json -j 1`: `total=17`, `passed=17`, `failed=0`
- `node nodesrc/tests.js -i tests/stdlib/bytebuf_result.n.md --no-tree -o tmp/mem-bulk-copy-bytebuf-result.json -j 1`: `total=6`, `passed=6`, `failed=0`
- `node nodesrc/tests.js -i tests/stdlib/io.n.md --no-tree -o tmp/mem-bulk-copy-io-j2.json -j 2`: `total=6`, `passed=6`, `failed=0`
- `node nodesrc/tests.js -i stdlib --no-tree -o tmp/mem-bulk-copy-final-stdlib-full.json -j 4`: `total=404`, `passed=404`, `failed=0`
- `cargo test -p nepl-core --test neplg2 -- --nocapture`: `48 passed`
- `cargo fmt --all --check`: pass
- `trunk build`: pass
- `node nodesrc/cli.js -i tests/playground_editor --playground-editor-tests -o json=tmp/playground-editor-mem-bulk-copy.json`: `13/13 passed`
- `cargo test -p nepl-core --test neplg2 llvm_mem_bulk_copy_stdlib_lowers_to_intrinsics -- --nocapture`: pass
- `node nodesrc/tests.js -i tests/stdlib/mem_bulk_copy.n.md --no-tree --runner llvm --llvm-all --llvm-compile-only -o tmp/mem-bulk-copy-llvm-compile.json -j 1`: `clang` 不在のため既存 LLVM runner と同じ理由で未実行。LLVM lowering 自体は上記 Rust test で `llvm.memcpy` / `llvm.memmove` の IR 出力を確認した。
- `tests/stdlib/io.n.md` の `-j 1` legacy runner failure は `origin/main` でも再現したため、別 issue `ISS-20260426T072648414Z-NODESRC-TESTS-LEGACY-RUNNER-OVERFLOW-B0D364B2` として分離した。
