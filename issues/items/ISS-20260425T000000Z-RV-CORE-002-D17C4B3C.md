---
id: ISS-20260425T000000Z-RV-CORE-002-D17C4B3C
title: "typecheck.rs が巨大化しすぎて責務が分離できていない"
area: core
status: open
resolved: false
priority: P1
type: architecture
created: 2026-04-25
updated: 2026-04-28
target: "nepl-core/src/typecheck.rs, nepl-core/src/passes/move_check.rs"
legacy_id: RV-CORE-002
source: "doc/review20260425/core.md#rv-core-002"
---

# RV-CORE-002: typecheck.rs が巨大化しすぎて責務が分離できていない

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-CORE-002-D17C4B3C`。

## 要約

型推論、trait capability、overload、名前探索、HIR lowering、effect 判定、import alias 補助、diagnostic recovery が 1 ファイルに集中しています。局所修正が別機能へ波及しやすく、性能改善も profiling point を切りにくい状態です。

## 影響

既知バグの根本原因を追いにくく、修正のたびに回帰範囲が広がります。`doc/neplg3/impl/compiler_structure.md` で示されている `check/`, `ty/`, `resolve/`, `hir/` 分割方針との乖離も大きいです。

## 修正方針

短期的には `Env`、overload 解決、prefix reduction、trait impl 検査を別 module へ切り出します。中期的には AST flat list から HIR への lowering を型検査結果と分離し、`resolve` / `ty` / `check` の依存方向を固定します。

## 検証

分割ごとに既存 `tests/compiler/**` と stdlib doctest を走らせ、ファイル移動だけの段階では出力 JSON の差分がないことを確認します。

## 関連ドキュメント

- [NEPLg2 静的検査の複雑化解消計画](../../doc/neplg2/static_check_complexity_reduction_plan.md)

## 2026-04-28 issue 整理

この issue は静的検査大規模修正の Stage 1、つまり module 境界の切り出しと責務分離を追跡する親 issue とする。`typecheck.rs` と `move_check.rs` の巨大化そのものを扱い、Resource IR の意味論は `ISS-20260425T000000Z-RV-CORE-009-58589A3F`、raw memory API / effect 境界は `ISS-20260427T132406497Z-CORE-MEM-RAW-MEMORY-OPERATIONS-BYPAS-DC67DF04` へ分ける。

完了条件は、behavior を変えない module 分割を先に行い、その後の Resource IR 移行で focused regression の範囲を狭められる状態にすることである。局所的な helper 追加だけで `typecheck.rs` / `move_check.rs` の責務集中を温存する修正は、この issue の解決とはみなさない。

## 2026-04-28 Stage 1 実装開始

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 1「module 境界の切り出し」として、`move_check.rs` の raw memory helper 分類を `nepl-core/src/passes/move_check/raw_memory.rs` へ分離した。今回は behavior を変えず、`RawMemoryCallKind`、raw memory helper 名の分類、raw memory call kind 判定を子 module に移し、親 `move_check.rs` は raw memory 分類 API を呼び出すだけにした。

これは Resource IR 導入前に `move_check.rs` の責務境界を作る最初の実装であり、raw memory provenance そのものの意味論変更は行っていない。この issue は引き続き open とし、次は function summary / branch merge / place-provenance 型の module 化を進める。

## 2026-04-28 Stage 1 function summary 境界

Stage 1 の続きとして、`move_check.rs` から function raw alias summary の型と merge/dedupe 処理を `nepl-core/src/passes/move_check/summary.rs` へ分離した。対象は `ValueAliasSummary`、`RawMemoryEffectSummary`、`FunctionRawAliasSummary`、raw memory effect dedupe、branch/match 合流時の alias merge、child summary の effect 集約である。

HIR / context に依存する実際の summary 生成はまだ `move_check.rs` 側に残している。今回の目的は behavior を変えずに、現行の HIR summary を Resource IR summary へ置き換えるための型境界を先に作ることである。この issue は引き続き open とし、次は place/provenance state と branch snapshot/merge の分離を進める。

## 2026-04-28 Stage 1 raw place 境界

Stage 1 の続きとして、raw memory place の状態型と key/range helper を `nepl-core/src/passes/move_check/raw_place.rs` へ分離した。対象は `RawPlaceState`、`RawPlaceInfo`、raw place key の format/parse、offset 合成、unknown offset 判定、byte range overlap 判定である。

HIR / context を見る `raw_memory_place_key` や branch merge 本体はまだ `move_check.rs` 側に残している。今回の目的は、Resource IR の `StorageId` / `PointerProvenance` / `CellState` へ置き換わる純粋な raw place 表現を先に module 境界へ出すことである。この issue は引き続き open とし、次は branch snapshot/merge と context state の分離を進める。

## 2026-04-28 Stage 1 state 型境界

Stage 1 の続きとして、`move_check.rs` から state 型を `nepl-core/src/passes/move_check/state.rs` へ分離した。対象は `VarState`、`BorrowKind`、`BorrowBinding`、`ExprBorrow`、`FieldMove`、`FieldMovePath`、`BorrowCount`、`ResourceStateSnapshot` である。

branch/loop/match merge の処理本体はまだ `move_check.rs` 側に残している。今回の目的は、Resource IR の `ResourceState` / `BorrowState` / `CellState` に置き換える前に、現行 checker の状態表現を HIR 走査本体から独立させることである。この issue は引き続き open とし、次は branch snapshot/merge helper の module 化を進める。

## 2026-04-28 Stage 1 branch merge 境界

Stage 1 の続きとして、`move_check.rs` から branch/loop/match 合流時の状態 merge 処理を `nepl-core/src/passes/move_check/branch_merge.rs` へ分離した。対象は `BranchStateSnapshot`、`snapshot_top_state`、`changed_state_names`、raw place / raw alias / i32 const / function alias / enum payload / aggregate field alias の branch merge helper、borrow stack merge、`merge_continuing_branch_states` である。

今回の変更は behavior を変えず、分岐合流で `VarState`、field move、borrow binding、raw memory place、function value effect alias をどのように保存的に合成しているかを独立 module にまとめた。Resource IR Stage 4 で branch / loop merge を Resource state merge に置き換える際、この module を置換対象として扱えるようにする。この issue は引き続き open とし、次は `move_check.rs` に残る HIR 走査本体、raw provenance 復元、function summary 生成の責務境界をさらに切る。

## 2026-04-28 Stage 1 raw provenance 境界

Stage 1 の続きとして、`move_check.rs` から raw provenance 復元 helper を `nepl-core/src/passes/move_check/provenance.rs` へ分離した。対象は field address から field path を復元する処理、i32 offset 定数評価、`MemPtr` / `RegionToken` / raw i32 address から raw memory place key を得る処理、aggregate field load の raw alias 復元、raw dealloc / store / fill / bulk copy の size helper である。

今回の変更は D3100 系の raw memory ownership 検査の意味論を変えず、HIR 表現から provenance を復元する暫定層を独立させた。Resource IR Stage 3 ではこの module を `PointerProvenance` / `StorageId` lowering に置き換える対象として扱う。この issue は引き続き open とし、次は function raw alias summary 生成と HIR 走査本体の境界をさらに整理する。

## 2026-04-28 Stage 1 summary build 境界

Stage 1 の続きとして、function raw alias summary の生成処理を `nepl-core/src/passes/move_check/summary_build.rs` へ分離した。対象は raw memory effect summary 化、summary 用 param alias seed、fast-path call tree summary、`expression_raw_alias_summary`、`block_raw_alias_summary`、function summary の fixed-point build である。

`summary.rs` には summary 型と merge/dedupe 操作、`summary_build.rs` には HIR を walk して現行 summary を作る処理を置く境界にした。検査意味論は変更していない。Resource IR 導入時は、この HIR summary builder を Resource IR lowering / effect summary builder へ置き換える対象とする。この issue は引き続き open とし、次は `move_check.rs` に残る visitor 本体と context state 操作の分割を進める。

## 2026-04-28 Stage 1 visitor 境界

Stage 1 の続きとして、実検査 visitor 本体を `nepl-core/src/passes/move_check/visitor.rs` へ分離した。対象は variable use collection、borrow source / reference argument handling、iterative visit fast path、raw memory call visit、function raw memory effect 適用、`visit_expr_with_escape`、`visit_block_with_escape`、temporary borrow / field move source handling である。

これにより `move_check.rs` は context state と alias/provenance helper の集合、`visitor.rs` は HIR を実際に歩いて D30xx/D31xx 系の検査を適用する層、という境界に近づいた。検査意味論は変更していない。この issue は引き続き open とし、次は `MoveCheckContext` の state operation / diagnostics operation をさらに分割して、Resource IR Stage 4 の resource state 実装へ置換しやすい形にする。

## 2026-04-28 Stage 1 raw state 境界

Stage 1 の続きとして、`MoveCheckContext` から raw memory ownership state の検査・更新処理を `nepl-core/src/passes/move_check/raw_state.rs` へ分離した。対象は non-Copy raw place の load/store/dealloc/realloc/byte write/bulk copy 検査、overlap 判定結果からの D3100 diagnostic 生成、partial load 時の `PossiblyMoved` 更新である。

`raw_place.rs` は raw place の表現と range helper、`provenance.rs` は HIR から raw place key を復元する層、`raw_state.rs` は raw place state を更新して diagnostic を出す層、という責務に分けた。検査意味論は変更していない。Resource IR Stage 4 ではこの module を `CellState` / `StorageId` の resource state update に置き換える対象とする。

## 2026-04-28 Stage 1 context state 境界

Stage 1 の続きとして、`MoveCheckContext` の lifecycle、scope stack、variable state、borrow count、field move、alias stack、borrow/move diagnostic state operation を `nepl-core/src/passes/move_check/context_state.rs` へ分離した。対象は `new`、resource state snapshot/restore、scope push/pop、binding declare/update、borrow retain/release、use/assign/drop/temporary borrow/field move 検査、borrow count rebuild である。

今回の変更は behavior を変えず、HIR visitor から呼ばれる状態操作 API を `pub(super)` の module 境界に閉じた。これにより `visitor.rs` は HIR traversal、`raw_state.rs` は raw place state、`context_state.rs` は現在の checker state machine という責務に分かれる。Resource IR Stage 4 ではこの module の state operation を `ResourceState` / `BorrowState` / `CellState` に置き換える対象とする。

## 2026-04-28 Stage 1 alias helper 境界

Stage 1 の続きとして、`move_check.rs` に残っていた value alias / raw alias / function raw alias summary helper 群を `nepl-core/src/passes/move_check/alias.rs` へ分離した。対象は function value alias、function parameter placeholder、aggregate field alias、enum payload alias、raw alias instantiation、call-site summary specialization、match binding alias、field projection alias の helper である。

今回の変更は behavior を変えず、現行 HIR alias 推測を 1 module に集約した。Resource IR Stage 3-4 ではこの module を `Place` / `PointerProvenance` / `EffectOp` lowering と resource summary へ置き換える対象とする。これにより `move_check.rs` は context 定義と `run` の orchestration だけに近づき、alias 推測の暫定層が明示された。

## 2026-04-28 Stage 1 typecheck env 境界

Stage 1 の typecheck 側の開始として、`typecheck.rs` 末尾に残っていた `Binding` / `BindingKind` / `Scope` / `Env` と scope lookup / callable lookup / overload binding update helper を `nepl-core/src/typecheck/env.rs` へ分離した。対象は global/local binding 挿入、current/outer/defined lookup、callable symbol lookup、same signature duplicate removal、nested function binding update である。

今回の変更は behavior を変えず、typecheck 本体から name/scope environment の storage と操作を独立 module に出した。これにより Stage 1 で予定している symbol/env、overload、trait lookup、effect inference、HIR lowering 補助の分離のうち、まず symbol/env の状態境界を作った。次は overload candidate selection か trait capability lookup の切り出し候補をレビューする。

## 2026-04-28 Stage 1 typecheck trait 境界

Stage 1 の typecheck 側の続きとして、trait capability / trait bound / impl matching の基礎型と helper を `nepl-core/src/typecheck/traits.rs` へ分離した。対象は `TraitInfo`、`TraitSemantics`、`ImplInfo`、`TraitBoundRef`、`TraitCapability`、type parameter bound collection、trait reference formatting / parsing、trait application matching、type parameter instantiation inference、substitution mapping である。

今回の変更は behavior を変えず、Copy / Clone / Drop capability と generic trait bound 推論を typecheck 本体から独立させた。これにより Stage 1 で予定している trait lookup / capability 判定の置換対象が明確になり、後続の Resource IR / effect model で trait capability を参照する場合も typecheck 本体を直接増やさずに済む。

## 2026-04-28 再レビューでの再オープン

remote main の borrow/raw memory 修正取り込み後、責務集中は解消していない。現時点の line count は `nepl-core/src/typecheck.rs` が 9582 lines、`nepl-core/src/passes/move_check.rs` が 5486 lines で、旧レビュー時の `typecheck.rs` 8759 lines より悪化している。

`typecheck.rs` には overload、trait lookup、match exhaustiveness、literal match、HIR lowering、module/import 補助、diagnostic recovery が同居し続けている。`move_check.rs` も borrow state、field move、raw memory provenance、function/enum/aggregate alias summary を 1 file で持つ状態になっており、S3/S4 self-host 移植時に `resolve/ty/check/hir/resource` へ分ける基準を Rust 側から読み取りにくい。

この issue は再度 open とし、self-host 移植前に少なくとも仕様境界を module 単位で切り出す。Resource IR そのものの設計は `ISS-20260425T000000Z-RV-CORE-009-58589A3F` で追跡し、この issue では typecheck/move_check の責務分離と regression scope 縮小を扱う。

## 旧レビュー本文

- 解決済: true
- 状態: verified
- 優先度: P1
- 種別: architecture
- 対象: `nepl-core/src/typecheck.rs`

### 根拠

- `nepl-core/src/typecheck.rs`: 8759 行。
- `nepl-core/src/typecheck.rs:566`: instantiation cache を型検査本体が直接持つ。
- `nepl-core/src/typecheck.rs:7777`: `Env` / `Scope` 実装も同じファイル内。
- `nepl-core/src/typecheck.rs:8354`: `LabelEnv` / `StringTable` など補助構造も同居。

### 問題

型推論、trait capability、overload、名前探索、HIR lowering、effect 判定、import alias 補助、diagnostic recovery が 1 ファイルに集中しています。局所修正が別機能へ波及しやすく、性能改善も profiling point を切りにくい状態です。

### 影響

既知バグの根本原因を追いにくく、修正のたびに回帰範囲が広がります。`doc/neplg3/impl/compiler_structure.md` で示されている `check/`, `ty/`, `resolve/`, `hir/` 分割方針との乖離も大きいです。

### 修正方針

短期的には `Env`、overload 解決、prefix reduction、trait impl 検査を別 module へ切り出します。中期的には AST flat list から HIR への lowering を型検査結果と分離し、`resolve` / `ty` / `check` の依存方向を固定します。

### 検証

分割ごとに既存 `tests/compiler/**` と stdlib doctest を走らせ、ファイル移動だけの段階では出力 JSON の差分がないことを確認します。
