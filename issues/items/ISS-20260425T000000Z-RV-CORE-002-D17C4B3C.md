---
id: ISS-20260425T000000Z-RV-CORE-002-D17C4B3C
title: "typecheck.rs が巨大化しすぎて責務が分離できていない"
area: core
status: open
resolved: false
priority: P1
type: architecture
created: 2026-04-25
updated: 2026-04-28
target: "nepl-core/src/typecheck.rs, nepl-core/src/passes/move_check.rs"
legacy_id: RV-CORE-002
source: "doc/review20260425/core.md#rv-core-002"
---

# RV-CORE-002: typecheck.rs が巨大化しすぎて責務が分離できていない

旧 `doc/review20260425` から移行した Issue。新しい正の ID は `ISS-20260425T000000Z-RV-CORE-002-D17C4B3C`。

## 要約

型推論、trait capability、overload、名前探索、HIR lowering、effect 判定、import alias 補助、diagnostic recovery が 1 ファイルに集中しています。局所修正が別機能へ波及しやすく、性能改善も profiling point を切りにくい状態です。

## 影響

既知バグの根本原因を追いにくく、修正のたびに回帰範囲が広がります。`doc/neplg3/impl/compiler_structure.md` で示されている `check/`, `ty/`, `resolve/`, `hir/` 分割方針との乖離も大きいです。

## 修正方針

短期的には `Env`、overload 解決、prefix reduction、trait impl 検査を別 module へ切り出します。中期的には AST flat list から HIR への lowering を型検査結果と分離し、`resolve` / `ty` / `check` の依存方向を固定します。

## 検証

分割ごとに既存 `tests/compiler/**` と stdlib doctest を走らせ、ファイル移動だけの段階では出力 JSON の差分がないことを確認します。

## 関連ドキュメント

- [NEPLg2 静的検査の複雑化解消計画](../../doc/neplg2/static_check_complexity_reduction_plan.md)

## 2026-04-28 issue 整理

この issue は静的検査大規模修正の Stage 1、つまり module 境界の切り出しと責務分離を追跡する親 issue とする。`typecheck.rs` と `move_check.rs` の巨大化そのものを扱い、Resource IR の意味論は `ISS-20260425T000000Z-RV-CORE-009-58589A3F`、raw memory API / effect 境界は `ISS-20260427T132406497Z-CORE-MEM-RAW-MEMORY-OPERATIONS-BYPAS-DC67DF04` へ分ける。

完了条件は、behavior を変えない module 分割を先に行い、その後の Resource IR 移行で focused regression の範囲を狭められる状態にすることである。局所的な helper 追加だけで `typecheck.rs` / `move_check.rs` の責務集中を温存する修正は、この issue の解決とはみなさない。

## 2026-04-28 Stage 1 実装開始

`doc/neplg2/static_check_complexity_reduction_plan.md` の Stage 1「module 境界の切り出し」として、`move_check.rs` の raw memory helper 分類を `nepl-core/src/passes/move_check/raw_memory.rs` へ分離した。今回は behavior を変えず、`RawMemoryCallKind`、raw memory helper 名の分類、raw memory call kind 判定を子 module に移し、親 `move_check.rs` は raw memory 分類 API を呼び出すだけにした。

これは Resource IR 導入前に `move_check.rs` の責務境界を作る最初の実装であり、raw memory provenance そのものの意味論変更は行っていない。この issue は引き続き open とし、次は function summary / branch merge / place-provenance 型の module 化を進める。

## 2026-04-28 Stage 1 function summary 境界

Stage 1 の続きとして、`move_check.rs` から function raw alias summary の型と merge/dedupe 処理を `nepl-core/src/passes/move_check/summary.rs` へ分離した。対象は `ValueAliasSummary`、`RawMemoryEffectSummary`、`FunctionRawAliasSummary`、raw memory effect dedupe、branch/match 合流時の alias merge、child summary の effect 集約である。

HIR / context に依存する実際の summary 生成はまだ `move_check.rs` 側に残している。今回の目的は behavior を変えずに、現行の HIR summary を Resource IR summary へ置き換えるための型境界を先に作ることである。この issue は引き続き open とし、次は place/provenance state と branch snapshot/merge の分離を進める。

## 2026-04-28 Stage 1 raw place 境界

Stage 1 の続きとして、raw memory place の状態型と key/range helper を `nepl-core/src/passes/move_check/raw_place.rs` へ分離した。対象は `RawPlaceState`、`RawPlaceInfo`、raw place key の format/parse、offset 合成、unknown offset 判定、byte range overlap 判定である。

HIR / context を見る `raw_memory_place_key` や branch merge 本体はまだ `move_check.rs` 側に残している。今回の目的は、Resource IR の `StorageId` / `PointerProvenance` / `CellState` へ置き換わる純粋な raw place 表現を先に module 境界へ出すことである。この issue は引き続き open とし、次は branch snapshot/merge と context state の分離を進める。

## 2026-04-28 Stage 1 state 型境界

Stage 1 の続きとして、`move_check.rs` から state 型を `nepl-core/src/passes/move_check/state.rs` へ分離した。対象は `VarState`、`BorrowKind`、`BorrowBinding`、`ExprBorrow`、`FieldMove`、`FieldMovePath`、`BorrowCount`、`ResourceStateSnapshot` である。

branch/loop/match merge の処理本体はまだ `move_check.rs` 側に残している。今回の目的は、Resource IR の `ResourceState` / `BorrowState` / `CellState` に置き換える前に、現行 checker の状態表現を HIR 走査本体から独立させることである。この issue は引き続き open とし、次は branch snapshot/merge helper の module 化を進める。

## 2026-04-28 Stage 1 branch merge 境界

Stage 1 の続きとして、`move_check.rs` から branch/loop/match 合流時の状態 merge 処理を `nepl-core/src/passes/move_check/branch_merge.rs` へ分離した。対象は `BranchStateSnapshot`、`snapshot_top_state`、`changed_state_names`、raw place / raw alias / i32 const / function alias / enum payload / aggregate field alias の branch merge helper、borrow stack merge、`merge_continuing_branch_states` である。

今回の変更は behavior を変えず、分岐合流で `VarState`、field move、borrow binding、raw memory place、function value effect alias をどのように保存的に合成しているかを独立 module にまとめた。Resource IR Stage 4 で branch / loop merge を Resource state merge に置き換える際、この module を置換対象として扱えるようにする。この issue は引き続き open とし、次は `move_check.rs` に残る HIR 走査本体、raw provenance 復元、function summary 生成の責務境界をさらに切る。

## 2026-04-28 Stage 1 raw provenance 境界

Stage 1 の続きとして、`move_check.rs` から raw provenance 復元 helper を `nepl-core/src/passes/move_check/provenance.rs` へ分離した。対象は field address から field path を復元する処理、i32 offset 定数評価、`MemPtr` / `RegionToken` / raw i32 address から raw memory place key を得る処理、aggregate field load の raw alias 復元、raw dealloc / store / fill / bulk copy の size helper である。

今回の変更は D3100 系の raw memory ownership 検査の意味論を変えず、HIR 表現から provenance を復元する暫定層を独立させた。Resource IR Stage 3 ではこの module を `PointerProvenance` / `StorageId` lowering に置き換える対象として扱う。この issue は引き続き open とし、次は function raw alias summary 生成と HIR 走査本体の境界をさらに整理する。

## 2026-04-28 Stage 1 summary build 境界

Stage 1 の続きとして、function raw alias summary の生成処理を `nepl-core/src/passes/move_check/summary_build.rs` へ分離した。対象は raw memory effect summary 化、summary 用 param alias seed、fast-path call tree summary、`expression_raw_alias_summary`、`block_raw_alias_summary`、function summary の fixed-point build である。

`summary.rs` には summary 型と merge/dedupe 操作、`summary_build.rs` には HIR を walk して現行 summary を作る処理を置く境界にした。検査意味論は変更していない。Resource IR 導入時は、この HIR summary builder を Resource IR lowering / effect summary builder へ置き換える対象とする。この issue は引き続き open とし、次は `move_check.rs` に残る visitor 本体と context state 操作の分割を進める。

## 2026-04-28 Stage 1 visitor 境界

Stage 1 の続きとして、実検査 visitor 本体を `nepl-core/src/passes/move_check/visitor.rs` へ分離した。対象は variable use collection、borrow source / reference argument handling、iterative visit fast path、raw memory call visit、function raw memory effect 適用、`visit_expr_with_escape`、`visit_block_with_escape`、temporary borrow / field move source handling である。

これにより `move_check.rs` は context state と alias/provenance helper の集合、`visitor.rs` は HIR を実際に歩いて D30xx/D31xx 系の検査を適用する層、という境界に近づいた。検査意味論は変更していない。この issue は引き続き open とし、次は `MoveCheckContext` の state operation / diagnostics operation をさらに分割して、Resource IR Stage 4 の resource state 実装へ置換しやすい形にする。

## 2026-04-28 Stage 1 raw state 境界

Stage 1 の続きとして、`MoveCheckContext` から raw memory ownership state の検査・更新処理を `nepl-core/src/passes/move_check/raw_state.rs` へ分離した。対象は non-Copy raw place の load/store/dealloc/realloc/byte write/bulk copy 検査、overlap 判定結果からの D3100 diagnostic 生成、partial load 時の `PossiblyMoved` 更新である。

`raw_place.rs` は raw place の表現と range helper、`provenance.rs` は HIR から raw place key を復元する層、`raw_state.rs` は raw place state を更新して diagnostic を出す層、という責務に分けた。検査意味論は変更していない。Resource IR Stage 4 ではこの module を `CellState` / `StorageId` の resource state update に置き換える対象とする。

## 2026-04-28 Stage 1 context state 境界

Stage 1 の続きとして、`MoveCheckContext` の lifecycle、scope stack、variable state、borrow count、field move、alias stack、borrow/move diagnostic state operation を `nepl-core/src/passes/move_check/context_state.rs` へ分離した。対象は `new`、resource state snapshot/restore、scope push/pop、binding declare/update、borrow retain/release、use/assign/drop/temporary borrow/field move 検査、borrow count rebuild である。

今回の変更は behavior を変えず、HIR visitor から呼ばれる状態操作 API を `pub(super)` の module 境界に閉じた。これにより `visitor.rs` は HIR traversal、`raw_state.rs` は raw place state、`context_state.rs` は現在の checker state machine という責務に分かれる。Resource IR Stage 4 ではこの module の state operation を `ResourceState` / `BorrowState` / `CellState` に置き換える対象とする。

## 2026-04-28 Stage 1 alias helper 境界

Stage 1 の続きとして、`move_check.rs` に残っていた value alias / raw alias / function raw alias summary helper 群を `nepl-core/src/passes/move_check/alias.rs` へ分離した。対象は function value alias、function parameter placeholder、aggregate field alias、enum payload alias、raw alias instantiation、call-site summary specialization、match binding alias、field projection alias の helper である。

今回の変更は behavior を変えず、現行 HIR alias 推測を 1 module に集約した。Resource IR Stage 3-4 ではこの module を `Place` / `PointerProvenance` / `EffectOp` lowering と resource summary へ置き換える対象とする。これにより `move_check.rs` は context 定義と `run` の orchestration だけに近づき、alias 推測の暫定層が明示された。

## 2026-04-28 Stage 1 typecheck env 境界

Stage 1 の typecheck 側の開始として、`typecheck.rs` 末尾に残っていた `Binding` / `BindingKind` / `Scope` / `Env` と scope lookup / callable lookup / overload binding update helper を `nepl-core/src/typecheck/env.rs` へ分離した。対象は global/local binding 挿入、current/outer/defined lookup、callable symbol lookup、same signature duplicate removal、nested function binding update である。

今回の変更は behavior を変えず、typecheck 本体から name/scope environment の storage と操作を独立 module に出した。これにより Stage 1 で予定している symbol/env、overload、trait lookup、effect inference、HIR lowering 補助の分離のうち、まず symbol/env の状態境界を作った。次は overload candidate selection か trait capability lookup の切り出し候補をレビューする。

## 2026-04-28 Stage 1 typecheck trait 境界

Stage 1 の typecheck 側の続きとして、trait capability / trait bound / impl matching の基礎型と helper を `nepl-core/src/typecheck/traits.rs` へ分離した。対象は `TraitInfo`、`TraitSemantics`、`ImplInfo`、`TraitBoundRef`、`TraitCapability`、type parameter bound collection、trait reference formatting / parsing、trait application matching、type parameter instantiation inference、substitution mapping である。

今回の変更は behavior を変えず、Copy / Clone / Drop capability と generic trait bound 推論を typecheck 本体から独立させた。これにより Stage 1 で予定している trait lookup / capability 判定の置換対象が明確になり、後続の Resource IR / effect model で trait capability を参照する場合も typecheck 本体を直接増やさずに済む。

## 2026-04-28 Stage 1 typecheck effect 境界

Stage 1 の typecheck 側の続きとして、raw body / raw memory helper / purity effect validation を `nepl-core/src/typecheck/effect_check.rs` へ分離した。対象は pure raw body の raw memory instruction 検査、compiler-owned raw memory boundary 判定、raw memory intrinsic 許可判定、raw callee の impure 判定、target gate で有効な raw body selection である。

今回の変更は behavior を変えず、現行の `Pure` / `Impure` と raw memory boundary の暫定検査を module 境界に出した。Stage 5 の internal effect / surface fold 導入時は、この module を `InternalAlloc` / `UnsafeMemory` / `ExternalIO` / `Nondet` の分類層に置き換える対象とする。

## 2026-04-28 Stage 1 typecheck call resolution 境界

Stage 1 の typecheck 側の続きとして、overload / call arity / pipe target / nested function capture / outer consumer expected type 推論の helper を `nepl-core/src/typecheck/call_resolution.rs` へ分離した。対象は user-visible arity、nested function capture collection、outer function consumer search、expected argument inference、overloaded callable entry 判定、function signature instantiation、pipe pending segment reduction、mixed arity / mixed purity overload preselection である。

今回の変更は behavior を変えず、prefix call reduction 本体から overload candidate selection と call-resolution 補助を切り出した。これにより Stage 1 で予定している overload 解決分離の境界が明確になり、後続で `resolve` / `type inference` / `effect inference` の依存方向を整理しやすくなる。

## 2026-04-28 Stage 1 typecheck match 境界

Stage 1 の typecheck 側の続きとして、match scrutinee / enum arm / scalar literal arm / exhaustiveness / match arm result type 検査を `nepl-core/src/typecheck/match_check.rs` へ分離した。対象は `check_match_expr`、enum variant type application、enum match binding scope、bool / char / i32 / u8 literal pattern 判定、wildcard ordering、duplicate arm、non-exhaustive arm、match arm result type unify である。

今回の変更は behavior を変えず、`BlockChecker` 本体から match 専用の診断生成と HIR match lowering 補助を切り出した。match は self-host compiler の構文解析・AST 変換・型分岐で多用されるため、後続の Resource IR Stage 3 では HIR 直走査から Resource operation lowering へ移す境界として扱う。この issue は引き続き open とし、次は `typecheck.rs` に残る HIR lowering 補助、module/import 補助、diagnostic recovery の分割を進める。

## 2026-04-28 Stage 1 typecheck trait check 境界

Stage 1 の typecheck 側の続きとして、trait bound satisfaction と trait application type argument 推論を `nepl-core/src/typecheck/trait_check.rs` へ分離した。対象は concrete type 判定、type parameter bound ref 照合、impl candidate による trait bound satisfaction、trait ref から一意の type parameter を推論する処理、trait method call 時の applied trait argument 推論である。

今回の変更は behavior を変えず、`traits.rs` に置いた trait 型定義 / 純粋 helper と、`BlockChecker` の環境を読む trait 検査処理を別 module に分けた。Copy / Clone / Drop capability と generic trait bound は memory safety と Resource IR の前提になるため、後続の Stage 4 resource check が trait capability を参照する場合も `typecheck.rs` 本体を増やさずに済む。この issue は引き続き open とし、次は field access / aggregate lowering / diagnostic recovery の境界を確認する。

## 2026-04-28 Stage 1 typecheck field access 境界

Stage 1 の typecheck 側の続きとして、struct / tuple / generic apply の field access 型解決と offset 計算を `nepl-core/src/typecheck/field_access.rs` へ分離した。対象は field index/name の解決、generic struct field の type parameter substitution、tuple field index validation、invalid field access diagnostic、layout offset 計算である。

今回の変更は behavior を変えず、aggregate field access の型解決と diagnostic を `BlockChecker` 本体から独立させた。field access は HIR lowering と Resource IR の `Place::Field` / aggregate projection lowering の接点になるため、Stage 3 ではこの module を resource lowering 側へ渡す境界として扱う。この issue は引き続き open とし、次は block/prefix lowering 本体に残る hoist / let lowering / assignment recovery の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck name lookup 境界

Stage 1 の typecheck 側の続きとして、`ImportResolution` を参照する qualified / unqualified name lookup helper を `nepl-core/src/typecheck/name_lookup.rs` へ分離した。対象は qualified alias lookup、unqualified lookup name 展開、import visibility 判定、defined value/callable lookup、未定義 non-mut hoist を許可する read lookup である。

今回の変更は behavior を変えず、scope storage を持つ `env.rs` と、module/import 可視性を読む lookup 層を別 module に分けた。これにより Stage 1 の symbol/env 境界がさらに明確になり、後続で `resolve` と `type inference` の依存方向を整理するときに import visibility の処理を `BlockChecker` 本体から切り離しやすくなる。この issue は引き続き open とし、次は block/prefix lowering 本体の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck type expr 境界

Stage 1 の typecheck 側の続きとして、`TypeExpr` から `TypeCtx` への lowering と補助状態を `nepl-core/src/typecheck/type_expr.rs` へ分離した。対象は `LabelEnv`、`StringTable`、primitive / named / apply / label / function / tuple / boxed / reference type expression の `TypeId` 変換である。

今回の変更は behavior を変えず、型式 lowering と string literal interning state を `typecheck.rs` 本体から独立させた。`traits.rs` は `type_expr` module から `type_from_expr` と `LabelEnv` を参照する形にし、trait bound collection と type expression lowering の依存を明示した。この issue は引き続き open とし、次は signature / mangling / overload signature 比較 helper の分離を確認する。

## 2026-04-28 Stage 1 typecheck signature 境界

Stage 1 の typecheck 側の続きとして、function signature 表現、symbol mangling、generic signature 比較、unbound type 判定を `nepl-core/src/typecheck/signature.rs` へ分離した。対象は `mangle_function_symbol`、`mangle_impl_method`、`function_signature_string`、`same_function_signature`、generic type parameter 対応の signature type comparison、unique type collection、unbound type variable 判定である。

今回の変更は behavior を変えず、overload / alias / impl method registration / trait check から共有される signature helper を独立 module にまとめた。`env.rs` と `trait_check.rs` は `signature` module を参照する形にし、名前環境と trait 検査が function signature 判定へ依存していることを明示した。この issue は引き続き open とし、次は block/prefix lowering 本体に残る hoist / let lowering / assignment recovery の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck HIR finalize 境界

Stage 1 の typecheck 側の続きとして、HIR type id 正規化と raw aggregate load 補助を `nepl-core/src/typecheck/hir_finalize.rs` へ分離した。対象は `resolve_type_ids_in_function` / `resolve_type_ids_in_block` / `resolve_type_ids_in_expr`、raw memory load name 判定、aggregate storage load から address expression を復元する補助、field offset を i32 add expression へ変換する補助である。

今回の変更は behavior を変えず、型検査結果の HIR を安定化する後処理と raw aggregate lowering の暫定 helper を `typecheck.rs` 本体から独立させた。Resource IR Stage 3 では raw aggregate address 復元を `Place` / projection lowering へ置き換える対象として扱う。この issue は引き続き open とし、次は block/prefix lowering 本体に残る hoist / let lowering / assignment recovery の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck binding rule 境界

Stage 1 の typecheck 側の続きとして、shadow / no-shadow / overload duplicate / field accessor detection / overload specificity helper を `nepl-core/src/typecheck/binding_rules.rs` へ分離した。対象は重要 stdlib symbol shadow warning、non-shadowable binding の衝突判定、same-signature function lookup、same-file overload arity conflict、function value overload preselection 用 specificity、`get_field` / `get_field_ref` / `set_field` wrapper detection である。

今回の変更は behavior を変えず、scope storage を持つ `env.rs` と、binding rule / overload rule を適用する helper を別 module に分けた。`match_check.rs` は `binding_rules::emit_shadow_warning` を参照する形にし、match binding の shadow warning も同じ rule set に従うことを明示した。この issue は引き続き open とし、次は block/prefix lowering 本体に残る hoist / let lowering / assignment recovery の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck ascription 境界

Stage 1 の typecheck 側の続きとして、type annotation / ascription と char literal context 判定を `nepl-core/src/typecheck/ascription.rs` へ分離した。対象は `apply_ascription`、char literal value 抽出、char literal が `i32` / `u8` context に適合するかの判定、`u8` 範囲外 diagnostic である。

今回の変更は behavior を変えず、型注釈による type unification と char literal の context-sensitive lowering を `typecheck.rs` 本体から独立させた。char literal は整数 backed codegen と `char` 型安全の境界にあるため、後続の type inference / literal lowering 整理でこの module を置換対象として扱えるようにする。この issue は引き続き open とし、次は call reduction の open-call state helper か block/prefix lowering 本体の分割単位を確認する。

## 2026-04-28 Stage 1 typecheck call reduction 境界

Stage 1 の typecheck 側の続きとして、prefix expression の call reduction state 管理を `nepl-core/src/typecheck/call_reduction.rs` へ分離した。対象は open-call stack の rebuild / next reducible search / reduction 後 index 更新、call reduction no-progress guard、`reduce_calls` / `reduce_calls_guarded`、dotted field symbol lowering、match arm から scrutinee 期待型を推論する helper である。

今回の変更は behavior を変えず、`check_prefix` 本体から call stack reduction と prefix 補助 lowering を独立させた。既存の `call_resolution.rs` は overload candidate / expected argument inference / pipe segment reduction の補助、今回の `call_reduction.rs` は実際の stack reduction orchestration という境界にした。この issue は引き続き open とし、次は `apply_function` の assignment / builtin / user call lowering 分割か、`check_block` の hoist / let lowering 分割を確認する。

## 2026-04-28 Stage 1 typecheck syntax helper 境界

Stage 1 の typecheck 側の続きとして、variant / trait method name parse、i32 literal parse、target/profile gate 判定を `nepl-core/src/typecheck/syntax_helpers.rs` へ分離した。対象は `parse_variant_name`、`parse_i32_literal`、`gate_allows` である。

今回の変更は behavior を変えず、match / name lookup / effect check / top-level gate processing から共有される primitive helper を `typecheck.rs` 本体から独立させた。`effect_check.rs`、`match_check.rs`、`name_lookup.rs` は `syntax_helpers` module を参照する形にし、syntax-level parse と type/effect 検査の依存方向を明示した。この issue は引き続き open とし、次は `apply_function` の assignment / builtin / user call lowering 分割か、`check_block` の hoist / let lowering 分割を確認する。

## 2026-04-28 Stage 1 typecheck function apply 境界

Stage 1 の typecheck 側の続きとして、呼び出し適用本体を `nepl-core/src/typecheck/function_apply.rs` へ分離した。対象は assignment operator lowering、`if` / `while` special form、overload candidate selection、field accessor lowering、enum / struct constructor lowering、trait method call lowering、function value indirect call lowering である。

今回の変更は behavior を変えず、`check_prefix` が stack reduction を行い、`function_apply.rs` が「stack 上の callee と args を HIR call / construct / assignment に適用する」責務を持つ境界にした。`typecheck.rs` は 4423 lines、`function_apply.rs` は 1644 lines となり、後続で assignment / builtin / user call / trait call をさらに分割する対象が明確になった。この issue は引き続き open とし、次は `check_block` の hoist / nested function handling か、`check_prefix` の literal / symbol lowering 境界を確認する。

## 2026-04-28 Stage 1 typecheck prefix check 境界

Stage 1 の typecheck 側の続きとして、prefix expression の lowering 本体を `nepl-core/src/typecheck/prefix_check.rs` へ分離した。対象は literal / symbol / let / set / addr-of / deref / intrinsic / match / tuple / group / block / pipe の stack lowering と、prefix 末尾の let recovery である。

今回の変更は behavior を変えず、`check_block` は statement と scope orchestration、`prefix_check.rs` は prefix syntax を stack entry と HIR expression へ lower する層、`call_reduction.rs` は open-call reduction、`function_apply.rs` は callee/args の適用、という境界にした。`typecheck.rs` は 2526 lines、`prefix_check.rs` は 1948 lines となった。この issue は引き続き open とし、次は `check_block` の hoist / nested function handling か top-level `typecheck` / `check_function` の orchestration 境界を確認する。

## 2026-04-28 Stage 1 typecheck block check 境界

Stage 1 の typecheck 側の続きとして、block / statement checking 本体を `nepl-core/src/typecheck/block_check.rs` へ分離した。対象は scope push/pop、non-mut let hoist、nested function signature hoist、directive gate、statement stack result validation、nested function body check、block final value recovery である。

今回の変更は behavior を変えず、`block_check.rs` は block scope と statement orchestration、`prefix_check.rs` は prefix syntax lowering、`function_apply.rs` は callee/args 適用、という境界にした。`typecheck.rs` は 1978 lines、`block_check.rs` は 582 lines となった。この issue は引き続き open とし、次は top-level `typecheck` と `check_function` の orchestration 境界、または残った helper 型の配置を確認する。

## 2026-04-28 Stage 1 typecheck model 型境界

Stage 1 の typecheck 側の続きとして、typecheck submodule 間で共有する小さな型定義を `nepl-core/src/typecheck/model.rs` へ分離した。対象は `ScalarMatchKind`、`CheckedFunction`、`EnumInfo`、`StructInfo`、`FieldIdx`、`FieldAccessorKind`、`AssignKind`、`StackEntry` である。

今回の変更は behavior を変えず、`typecheck.rs` に残っていた共有 model 型を module 境界へ移した。各 submodule は従来どおり親 module 経由で型を参照できるようにし、親 file は top-level orchestration と `BlockChecker` context 定義に近づけた。`typecheck.rs` は 1918 lines、`model.rs` は 65 lines となった。この issue は引き続き open とし、次は top-level `typecheck` と `check_function` の分割可否を確認する。

## 2026-04-28 Stage 1 typecheck driver 境界

Stage 1 の typecheck 側の続きとして、public `typecheck` driver と `TypeCheckResult` を `nepl-core/src/typecheck/driver.rs` へ分離した。対象は top-level directive / enum / struct / trait / impl / function registration、import resolution、instantiation expansion、entry resolution、diagnostic summary、LLVM entry fallback helper である。

今回の変更は behavior を変えず、`typecheck.rs` では `pub use driver::{typecheck, TypeCheckResult}` により public API を維持した。`driver.rs` は module 全体の orchestration、親 `typecheck.rs` は `check_function` と `BlockChecker` context 定義、各 submodule は個別検査・lowering という境界になった。`typecheck.rs` は 377 lines、`driver.rs` は 1578 lines となった。この issue は引き続き open とし、次は `check_function` と `BlockChecker` context の配置を確認する。

## 2026-04-28 Stage 1 typecheck function check 境界

Stage 1 の typecheck 側の続きとして、single function body checking を `nepl-core/src/typecheck/function_check.rs` へ分離した。対象は function type snapshot、parameter scope registration、raw body effect validation、`BlockChecker` 起動、pending trait bound check、HIR function assembly、HIR type id finalization である。

今回の変更は behavior を変えず、`driver.rs` は module / top-level orchestration、`function_check.rs` は 1 関数の body checking と HIR function assembly、`block_check.rs` 以下は block / prefix / call lowering という境界にした。`typecheck.rs` は 72 lines、`function_check.rs` は 322 lines となり、親 file は module wiring と `BlockChecker` context 定義に絞られた。この issue は引き続き open とし、次は `BlockChecker` context 定義の配置と Stage 1 完了条件を確認する。

## 2026-04-28 Stage 1 typecheck context 境界

Stage 1 の typecheck 側の続きとして、`BlockChecker` context 定義を `nepl-core/src/typecheck/context.rs` へ分離した。対象は `TypeCtx`、`Env`、label / string table、diagnostic buffer、trait bound check queue、enum / struct / trait / impl table、instantiation table、target/profile/source map を束ねる checker state である。

今回の変更は behavior を変えず、`typecheck.rs` は module declaration、submodule 共有用の限定 import、public `typecheck` re-export のみに縮小した。`BlockChecker` の field visibility は `pub(super)` に留め、既存 sibling module からの検査 state 参照は維持しつつ crate 外 API には出さない。`typecheck.rs` は 32 lines、`context.rs` は 41 lines となり、親 file は module wiring だけになった。この issue は引き続き open とし、次は typecheck Stage 1 の残責務を再確認し、必要なら `driver.rs` / `function_apply.rs` / `prefix_check.rs` の二段目分割を issue 化する。

## 2026-04-28 Stage 1 typecheck assignment apply 境界

Stage 1 の typecheck 側の続きとして、assignment operator lowering を `nepl-core/src/typecheck/assignment_apply.rs` へ分離した。対象は `let` / `set` の environment binding 更新、address-of lowering、deref lowering、assignment arity / undefined / immutable / type mismatch diagnostic である。

今回の変更は behavior を変えず、`apply_function` の先頭に混在していた state 更新型 special form を通常 call / overload / trait call の適用処理から分けた。`function_apply.rs` は 1483 lines、`assignment_apply.rs` は 186 lines となった。assignment は borrow/resource check の入力になる HIR `Let` / `Set` / `AddrOf` / `Deref` を作る境界なので、Stage 3-4 で Resource IR lowering と照合する対象として明示する。この issue は引き続き open とし、次は `function_apply.rs` に残る overload candidate selection と field accessor lowering の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck control apply 境界

Stage 1 の typecheck 側の続きとして、control special form lowering を `nepl-core/src/typecheck/control_apply.rs` へ分離した。対象は `if` の arity / condition type / branch type validation と HIR `If` lowering、`while` の arity / condition type / body unit validation と HIR `While` loweringである。

今回の変更は behavior を変えず、`apply_function` から callee lookup を使わない control special form を分けた。`function_apply.rs` は 1415 lines、`control_apply.rs` は 99 lines となった。control special form は block/prefix lowering と function call 適用の接点であり、後続で effect/resource check が branch / loop の state merge と接続する箇所を追いやすくする。この issue は引き続き open とし、次は `function_apply.rs` に残る overload candidate selection と field accessor lowering の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck field apply 境界

Stage 1 の typecheck 側の続きとして、field accessor の適用処理を `nepl-core/src/typecheck/field_apply.rs` へ分離した。対象は field index/name argument の読み取り、`get` の raw aggregate load address 補正、`get_ref` の field reference address lowering、`put` の store intrinsic lowering である。

今回の変更は behavior を変えず、field 型解決を担当する `field_access.rs` と、field accessor binding を HIR operation に変換する `field_apply.rs` の責務を分けた。`function_apply.rs` は 1268 lines、`field_apply.rs` は 160 lines となった。field projection は Resource IR の `Place::Field` / pointer provenance lowering と接続するため、Stage 3-4 で raw aggregate address 復元を置き換える対象として扱いやすくなる。この issue は引き続き open とし、次は `function_apply.rs` に残る overload candidate selection と constructor / trait call lowering の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck constructor apply 境界

Stage 1 の typecheck 側の続きとして、enum / struct constructor lowering を `nepl-core/src/typecheck/constructor_apply.rs` へ分離した。対象は enum variant name 解決、constructor arity diagnostic、payload expression assembly、generic type argument application、tag unit struct の unit field 補完、HIR `EnumConstruct` / `StructConstruct` 生成である。

今回の変更は behavior を変えず、constructor lowering を trait method call / normal call / indirect call の適用処理から分けた。`function_apply.rs` は 1154 lines、`constructor_apply.rs` は 205 lines となった。constructor は AST/HIR 型表現と resource/lifetime check の入力になる owned aggregate 生成の境界なので、Stage 3-4 で aggregate place lowering と照合しやすくなる。この issue は引き続き open とし、次は `function_apply.rs` に残る trait call lowering と indirect call lowering の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck indirect apply 境界

Stage 1 の typecheck 側の続きとして、function value / indirect call fallback を `nepl-core/src/typecheck/indirect_apply.rs` へ分離した。対象は `FnValue` / function-typed variable / function-typed expression の `CallIndirect` lowering、capturing function value unsupported diagnostic、indirect call function type diagnostic、expected return type unification である。

今回の変更は behavior を変えず、binding で解決された normal call / trait call と、runtime function value call を別 module に分けた。`function_apply.rs` は 1077 lines、`indirect_apply.rs` は 93 lines となった。function value は self-host compiler の callback / visitor / diagnostic pipeline で増える可能性が高いため、capture policy と `CallIndirect` lowering の境界を明示しておく。この issue は引き続き open とし、次は `function_apply.rs` に残る trait call lowering と overload selection の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck trait bound apply 境界

Stage 1 の typecheck 側の続きとして、選択済み関数の generic trait bound 検査を `nepl-core/src/typecheck/trait_bound_apply.rs` へ分離した。対象は選択済み callable の type parameter bound、明示・推論 type argument mapping による trait self type / trait argument substitution、raw / resolved / inferred type argument の bound satisfaction 確認、concrete 型で満たせない場合の `TypeTraitBoundUnsatisfied` diagnostic、未確定型の場合の pending trait bound queue 登録である。

今回の変更は behavior を変えず、`function_apply.rs` の通常 call assembly から trait bound satisfaction の適用層を独立させた。`function_apply.rs` は 999 lines、`trait_bound_apply.rs` は 112 lines となった。generic trait bound は type safety と capability check の前提なので、呼び出し HIR 生成の中に埋め込まず、Stage 1 の境界として明示しておく。この issue は引き続き open とし、次は `function_apply.rs` に残る trait method call lowering と overload selection の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck trait call apply 境界

Stage 1 の typecheck 側の続きとして、`Trait::method` call の self type 推論と bound / impl 確認を `nepl-core/src/typecheck/trait_call_apply.rs` へ分離した。対象は選択済み callable 経路での `FuncRef::Trait` 推論、unbound trait method 経路での receiver / expected self type 推論、trait application argument 推論、impl / type parameter bound による satisfaction 確認、pure context から impure trait method を呼ぶ場合の diagnostic である。

今回の変更は behavior を変えず、通常 function call の HIR assembly と trait method call の capability/type safety 判定を分けた。`function_apply.rs` は 793 lines、`trait_call_apply.rs` は 215 lines となった。trait call は generic bound、impl matching、effect check が交差する境界なので、Stage 1 の module 分割で独立させ、後続の overload selection 分割や Resource IR の capability 参照と混ざらないようにする。この issue は引き続き open とし、次は `function_apply.rs` に残る overload candidate selection / normal call assembly の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck overload selection 境界

Stage 1 の typecheck 側の続きとして、overload candidate selection と曖昧性解消を `nepl-core/src/typecheck/overload_selection.rs` へ分離した。対象は explicit type argument 適用、arity / capture / argument type / expected return による候補絞り込み、pure context での pure overload 優先、signature dedupe、field accessor より通常 overload を優先する規則、concrete / type parameter count / instantiated specificity / declared specificity による tie-break、no match / ambiguous diagnostic である。

今回の変更は behavior を変えず、`function_apply.rs` を selected callable の instantiation と HIR call assembly に寄せ、overload resolution policy を独立 module にまとめた。`function_apply.rs` は 464 lines、`overload_selection.rs` は 372 lines となった。overload selection は type inference、expected type、effect safety が交差するため、後続で resolve / type inference / effect inference の依存方向を整理するときの置換対象として扱う。この issue は引き続き open とし、次は selected callable instantiation / normal call assembly の分離可否を確認する。

## 2026-04-28 Stage 1 typecheck selected call apply 境界

Stage 1 の typecheck 側の続きとして、overload selection 後の selected callable instantiation と normal call assembly を `nepl-core/src/typecheck/selected_call_apply.rs` へ分離した。対象は explicit type argument の再適用、selected function type の instantiation、capture / user argument arity 検査、char literal context 適用、argument unification、selected function effect 診断、inferred type argument 正規化、generic trait bound 検査の呼び出し、field accessor / constructor / trait call への委譲、instantiation table 更新、capture 引数と function value 引数の HIR assembly である。

今回の変更は behavior を変えず、`function_apply.rs` を call entry point / binding lookup / high-level dispatch に縮小した。`function_apply.rs` は 166 lines、`selected_call_apply.rs` は 314 lines となった。通常 call assembly は type/effect/trait/constructor/field 境界が交差するため、Stage 1 で独立 module にしておくことで、後続の resolve / type inference / effect inference / Resource IR lowering の依存方向を整理しやすくする。この issue は引き続き open とし、次は残った `function_apply.rs` の binding lookup orchestration と indirect/unbound trait dispatch の整理、または `driver.rs` の orchestration 分割可否を確認する。

## 2026-04-28 Stage 1 typecheck call binding lookup 境界

Stage 1 の typecheck 側の続きとして、call apply 専用の binding lookup と function value fallback 判定を `nepl-core/src/typecheck/call_binding_lookup.rs` へ分離した。対象は symbol-resolved `FnValue` の callable symbol lookup、qualified call の alias/import 解決、unqualified callable lookup、同名 function-typed value が存在する場合の direct call 回避判定である。

今回の変更は behavior を変えず、`function_apply.rs` から name/import/env lookup の詳細を外し、call entry point は lookup 結果に基づく dispatch に集中する形にした。`function_apply.rs` は 132 lines、`call_binding_lookup.rs` は 61 lines となった。call binding lookup は resolve/name lookup と call application の接点なので、後続で resolve phase と typecheck phase の依存方向を整理するときの境界として扱う。この issue は引き続き open とし、次は `function_apply.rs` に残る unbound trait / indirect call fallback の配置、または `driver.rs` の orchestration 分割可否を確認する。

## 2026-04-28 再レビューでの再オープン

remote main の borrow/raw memory 修正取り込み後、責務集中は解消していない。現時点の line count は `nepl-core/src/typecheck.rs` が 9582 lines、`nepl-core/src/passes/move_check.rs` が 5486 lines で、旧レビュー時の `typecheck.rs` 8759 lines より悪化している。

`typecheck.rs` には overload、trait lookup、match exhaustiveness、literal match、HIR lowering、module/import 補助、diagnostic recovery が同居し続けている。`move_check.rs` も borrow state、field move、raw memory provenance、function/enum/aggregate alias summary を 1 file で持つ状態になっており、S3/S4 self-host 移植時に `resolve/ty/check/hir/resource` へ分ける基準を Rust 側から読み取りにくい。

この issue は再度 open とし、self-host 移植前に少なくとも仕様境界を module 単位で切り出す。Resource IR そのものの設計は `ISS-20260425T000000Z-RV-CORE-009-58589A3F` で追跡し、この issue では typecheck/move_check の責務分離と regression scope 縮小を扱う。

## 旧レビュー本文

- 解決済: true
- 状態: verified
- 優先度: P1
- 種別: architecture
- 対象: `nepl-core/src/typecheck.rs`

### 根拠

- `nepl-core/src/typecheck.rs`: 8759 行。
- `nepl-core/src/typecheck.rs:566`: instantiation cache を型検査本体が直接持つ。
- `nepl-core/src/typecheck.rs:7777`: `Env` / `Scope` 実装も同じファイル内。
- `nepl-core/src/typecheck.rs:8354`: `LabelEnv` / `StringTable` など補助構造も同居。

### 問題

型推論、trait capability、overload、名前探索、HIR lowering、effect 判定、import alias 補助、diagnostic recovery が 1 ファイルに集中しています。局所修正が別機能へ波及しやすく、性能改善も profiling point を切りにくい状態です。

### 影響

既知バグの根本原因を追いにくく、修正のたびに回帰範囲が広がります。`doc/neplg3/impl/compiler_structure.md` で示されている `check/`, `ty/`, `resolve/`, `hir/` 分割方針との乖離も大きいです。

### 修正方針

短期的には `Env`、overload 解決、prefix reduction、trait impl 検査を別 module へ切り出します。中期的には AST flat list から HIR への lowering を型検査結果と分離し、`resolve` / `ty` / `check` の依存方向を固定します。

### 検証

分割ごとに既存 `tests/compiler/**` と stdlib doctest を走らせ、ファイル移動だけの段階では出力 JSON の差分がないことを確認します。
