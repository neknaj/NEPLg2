2026-04-09 Playground

- terminal panel を shared terminal session / shared shell backend に寄せる
- mobile / touch 環境での split / drag UI を再設計する
- `tests/playground_editor/` に multi-file import / completion / fold / problem list 表示の fixture を追加する
- pointer 操作、fold click、scroll、completion UI の surface 回帰を CLI で固定できるようにする
