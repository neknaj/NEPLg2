#entry main
#indent 4

#import "std/math"
#use std::math::*
#if[target=wasi]
#import "std/stdio"
#use std::stdio::*

fn main <()*> ()> ():
    let mut x <i32> 0;
    while lt x 5:
        print "count = ";
        println_i32 x;
        set x add x 1;
